# MASKD 開発引き継ぎ資料

> 別セッション／別担当への引き継ぎ用。この1枚を最初に読めば、すぐに開発を再開できるようにまとめています。
> 最終更新: 2026-07-22 / 到達バージョン: **develop v1.40.0 / main v1.1.7**

---

## 1. プロジェクト概要

- **MASKD** … 仮面をかぶった騎士たちの戦いを描く心理戦・推理カードゲーム（日本語UI）。**1人対戦（プレイヤー vs CPU）**。
- 実体は **`index.html` 1ファイル完結**（約 745KB）。HTML/CSS/JS すべてインライン。カード画像は base64 JPEG を `CARD_IMG` に埋め込み。
- **GitHub Pages で公開**。リポジトリ `perusonao/maskd`、**`main` ブランチ = 本番デプロイ**。
- ルール全文は README.md、変更履歴はアプリ内（ヘッダーの「MASKD」タップ）に `CHANGELOG` として保持。

## 2. デプロイ運用（重要）

- **`main` へ push すると GitHub Pages が自動デプロイ**（ワークフロー名: "pages build and deployment"）。
- このプロジェクトは一貫して **`main` に直接コミット／push** して運用してきた（feature ブランチ運用はしていない）。デプロイ確認も main のワークフロー成功で判断。
- push 後は Actions の該当 run が `completed / success` になったことを確認する。

## 3. 主要コードの場所（`index.html` 内・行番号は目安）

| 内容 | 目印 | 行(目安) |
|---|---|---|
| バージョン定数 | `const GAME_VERSION` | 3390 |
| 変更履歴 | `const CHANGELOG` | 3391 |
| 勝敗判定 | `function judge(dealer, child)` | 2955 |
| 配札 | `function dealHands()` | 1759 |
| マッチ開始（配札演出→） | `function beginMatchSetup()` | 1864 |
| ラウンド開始 | `function startRound()` | 2077 |
| ラウンド開始モーダル表示 | `function showRoundStartModal()` | 2106 |
| モーダル「はじめる」→本編 | `function beginRoundPlay()` | 2117 |
| スコア／名前描画 | `function renderScore()` | 2155 |
| 残り手札ボーナス係数 | `LEFTOVER_TOPN=2`, `AIR_LEFTOVER_FLAT=2` | 1696-1697 |
| CPU戦略 | `let cpuStrategy` | 2772 |
| CPU仮面名 | `const CPU_MASK_NAME` | 2777 |
| プレイヤー名保存(クラウド) | `window.__maskdSaveName` 呼び出し | 1884 |
| カード画像 | `const CARD_IMG` (base64・巨大行) | 1690 |

## 4. ルールの要点（実装済み）

- 全8ラウンド。毎ラウンド親（dealer）／子（child）が交代。親のみ「決戦・撤退・入替」を選べる。
- 5属性 **Sun / Dark / Moon / Kaos / Air** × 数字0〜5 ＝ 全30枚を15枚ずつ分配。
- 勝敗 `judge()`:
  - **Air** が絡む → 必ず引き分け。ただし **Kaos は Air に勝つ**。
  - **Kaos**（Airなし）→ 数字が大きい方勝ち。同値は Kaos 側敗北。特殊: Kaos 0・1 は全属性の5に勝つ／Kaos5 は5に勝つが1に負ける。
  - 同属性 → 数字勝負。異属性(Moon/Dark/Sun) → **じゃんけん**（月✊＞闇✌＞太陽✋＞月）。
  - 終盤決戦（第7・8R）の決戦は得点2倍。
- **最終得点 ＝ 獲得点 ＋ 残り戦略カウンター ＋ 残り手札ボーナス**
  - 残り手札ボーナス ＝ **非Air札の数字上位2枚の合計** ＋ **Air は数字無関係で1枚2点**（`LEFTOVER_TOPN` / `AIR_LEFTOVER_FLAT`）。
  - これは「手札温存偏重」を是正した v0.32.0 の重要修正。小数係数は不採用（ユーザー方針）。

## 5. UI/表示まわりの要点

- カード表示は 2 モード切替（初期=初心者）: `cardStyle` = `'beginner'` | `'rich'`（localStorage `maskd_cardstyle`）。`useRichCards()`。
  - 初心者: 属性色＋大きなじゃんけん記号＋数字＋英語属性名。上級者: リッチな仮面イラスト。
- 属性名は **英語表記に統一**（SUN/DARK/MOON/KAOS/AIR）。
- レイアウトは短い画面で崩れやすい。`@media (max-height: 820/750/680px)` の3段で調整済み。要素を足したら**必ず短い画面(例 402×709)で見切れ確認**すること（過去に何度も再発）。
- v0.32.6 で: 両者の手札枚数/山札枚数表示を撤去、山札は「一番上の裏イメージ」表示、盤面メッセージエリア廃止→ラウンド開始モーダル化。
- プレイヤー名（初回入力・localStorage `maskd_playername` ＋ Firestore 登録）と相手仮面名（積極的=剛力仮面／慎重=鉄壁仮面／トリッキー=技巧仮面）を表示。

## 6. Firebase / データ

- Firestore（projectId `maskd-8eafb`）にゲームログと名前を書き込み。
  - `window.__maskdSaveLog` → コレクション `logs`
  - `window.__maskdFetchLogs` / `__maskdFetchCount`
  - `window.__maskdSaveName` → コレクション `players`（名前は最大12文字）
  - `buildGameLog()` が1試合分のログを組み立て（`playerName` 含む）。
- **バランス調整の進め方**: 実ログ(JSON)を回収 → Node シミュレータ（本物の `judge`/配札/得点を移植）で戦略勝率を検証 → 係数を決める、という流れで進めてきた。

## 7. 検証方法（このリポジトリでの慣習）

- **Playwright ヘッドレス**で実ブラウザ確認:
  - Chromium: `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`
  - モジュール: `/opt/node22/lib/node_modules/playwright`
  - `file:///home/user/maskd/index.html` を開く。名前モーダルで止まるので、テストでは `addInitScript` で `localStorage.setItem('maskd_playername','...')` を先に入れる。
  - 配札演出（dealerDrawOverlay）をクリックで消化 → ラウンド開始モーダル（`#roundStartModal.show`）→ `beginRoundPlay()` で本編へ。
  - 過去のテストスクリプト例（scratchpad・コンテナ揮発）: `progfull.js`(全8R通し), `rscap.js`(モーダル撮影), 各種 `sim*.js`(バランス)。※ scratchpad はセッション消滅で消えるため、必要なら作り直す。
- 変更後は **短い画面での見切れ確認** ＋ **JSエラー0** を最低ライン。

## 8. 直近の到達点（バージョン履歴の要約）

- v0.31系: UI刷新（相手手札を上部・直タップ・VS仕切り・局面ラベル・スコア強調・使用済ミニカード等）
- v0.32.0: 残り手札ボーナスの得点ルール変更（温存偏重の是正）
- v0.32.1: 場札拡大・戦略バー復活・使用済を実画像・自分の山札を一覧表示
- v0.32.2: 山札モーダルをコンパクト化・手札に「相手視点」候補アイコン・相手山札の最上段強調
- v0.32.3: 属性を英語統一・候補はじゃんけん統一・入替モーダル廃止(直タップ)・文字削減
- v0.32.4: 入替=決戦確定（札を選んだらそのまま勝敗へ）
- v0.32.5: プレイヤー名の入力/登録・相手の仮面名表示
- v0.32.6: 手札/山札の枚数表示を削除・山札を裏イメージ表示・ラウンド開始モーダル追加
- v0.33.0: **CPU AI 大幅強化**（親の1手読み・難易度スケーリング修正）。本物のjudge/配札/得点を移植したNodeシミュレータで検証し、強い参照プレイヤー相手の勝率が旧CPUの13〜34%→約49〜54%(強)へ改善。CPU主要関数は `cpuChooseCard`/`cpuDecide`/`cpuWantsForce` ＋ 新規 `cpuEps`/`cpuOppRemainingCards`/`cpuDealerLeadValue`。
- v0.34.0: 勝敗演出のジュース化（3Dめくり `flipIn`／設置 `card-place`／決着インパクト `fireImpact`＋ハプティクス）。`prefers-reduced-motion` で抑制。
- v0.35.0: リザルト/統計拡充（MVPカード・勝率・連勝＝`computeGameMVP`/`computeStreaksAndRate`）＋ 決戦/撤退の公開演出中のnull参照に安全装置。
- v0.36.0: 初心者オンボーディング（初回R1〜R3のみラウンド開始モーダルに「🔰今回のポイント」＝`onboardTip`/`ONBOARD_KEY`）。
- v0.37.0: **ミッション形式チュートリアル**（実際に操作して学ぶ）。データ＝`TUT_MISSIONS`、エンジン＝`openMissions`/`renderMission`/`tutPlayCard`/`tutDecide`/`tutFinish`、専用オーバーレイ `#tutMissionOverlay`（本編エンジンとは独立、`judge()`で答え合わせ）。タイトルの「🎓 ミッションで学ぶ」から起動。`MISSIONS_DONE_KEY`。
- v0.38.0: ①初回ウェルカム画面 `#welcomeModal`＋`welcomeChoose()`（初回はスライド自動表示をやめ、ミッション/遊び方/対戦を選択）。②ミッション追加で全8問に（role `swap`＝入替、`strict`＝温存判定を engine に追加）。③**効果音**（WebAudio合成、`sfx(name)`/`toggleSound()`、キー `maskd_sound`、フッター `#soundToggle`）。フック: `fireImpact`(win/lose)・`resolveFightJudge`(draw)・`fireKaosFlash`/`fireAirFlash`・`playYouCard`・`tutShowResult`/`tutPlayCard`。
- v0.39.0: ①**実績システム**（`ACHIEVEMENTS`・`checkAchievements(ctx)`・`openAchievements()`・キー `maskd_achievements_v1`・モーダル `#achieveModal`）。フック: `endGame`・`tutFinish`。導線: タイトル/結果画面の「🏅 実績」。②ミッション3追加で全11問（role `forceDecide`＝奇襲を追加）。
- v0.40.0: ①**上級ミッション** `TUT_ADVANCED`（本格派4問）。エンジンは `tutList`/`tutSet` で基本(`TUT_MISSIONS`)/上級を切替、`openMissions('advanced')`、`ADVANCED_DONE_KEY`/`advancedCleared()`。導線: タイトル「⚔️ 上級ミッション」＋基本修了後の誘導。②実績6追加で**全18種**（`_maxDeficit`等のヘルパー追加、`endGame`ctxに `summary`/`advancedCleared` を追加）。③対戦画面の「あなたの手札／相手の手札」見出しを削除（`handTitle` 既定空、`ohs-label` 撤去）。④**admin.html**: 対戦ログ表に「名前」列＋検索＋詳細に `playerName` を表示。
- v0.41.0: レイアウト見直し。①トップ: 上部ボタンを `top-learn-btns`（ミッション/上級）＋ `top-info-btns.mini`（実績/遊び方/ルール）の2段に整頓（更新履歴はフッター`MASKD`タップに集約）。②結果画面(`endGame`): 各ラウンド内訳/残り手札/統計を `<details class="ov-details">` で折りたたみ（要点＝スコア/MVP/連勝/実績は上部固定）。「もう一度/とじる」が初期表示内に収まるように。
- **v0.42.0（現在）**: ①CPU思考中インジケータ `#cpuThinking`（`showCpuThinking`/`hideCpuThinking`。CPU手番のsetTimeout前に表示、`cpuActAsDealer`/`cpuActAsChild`/`cpuDecide` 冒頭で解除。CPU待ちは 700/800ms）。②得点リード可視化（`renderScore` でリード側 `#youLP`/`#cpuLP` に `.leading`、`#youLeadMark`/`#cpuLeadMark` に「▲ +N」）。
- **v0.43.0（現在）**: ①盤面レイアウト切替 A(標準)/B(メリハリ)。`boardLayout`/`maskd_layout`、`applyLayout()`/`setBoardLayout()`/`toggleLayout()`、`.app.layout-b` CSS、上段スコアバー `#topScoreBar`（`renderScore`が同期、Bでは `.pinfo .pi-pt` を隠す）。導線: トップ設定 `#layoutOpts`＋フッター `#layoutToggle`。②Kaos/Air演出強化（`.kaos-flash` に紫の衝撃波リング`::before`＋ラベルpop、`.air-flash` のAIRラベルをふわっと浮上）。③**不具合修正**: `.hist-scroll > *{flex-shrink:0}` で対戦ログ一覧の行つぶれ（縦フレックスのshrink）を解消。
- v0.44.0: **段階的ルール開放「はじめての対戦」**。`LEARN_STAGES`(4段階)＋`stageConfig`/`deckAttrs`/`matchRounds`＋`stageAllows()`。縮小デッキ`dealHandsStage()`、`computePossibleAttrs`は`deckAttrs`基準に変更、`startGame(dealer, stage)`でステージ設定、`TOTAL_ROUNDS`→`matchRounds`(通常は8)、`scoreMultiplier`/`isLateGame`は`late2x`フラグ＋`matchRounds-1`でゲート、`renderActions`/`cpuWantsForce`/resolveFightで入替・奇襲をゲート、`endGame`でステージ時は`endLearnStage()`へ（履歴/実績/統計に残さない）。UI: `#learnOverlay`＋`openLearnLadder`/`showStageIntro`/`startLearnStage`/`endLearnStage`、`LEARN_KEY`(進捗)。実績`graduate`。導線: タイトル/ウェルカムの「🎓 はじめての対戦」。※タイトルのボタン構成も再編。
- v0.45〜v0.55: 表示改善の細部修正、ホーム画面のロビー化、ゲームモード選択（初級/中級/上級/本戦）とレベル別ライバル・★実績・仮面あて予想、進行度に応じたオンボーディング、プレイログ計測強化、テーマ文言を「仮面舞踏会」から「仮面騎士の戦い」へ統一。詳細はアプリ内 `CHANGELOG` を参照。
- v0.56.0: **v1.0前の画面整理**。①`openLearnLadder`（「はじめての対戦」4段ラダー）への導線をホーム/ウェルカムから撤去（新しいレベル制と内容が重複していたため）。`LEARN_STAGES`関連の関数・DOM(`#learnOverlay`は練習モード結果画面と共用のため存置)は到達不能なコードとして残置＝**復活させる場合はホームの`.home-learn-link`とウェルカムの選択肢を戻すだけでよい**。②結果画面の下部ボタンを「🏅実績」「📊戦績」の2つに集約（`openHistory`/`openMatchLog`は結果画面から除去、`openMatchLog`は`#statsModal`内に残置）。③`#statsModal`から「みんなの統計（クラウド）」ボタンを除去（`openGlobalStats`/`#globalStatsModal`は到達不能のまま残置）。④`#welcomeModal`を「▶さっそく対戦する」中心の2択に簡素化。⑤`#tutorialOverlay`（遊び方）に「⚔️上級ミッションに挑戦」を常設リンクとして追加（従来は基本ミッション完走後のみ到達可能だった）。⑥①の影響で解除不能になった実績`graduate`（仮面騎士デビュー）を`ACHIEVEMENTS`から削除（19種→18種）。
- v0.57.0: **対戦設定画面(`#instructions`)の再構成**。ユーザー提案のモックアップを元に、番号ステップ`.setup-step`（①レベル→②CPU強さ→③ルール→④ライバル→⑤アクション）に再編。旧`.setup-details`（折りたたみ「詳細設定」）は廃止しCSSごと削除、アクション設定は常時表示のステップ⑤に格上げ。③`updateModeSummary()`は長文パラグラフをやめ`.rule-chips`（属性/アクション/記録/終盤2倍のアイコンチップ）＋`.rule-rounds`（ラウンド数）に変更。④`renderLevelInfo()`に`.lv-note`を追加し、「CPUの強さはライバル自体を変えない（同じ相手の手加減のみ変える）」ことを正確に説明（旧モックアップ案にあった「対戦相手はこの強さのライバルになる」という誤った表現は採用せず修正）。「詳細を見る」ボタン（飛び先未定義）とモックアップの下部タブバー（ホーム/デッキ/対戦/成績/設定＝未実装のナビ構造）は範囲外として見送り。
- v0.57.1: **対戦設定画面を1画面(スクロールなし)に圧縮**。`#instructions`は`.app{height:min(920px,97vh)}`の枠に`inset:0`で収まる構造(`overflow-y:auto`)なので、実際の可視高はビューポートではなく`.app`の高さで決まる点に注意。文言短縮（レベルボタンラベル・ルールチップ・★目標=`LEVEL_META.stars[].short`を新設し1行連結表示・注記1行化）と余白圧縮（`.setup-step`padding/margin・`.lv-*`各要素）で実施。`.lv-name`/`.lv-quote`/`.lv-goals-line`/`.lv-note`に`white-space:nowrap`＋`text-overflow:ellipsis`を付与し、内容量に関わらず行数を固定(高さの予測可能性を担保)。検証は幅402px×高さ820/844px系で全6通り(r1/r2/r3×basic/full)ともscrollHeight===clientHeight(スクロール不要)を確認。ただしiPhone SE等の小型端末(高さ667px前後)では`.app`高が97vh制約で縮むため約130px分の内部スクロールが残る(既知の制約、フォント読み込みタイミングにより実測値は多少変動しうる)。
- v0.58.0: **ホーム画面の整理**（ユーザーの10項目レビューのうち、事実誤認や範囲外(TODO.md B/フリーズ対象C)を除いた7項目を実装。カルーセル化とEXPバー化は保留、デイリーチャレンジ機能は明確に見送り＝TODO.md カテゴリB）。①`.home-cta-img`（対戦する）を`.mask-fan`より上に移動。②メニュー2段(`.home-menu`×2)を`.home-menu-group`(gap6px)でまとめ、`.home-set-img`との間に`.home-menu-divider`（細い金の区切り線）を追加。④ホームロゴの`#versionTagTop`（バージョン表記）を撤去し、`#appSettings`末尾に`#versionTagSettings`として移設（JS側は`document.getElementById('versionTagSettings').textContent=GAME_VERSION`に変更）。⑥お知らせベルに**正直な未読バッジ**（`unseenChangelogCount()`/`markChangelogSeen()`/`updateBellBadge()`、キー`maskd_changelog_seen_version`）。初回(未設定時)はその場でGAME_VERSIONを既読扱いにして0件表示＝導入前の履歴を「未読」として水増ししない設計。以後はバージョンが上がるたびに`CHANGELOG`配列中の位置で件数を算出、`openChangelog()`で既読化。⑦`.home-title`のfont-size 60px→44px（ロゴ縮小）。⑧`MASK_FAN`各要素に`d`(一言説明)を追加：MOON="闇に勝つ"/AIR="多くは引分"/SUN="月に勝つ"/KAOS="特殊ルール"/DARK="太陽に勝つ"（`beats={moon:'dark',dark:'sun',sun:'moon'}`の実ルールから正確に算出。ユーザー提示のサンプル文言＝「推理/入替/得点」等はゲーム内容と不一致のため不採用）。⑨設定ボタンは実在7導線（ミッション/実績/ランク/遊び方/ルール/戦績/設定）のみで統一、架空の「ショップ」等は追加せず。加えて`.home-screen{justify-content:flex-start→center}`に変更（要素縮小に伴い縦長画面で下部に不自然な空白ができたための微調整、7項目の指示範囲内の軽微な追随修正）。検証: 402×874/390×844/375×667の3サイズでホーム画面のスクロール無し・バッジ表示/既読化・設定画面のバージョン表示を確認。
- v0.58.1: v0.58.0の未実装分だった「⑤Lv表示の正直化」を実施。`#hbLv`→`#hbTotal`にリネームし、`1+Math.floor(total/2)`の疑似Lv算出をやめて実対戦数をそのまま「◯戦」（未対戦時は「対戦前」）と表示。勝率バー(`#hbBar`)自体の仕様は変更なし（表示中の「Lv」という誤解を招く枠組みだけ外した）。③カルーセル化は見送りのまま据え置き。
- v0.59.0: **対戦設定画面のレベル選択を再構成＋仮面あての位置づけ変更**。①`#ruleOpts`を「初級／中級／上級／本戦」の4択に統合（`data-val="top"`が本戦、クリック時に`selectedRule='r3'; selectedAction='full'`を同時セット）。旧`#actionOpts`(step⑤「アクション設定」)は完全に削除し、選択中の状態表示は新設の`currentLevelChoice()`(rule+actionから'r1'/'r2'/'r3'/'top'を逆算)で行う。**発見した重要な仕様**: `beginMatchSetup()`は`isTop`(=r3&&full)でないと`_lastPracticeCfg`経由の「練習モード」(`stageConfig.practice=true`)になり、`endPracticeMode()`(`#learnOverlay`使用)へ分岐して**`saveGameToHistory()`が呼ばれない＝ホームの「◯戦」に一切カウントされない**。つまり本戦以外は全部ローカル統計に残らない実装だった(テレメトリ送信`maybeSendGameLog`は練習モードでも呼ばれるので計測自体はできている)。②「🔰初心者ではじめる」ボタン(`startBeginnerPreset()`)を削除。`onHomePlay()`は`isFirstTimer()`なら対戦設定画面自体をスキップして直接初級を開始するため、この画面に到達した時点で見ている人は初心者ルート対象外＝完全に重複した導線だった(v0.56.0で削除した「はじめての対戦」ラダーと同じパターン)。③**仮面あて(読み)を得点計算から分離**：`endGame()`の`youFinal`から`+youReadBonus`を削除。CPU側に対応する仕組みが無く非対称だった点をユーザーが指摘、「独立したミニゲームならアリ、ルールの一部なら不要」との判断で「勝敗に影響しない成績」に格下げ。結果画面の得点内訳テーブルから読みボーナス行を削除し、代わりに`milestoneSection`(MVP/通算成績と同じ場所)に「🎯 仮面あてN回的中（勝敗には影響しません）」を追加。`youReadBonus`変数自体はテレメトリ用に残置(`readBonus:`ログ)、`youReadHits`ベースの実績「看破者」・★目標判定は無変更。UIの誘導文言(`+2pt`を含む予想プロンプト・的中トースト)も全て「勝敗に影響しない」トーンに書き換え。README「特徴」も同様に修正。
- v0.59.1: **アンケート機能を追加**（Phase2「300人にプレイしてもらう」に向けたフィードバック収集基盤）。①`#surveyScreen`（設定画面から`openSurvey()`で遷移）で「面白かった度合い(1-5)」「またやりたいか(ぜひ/たぶん/あまり)」「自由記述(任意・500字)」の3問に回答できる。②Firebase Firestoreの新規`surveys`コレクションに`window.__maskdSaveSurvey()`で保存（`logs`/`players`と同じ匿名パターン、`playerId`は`getPlayerId()`を再利用）。**Firestoreセキュリティルールに`surveys`用のcreate/read許可を追加する必要がある**（index.html内のコメントに例を記載）。③送信時、`https://ntfy.sh/maskd-survey-20260717`（ユーザー個人の秘密トピック名）へブラウザから直接POSTしてスマホへプッシュ通知（ntfyアプリで同トピックを購読していれば届く。サーバー/Cloud Functions不要・失敗しても回答自体はFirestoreに保存済みなので握りつぶす設計）。④`admin.html`にアンケート集計セクションを追加（`surveySectionHTML()`、ダッシュボード最上部に表示。回答数・平均点・またやりたい内訳・個別コメント一覧。読み取り失敗時は`logs`と同様のセキュリティルール案内バナー）。テスト用に`window.__adminMock(logs,count,surveys)`の第3引数を追加。既存のFirebase設定（プロジェクト`maskd-8eafb`）をそのまま流用しているため追加のデプロイ作業は不要、Firestoreルールの追加だけで有効化できる。
- v0.59.2: **ntfy通知が届かない不具合を修正**。`fetch('https://ntfy.sh/'+NTFY_TOPIC, {headers:{'Title':'MASKD アンケート回答',...}})`の`Title`ヘッダー値に日本語（マルチバイト文字）を直接渡していたため、`Headers`が`TypeError: Cannot convert argument to a ByteString`で**同期的に例外を投げていた**（HTTPヘッダー値はASCII/Latin1のみ有効という仕様）。`fetch(...).catch(()=>{})`という書き方は非同期の**rejectionしか**捕まえられず、`fetch()`呼び出し自体が投げる同期例外は外側の`try{}catch(e){}`（中身が空）で握りつぶしていたため、画面上は「送信しました」と正常表示されるのに実際には一度もHTTPリクエストが飛んでいなかった。修正: `Title`を`'MASKD Survey'`（英語）に変更、両方のcatch節に`console.warn`を追加して今後同種の不具合がサイレントに埋もれないようにした。**教訓**: fetchのカスタムヘッダーに動的な文字列（特に日本語）を入れる時は、値がASCII範囲であることを確認するか、`encodeURIComponent`等でエスケープすること。
- v0.59.3: **通知から管理画面へワンタップ遷移**。①`index.html`に`ADMIN_URL='https://perusonao.github.io/maskd/admin.html?pass=maskd-admin'`を追加し、ntfy送信時のヘッダーに`'Click':ADMIN_URL`を付与（ntfyの標準機能。通知をタップするとこのURLが開く）。②`admin.html`に`tryAutoUnlockFromURL()`を追加：起動時に`?pass=`パラメータを見て、既存のパスフレーズ用SHA-256ハッシュ(`PASS_SHA256`)と照合し、一致すれば`sessionStorage`に解錠フラグを立てて自動的にダッシュボードを表示。一致後は`history.replaceState`でURLから`pass`パラメータを消し、アドレスバーや共有時に合言葉が平文で残らないようにしている。一致しない場合は通常通りゲート画面のまま（誤った値でバイパスされないことをPlaywrightで確認済み）。admin.html自体に「このゲートは目隠しであり、真のセキュリティではありません」という既存の注記があり、この設計とも整合している。
- **v0.59.4（現在）**: **結果画面からアンケートへ誘導**。`surveyPromptHTML()`（未回答なら`localStorage`の`maskd_survey_submitted`を見て誘導文言を返す。既に回答済みなら空文字を返し何も出さない）を新設し、本戦の結果画面(`endGame()`の`extra`、`milestoneSection`の直後)と練習モードの結果画面(`endPracticeMode()`の`learnBox`、`.tutm-controls`の直前)の両方に挿入。クリック時は`promptSurvey()`が`#overlay`/`#resultOv`/`#learnOverlay`を全て閉じてから`openSurvey()`を呼ぶ(元がどちらの結果画面でも同じ関数で遷移できる)。Playwrightで「未回答時は表示→クリックで遷移→送信後は次の対戦で非表示」の一連の流れを確認済み。
- **付随作業（コード変更なし）**: v1.0前の実機テストで溜まった`logs`(42件)・`surveys`(5件)をFirestore REST API(読み取りは`allow read:true`のため可能)経由でJSONにバックアップしてユーザーに送付。削除はセキュリティルールで`allow update, delete: if false`のため外部からは実行不可・Firebaseコンソールでのユーザー自身の手動削除が必要（手順を案内済み）。副次的に、`players`コレクションが`logs`/`surveys`のような専用ルールを持たずキャッチオール`allow read,write:if false`に該当し、名前登録機能(`window.__maskdSaveName`)が恐らく常に失敗していたことが判明（今回は対応範囲外として未修正、要望があれば`/players/{docId}`用のルール追加で直せる）。
- v0.60.0: ユーザーからの4件の要望＋UIレビューの残りに対応。①**練習モード(初級/中級/上級)もプレイ記録の対象に**：`endPracticeMode()`内で`computeMatchSummary()`の結果(`_summary`、既存の`maybeSendGameLog`と共有)を使い`saveGameToHistory(youFinal, cpuFinal, _summary, {level:levelKey, isTop:false})`を新規に呼び出す。`saveGameToHistory`のシグネチャに4件目の引数`meta={level, isTop}`を追加し、保存されるゲームオブジェクトに`level`/`isTop`タグを付与（本戦側の呼び出し=`endGame()`も`{level:levelKeyFromAttrs(deckAttrs), isTop:true}`を渡すよう更新）。これによりv0.59.0の引き継ぎメモに書いた「本戦以外は`saveGameToHistory`が呼ばれずホームの『◯戦』にカウントされない」という既知の欠落が解消。実績システム(`checkAchievements`)側への配線は今回のスコープ外（要望が「プレイ記録」のみだったため）。②**ホーム戦績バッジの表記変更**：`renderHomeStats()`の`rateEl.textContent`を`勝率${...}%`のみから`${s.wins}勝（勝率${s.winRate.toFixed(0)}%）`に変更。0戦時の`勝率 –%`表示は変更なし。バッジ幅は`.home-badge`が`display:flex`の可変幅でレイアウト崩れなし（12戦分のダミー履歴を注入してPlaywrightで実測、`.home-badge`幅165px/`.home-top`幅332pxで`.home-bell`と重ならないことを確認済み）。③**プレイヤー登録人数の可視化＋通知**：`submitPlayerName()`内の`window.__maskdSaveName(v)`呼び出し直後に、ntfy(`NTFY_TOPIC`)へ英語ヘッダー(`Title:'MASKD New Player'`)でPOSTする通知ブロックを追加（v0.59.2の教訓＝ヘッダー値は非ASCII不可、に従い日本語は本文(body)側のみで使用）。`admin.html`側は`loadData()`に`players`コレクションの`getDocs`(最大300件)＋`getCountFromServer`を追加、新設`playersSectionHTML()`でKPI件数＋直近登録者一覧を表示。**この機能が実データを表示するには、まだ未適用の`/players/{docId}`用Firestoreルール（v0.59.4の引き継ぎ事項）をユーザー側で追加する必要がある**（`surveys`と同じパターンのルール文を提示済み）。④**ホーム画面メニュー並び替え＋対戦設定画面の整理**：ユーザーの10項目UIレビューのうち、事実確認の上で妥当と判断した項目のみ実装。ホームメニューを「遊び方/ルール/実績」「ミッション/ランク/戦績」の2段に並び替え（初見ユーザーが遊ぶ前に必要な情報を上段に）。`#ruleOpts`のラベル「使う属性（むずかしさ）」→「使用するカード」、`#cpuDiffOpts`各ボタンに`.opt-sub`（CPUが手加減/CPUが本気）を追加、`updateModeSummary()`のルールチップ文言を明確化（「+KAOS（特殊）」→「+KAOS（特殊カード）」等）。既存の「このレベルのルール」「このレベルの主」(旧`step③④`)を`<details class="setup-details">`（要素名は`.ov-details`と同系統のネイティブ`<details>`パターン）で折りたたみ、初見でも①②の選択と対戦ボタンまでを一目で見せる構成に変更。`renderLevelInfo()`に「クリア条件」の見出しラベルを追加。**却下した提案**：レビュー内のカード一言説明の修正案（MOON→「SUNに勝つ」等）は`beats={moon:'dark',dark:'sun',sun:'moon'}`の実ルールと逆で不採用（v0.58.0の既存の正しい文言を維持）。「本戦」を「ランキング対象」とする表現も、ランキング機能自体が未実装(`homeRankSoon()`は「近日公開予定」)のため不採用、実態通り「勝敗を記録」のまま。CTAボタン(`対戦する`)の拡大は、ボタンが固定比率(`830/174`)の画像アセットで既に`max-width:400px`(カラム全幅)に達しており、これ以上拡大すると他要素とアスペクト比が崩れる／余白が破綻するため見送り（ユーザー自身も①について余白崩れを懸念していたため、安全側に倒した）。検証: Playwright(402×874)でホームメニュー順・折りたたみ開閉・CPUサブ文言・ルールチップ文言・クリア条件見出しの表示、および対戦設定画面が折りたたみ込みで`scrollHeight===clientHeight`(スクロール不要)のままであることを確認。4パターンのCPU設定(aggressive/strong, cautious/weak, tricky/normal, random/normal)によるフルゲーム回帰テストもJSエラー0・オーバーフローなしで通過。**なお`/players/{docId}`ルールはこの後ユーザー側で追加・適用済みを確認**（Firestore REST APIで`players`コレクションへの読み取りが`200`（空データ）になり、ルール未設定の別コレクションで試すと`403 PERMISSION_DENIED`になることと比較して確認）。
- v0.60.1: 「管理画面でプレイヤーの一覧（プレイ数）を見たい」への対応。**発見した設計上の欠落**: `players`コレクションのドキュメントは`{name, serverTs}`のみで`playerId`を保存しておらず、`logs`（対戦ログ、`playerId`/`playerName`を保存済み）と正確に突き合わせる手段がなかった。①`window.__maskdSaveName`の引数を`(name)`→`(name, playerId)`に拡張し、`players`ドキュメントに`playerId`フィールドを追加保存（呼び出し元`submitPlayerName()`も`window.__maskdSaveName(v, getPlayerId())`に変更）。②`admin.html`の`playersSectionHTML()`に`playCountFor(x)`を追加：`x.playerId`があれば`allLogs.filter(g=>g.playerId===x.playerId).length`で正確にカウント、無ければ(この変更より前の登録データ)`allLogs.filter(g=>g.playerName===x.name).length`で名前一致にフォールバック（同名の別人がいる場合は不正確になりうる旨を一覧下に注記として明示）。プレイヤー一覧の各行に「◯戦」を追加表示。Playwrightの`__adminMock`に、playerId有り/無し両方のケース(新規3件+旧データ1件+未プレイ1件)を混在させたテストデータで検証し、それぞれ正しい件数(3戦/1戦/2戦/0戦)が出ることを確認。
- v0.61.0: 管理画面(admin.html)に**論理削除(非表示)機能**を追加。ユーザーからの「履歴の論理削除機能は作れるか」への回答として2方式を提示し（①追記型＝既存の`allow create:if true; allow update,delete:if false`という追記のみのセキュリティ設計を一切変えずに済む方式、②直接更新型＝対象ドキュメントに`hidden:true`を直接書き込む方式だが`allow update`を緩める必要があり、admin.htmlのパスフレーズゲートは目隠し程度(真の認証ではない)なので誰でも同じ更新を実行できてしまう懸念がある）、ユーザーは①を選択、対象は「対戦ログ・アンケート回答・登録プレイヤー」の3種類全部を選択。**実装**: 新規コレクション`hidden_records`に`{col:'logs'|'surveys'|'players', docId, action:'hide'|'unhide', ts:serverTimestamp()}`を`addDoc`で追記するだけの設計（対象データ自体は一切書き換えない）。同じ`(col,docId)`に対する最新のイベント(タイムスタンプ順)が現在の非表示状態を表す。admin.html側は`loadData()`で`logs`/`surveys`/`players`の各ドキュメント取得時に`d.id`を`_id`として保持するよう変更（従来は`d.data()`のみでFirestoreドキュメントIDを捨てていた）、`hidden_records`も読み込んで`hiddenMap`(`col|docId`→真偽値)を構築。`isHidden(col,id)`/`visibleLogs()`/`toggleHidden(col,id,hide)`/`hideBtnHTML(col,id)`/`bindHideButtons()`を新設。**統計への反映方針**: バランス分析・KPI・プレイ数などの集計は非表示分を**常に**除外(`visibleLogs()`を使用、トグルの状態に関わらず)。一方、生データテーブル・アンケート一覧・プレイヤー一覧は既定で非表示分を除外して表示しつつ、ヘッダーの「非表示も表示」ボタン(`showHidden`フラグ)をONにすると非表示分もグレーアウト＋「元に戻す」ボタン付きで一覧に混ぜて表示し、目視確認・復元ができるようにした（完全に隠して見えなくすると誤って非表示にした時に気付けないため）。CSV/JSON書き出しも非表示分を除外(`visibleLogs()`)。各セクションに「（うち非表示 N件）」の注記を追加。**発見した副作用**: `showDetail(i)`は`allLogs.indexOf(g)`でグローバルな配列インデックスを使う設計だったため、`filtered()`の元データを`allLogs`→`(showHidden?allLogs:visibleLogs())`に変えても、返るオブジェクトは同一参照なので`indexOf`は変わらず正しく動作する(参照ベースの一致を利用)。`firestore.rules`ファイル(リポジトリに存在する、Firebaseコンソールに貼り付けて使う設計)が`logs`用のルールしか無く、これまでにユーザーが手動でコンソールに追加した`surveys`/`players`用ルールが反映されていなかったことに気づき、`isValidSurvey()`/`isValidPlayer()`/`isValidHiddenRecord()`を追加してリポジトリのルールファイルをユーザーの実際の設定に追いつかせた（今後`hidden_records`のルールを追加する際は、この更新済みファイルを丸ごと貼り直せば良い）。Playwrightで、非表示化→トグルでの表示/非表示切り替え→統計(勝率・KPI)への反映→復元、の一連の流れと、Firebase未接続時に実ボタンをクリックした際の確認ダイアログ/エラーメッセージが正しく出ることを確認。
- v0.61.1: 「プレイヤー名がFirestoreに登録されていなければ登録させてほしい」への対応。**背景**: `players`コレクションはv0.60.0リリース後もしばらくセキュリティルール未設定でキャッチオールのdeny-allに該当し(ユーザーが手動でFirebaseコンソールにルールを追加するまでの間)、その期間に名前を入力したユーザーは`window.__maskdSaveName`が常に失敗していた(黙って握りつぶされていた)。`showNameModal()`は`playerName`(localStorage)が空の時だけ表示される設計のため、既に名前を入力済みのそうしたユーザーは今後も二度と登録画面を通らず、ルールを直しても永久にクラウド未登録のまま取り残される問題があった。**実装**: 新規キー`PLAYER_NAME_SYNCED_KEY='maskd_playername_synced'`に「クラウドへの登録に成功した名前」を記録する。`syncPlayerNameToCloud(name)`(新設)が`window.__maskdSaveName(name, getPlayerId())`を呼び、成功時のみこのキーを更新(失敗時は更新しない→次回また再送を試みる、というリトライ設計)。`submitPlayerName()`は直接`window.__maskdSaveName(...)`を呼んでいた箇所をこの関数呼び出しに置き換え。`ensurePlayerNameSynced()`(新設)は「`playerName`はあるが`PLAYER_NAME_SYNCED_KEY`が現在の名前と一致しない(=未登録 or 登録未確認)」場合にのみ`syncPlayerNameToCloud`を呼ぶ救済関数で、Firebaseモジュールの初期化ブロック内、`window.__maskdLoggingReady=true`の直後(＝`window.__maskdSaveName`が実際に使える状態になった直後)から`window.ensurePlayerNameSynced()`として1回だけ呼ばれる(通常の初回登録フローとは別経路)。**テスト上の制約**: このサンドボックス環境はgstatic.com(Firebase CDN)への外部アクセスがブロックされているため、`window.__maskdLoggingReady`が実際にtrueになる場面をPlaywrightで再現できず、自動トリガー呼び出し自体はコードレビューでのみ確認。関数のロジック(未同期なら送る/既に同期済みなら送らない/名前未設定なら何もしない/送信失敗時はフラグを立てずリトライ余地を残す/`submitPlayerName()`経由でも同じ経路でフラグが立つ)はPlaywrightで`window.__maskdSaveName`を直接スタブして5パターン全て検証済み。
- v0.61.2: admin.htmlのレビュー対応中に発見した重大な既存バグの修正。**発見の経緯**: ユーザーからのadmin.htmlレビューへの回答を準備する過程で、v0.55.0で「実装済み」とされていたセッション開始/対戦開始/離脱/チュートリアル開始・完走の軽量イベント計測(`logEvent()`、`window.__maskdSaveLog`経由で`logs`コレクションへ書き込み)が、`firestore.rules`の`isValidLog()`が`winner`/`playerFinal`/`cpuFinal`を無条件で必須にしていたため、実際には一度も保存できていなかったことが判明(軽量イベントのペイロードはこれら3項目を持たない設計のため、Firestoreのルール違反で毎回create拒否されていた)。レビューが求めていた「離脱率」KPIは、そもそも元データが存在しない状態だった。**修正**: ①`isValidLog()`を「完了した対戦の記録(winner等必須)」または「軽量イベント(`type`が既知の5種類のいずれか)」のどちらかを満たせばOKとするOR条件に変更(既存の完了対戦の記録の互換性は維持)。②`admin.html`: `logs`コレクションに対戦記録とイベントが混在するようになるため、`isGameRecord(g)=(!g.type||g.type==='game')`を新設し、`visibleLogs()`(既存のゲーム統計・生データテーブル・CSV/JSON書き出し全てが依存する)をイベント除外込みに変更(混入すると勝率などの集計が壊れるため)。新設`eventStatsHTML(eventLogs)`でイベント件数(セッション開始/対戦開始/離脱/チュートリアル開始・完走)と、そこから計算した離脱率(離脱/対戦開始)・チュートリアル完走率(完走/開始)を表示するカードを追加(`render()`のKPIグリッド直後、および「対戦ログがまだ0件」の早期returnルートの両方から呼べるよう関数として切り出し)。イベントが0件の間はセクションの代わりに注記のみ表示。**ユーザーの実際のFirestoreルールとの食い違いも発覚**: ユーザーが管理コンソールに貼り付けていた実際のルールを見せてもらったところ、`hidden_records`のブロックが`isValidHiddenRecord()`を呼び出しているのに、その関数定義自体が存在しない状態だった(関数の未定義参照はルールのコンパイルエラーになり、ルールセット全体の公開に失敗している可能性が高い)。また`isValidSurvey()`は`fun is number`ではなく`fun is int`、`size()<=10`ではなく`<=20`、`players`は`isValidPlayer()`を使わず`allow create: if true`のみ、と要所要所でユーザー側の手動編集により差分があった。実害のない差分(int/number、サイズ上限、players簡略化)はユーザーの現状を尊重してそのままにし、実際に壊れていた2点(未定義の`isValidHiddenRecord`、`isValidLog`が軽量イベントを弾く)だけを修正した完全な一枚のルールファイルを新たに提示（`firestore.rules`と同一内容、丸ごと貼り替えれば良い）。テストはv0.61.0と同様のPlaywright `__adminMock`パターンで、対戦記録とイベントが混在するデータ・イベントのみのデータ・完全に空のデータの3パターンについて、統計値がイベントに汚染されないこと・離脱率/完走率が正しく計算されること・生データテーブルにイベント行が混入しないことを確認。
- v0.61.3: ユーザー提供のスマホ実機スクリーンショット(ラウンド結果画面)から「レイアウトが崩れている」不具合を特定。**調査**: 当初は`.rdetail`(結果説明文、ヒントON時は`beginnerResultNote()`による3すくみ解説などが追記され長くなる)が`.result-card`の高さを超えて`.field`からはみ出すオーバーフロー説だと仮定したが、Playwrightで402×709/402×874双方で長文ケースを再現しても`.result-card`は`.field`内に収まり(`cardOverflowsFieldTop/Bottom`とも`false`)、この説は棄却。実機スクショを詳細に拡大したところ、`.result-card`(幅82%)の左右の余白部分に、背後の盤面(スコア表示・使用済み札などの明るい色のUI)がうっすら透けて見えていることが判明。原因は`.result-overlay`の背景`rgba(13,12,20,0.72)`が72%不透明でしかなく、`.result-card`自体(96%不透明)がカバーしない左右9%ずつの余白では背後がほぼそのまま透けてしまう設計だった。Playwrightで実際に対戦を最後まで進めて`win`/`lose`の結果画面を撮影し、結果カード右端に「敗」の字が透けて見えることを実機と同様に再現・確認。**修正**: `.result-overlay`の背景を`rgba(13,12,20,0.94)`に変更(`.result-card`本体の96%不透明に近い水準へ)。再度同じ手順で撮影し、透け込みが解消されたことを確認。4パターンのフルゲーム回帰テストも通過。
- v1.0.0（正式版フリーズ宣言）: TODO.mdの「v1.0完成の定義」4項目をあらためて棚卸しし、A4(計測)がv0.61.2で・A5(Launch整備)が動画/X投稿文完成で・②(見切れなし)が今回のレイアウト修正で、いずれも既に満たされていることを確認。最後に残っていた①「初訪問ユーザーが詰まらず1戦できる」を、このセッションで初めて**本物のコールドスタート**(localStorage完全に空)でPlaywright検証：ウェルカムモーダル→「さっそく対戦する」→名前入力モーダル→ライバル登場→親決め(0カードを1枚めくる)→配札演出→初級4ラウンド完走→結果画面、まで一気通貫でエラー0・横スクロール見切れなし(402×709の短い画面)を確認。全項目達成を確認した上で`GAME_VERSION`/`CHANGELOG`を`v1.0.0`に更新し、フリーズを宣言。

  **今後のリリース運用（ユーザーとの合意）**: v1.0.0以降は用途に応じてブランチを使い分ける。
  - 不具合修正 → バージョンは`1.0.x`で加算 → `main`に直接push（従来通りの運用を継続）。
  - 仕様変更を伴う修正 → バージョンは`1.x.0`で加算 → `develop`ブランチにpush。
  - `develop`にpushした内容を実機確認できるプレビュー環境が必要、という要望があり検討中(下記参照)。

  **プレビュー環境の設計候補**: 現在のGitHub Pagesは「ブランチからデプロイ」方式(`main`固定、Settings上の設定。リポジトリに`.github/workflows/`のカスタムワークフローは存在せず、"pages build and deployment"はGitHubが自動生成する同期ワークフロー)。`develop`用のプレビューを追加する方法を2つ検討した：
  1. **GitHub Actions化 + 合成デプロイ**: Pages設定を「GitHub Actionsから配信」に切り替え、`push: [main, develop]`いずれのトリガーでも「`main`の内容をルートに、`develop`の内容を`/dev/`配下に」両方チェックアウトして1つのアーティファクトとしてデプロイするワークフローを新設する。両ブランチの履歴を汚さずに済むのが利点。ただしPages配信方式の切り替えに管理者権限が必要な可能性があり、要検証。
  2. **`main`へのボットコミット方式**: `develop`にpushされたら、その内容を`main`の`dev/`サブフォルダにコピーしてコミット・pushするワークフロー。Pages設定は変更不要ですぐ作れるが、`main`の履歴に`github-actions[bot]`名義の自動コミットが増える(`dev/`フォルダのみに限定されるが)。
  外部ホスティング(Netlify/Vercel等の「ブランチごとに自動でプレビューURLが出る」機能)も選択肢としてはあるが、ユーザーの外部アカウント連携が必要なためこのセッションでは完結できない。

  **→ 方式2を採用・実装済み**（Pages設定変更が不要ですぐ動かせるため）。`.github/workflows/dev-preview.yml`を新設：`develop`へのpushをトリガーに、`main`をチェックアウトして`develop`のランタイム資産(index.html/admin.html/manifest/画像アイコン類、計14ファイル)を`dev/`フォルダへコピーし、差分があれば`github-actions[bot]`名義でコミット・pushする。結果、同一のPagesサイトから`https://perusonao.github.io/maskd/`(本番=main)と`https://perusonao.github.io/maskd/dev/`(確認用=develop)の両方が見られる。PWAマニフェスト・画像参照は元々相対パスのため、サブフォルダ配信でもパス書き換え不要だった。ボットコミットで`main`の履歴が増える副作用はユーザーに説明済み・了承済み（「今は気になりません」）。

  **gitタグのpushで判明した制約**: `git tag`のpushだけ`HTTP 403`で失敗する(ブランチのpushは問題なく通る)。この環境のgit用ローカルプロキシがタグの新規ref作成を許可していない可能性がある(`create_trigger`/`send_later`等の一部MCPツールで見られた「承認が必要」エラーと同系統の制限とみられる)。対処として、タグ作成はユーザー自身にGitHub Web UIの「Releases → Create a new release」(タグ名入力するとreleaseと同時にtagも作成される)、またはユーザー自身の環境からの`git push origin <tag>`を案内する運用にした。`mcp__github__get_tag`で作成後のタグ存在は確認できる。

- **v1.0.1（現在）**: 「v1.0.0より前の変更履歴は管理画面のみで確認できるようにしたい」への対応。**実装**: index.htmlの`CHANGELOG`配列から、v1.0.0より前の全エントリ(v0.1.0〜v0.61.3、'旧バージョン'含む、計102件)を削除し、`v1.0.1`/`v1.0.0`の2件のみを残した(プレイヤー向けのお知らせベル`openChangelog()`・未読バッジ`unseenChangelogCount()`は元々`CHANGELOG`配列をそのまま参照する実装だったため、フィルタ処理を追加する必要はなく、データ自体を削るだけで両方に反映される)。削除した102件は一字一句そのまま`admin.html`に`PRE_V1_CHANGELOG`という新規配列として移設し、`preV1ChangelogHTML()`(新設)で「📜 開発履歴アーカイブ（v1.0.0より前）」という見出しの`<details>`(既定は折りたたみ)として表示するようにした。ダッシュボード最下部(生データテーブルの直後)、および対戦ログ0件時の早期returnルートの両方から呼ばれる。**注意点**: `admin.html`の`<script type="module">`はモジュールスコープのため、`PRE_V1_CHANGELOG`はブラウザの外部(例えばPlaywrightの`page.evaluate`からの直接アクセスや将来的な別スクリプトからの参照)からは見えない。ページ内の他の関数(`render()`等、同じモジュール内)からは通常通り参照できるため機能上は問題ない。検証時もこの点を踏まえ、DOM上に実際に描画された内容(`.rrow`要素の件数や中身)で正しさを確認する方式に切り替えた(102件・先頭v0.61.3・末尾「旧バージョン」を確認)。**既存ユーザーへの影響**: `unseenChangelogCount()`は「前回既読のバージョン」を`CHANGELOG.findIndex()`で探す実装のため、pre-1.0のバージョン文字列(例:'v0.61.3')が既読として保存されている端末では`findIndex`が`-1`を返し、`idx===-1?0:idx`のフォールバックにより未読件数は単に0になる(エラーにはならない、不自然に大きな数字も出ない)。

- **v1.0.2（現在）**: 「開発用の環境（`/dev/`）で遊んだ結果を本番用のデータに混ぜたくない」への対応。**背景**: `dev-preview.yml`ワークフローが`develop`の`index.html`/`admin.html`をそのまま`main`の`dev/`配下にコピーする設計のため、`/dev/index.html`と本番の`/index.html`は同一のFirebaseプロジェクト(`maskd-8eafb`)・同一コレクション(`logs`/`surveys`/`players`/`hidden_records`)を見に行ってしまい、動作確認中のプレイ・アンケート・名前登録がそのまま本番集計に混入する状態だった。**実装**: 両ファイルとも`location.pathname.includes('/dev/')`でランタイムに実行環境を判定し(`isDevPreview`)、真であれば全てのFirestoreコレクション名に`_dev`サフィックスを付けて読み書きする(`col(name)`/`envCol(name)`ヘルパー、Firebaseプロジェクト自体は共通のまま論理的にコレクションだけ分離)。index.html側は`window.__maskdEnv`('prod'/'dev')を新設し、`__maskdSaveLog`/`__maskdSaveName`/`__maskdSaveSurvey`/`__maskdFetchLogs`/`__maskdFetchCount`の5関数すべてを`col(...)`経由に変更。admin.html側は既存の`isHidden(col,id)`/`toggleHidden(col,id,hide)`が`col`という引数名を「非表示対象の種別」の意味で既に使っていたため、名前衝突を避けて`envCol`という別名で実装(`loadData()`内の7箇所のFirestore参照＋4箇所のルール案内エラーメッセージを`envCol(...)`に変更)。人間が一目で気づけるよう、index.htmlのホーム画面に`#devEnvBadge`(「DEV PREVIEW（開発確認用・本番とは別データ）」、`/dev/`判定時のみ表示)、admin.htmlのヘッダーに`#envBadge`(「DEV」、同条件で表示)を追加。`firestore.rules`にも`logs_dev`/`surveys_dev`/`players_dev`/`hidden_records_dev`の4コレクションを追加(各バリデーション関数は本番と共用、コレクション名のみ分離)。**要作業**: この4つの新規ルールはリポジトリのファイルを更新しただけでは有効にならず、Firebaseコンソールの「Firestore Database→ルール」タブに`firestore.rules`の全文を再度貼り付けて公開する必要がある(過去の`surveys`/`players`/`hidden_records`ルール追加時と同じ運用)。**検証の制約**: このサンドボックス環境はFirebase CDN(gstatic.com)への外部アクセスがブロックされているため、`window.__maskdEnv`や実際のFirestore書き込み先を実ネットワーク経由では確認できない(v0.61.1と同じ制約)。かわりに、Firebase呼び出しに依存しない部分(`#devEnvBadge`/`#envBadge`の表示切り替えロジック)をPlaywrightで検証：`/`相当のパスでは両バッジとも非表示、ローカルにコピーした`dev/index.html`・`dev/admin.html`(パスに`/dev/`を含む)ではそれぞれ正しく表示されることを確認。4パターンのCPU設定によるフルゲーム回帰テストも新規JSエラーなしで通過（既存のFirebase CDN到達不能によるネットワークエラーのみ、これは今回の変更と無関係の既知の制約）。

- **v1.0.3（現在）**: ユーザーが実機で「ntfyの新規プレイヤー登録通知は届いたが、admin.htmlの登録プレイヤー一覧には該当する登録が見当たらない」ことに気づき報告、その原因調査と修正。**原因**: `submitPlayerName()`が`syncPlayerNameToCloud(v)`(Firestoreの`players`への非同期書き込み)の**完了を待たずに**、直後で無条件にntfy通知(`Title:'MASKD New Player'`)を送信する作りになっていた。`syncPlayerNameToCloud`は`window.__maskdSaveName`が未定義(Firebaseモジュールの初期化がまだ終わっていない、あるいはFirebase CDNへの到達自体に失敗している等)の場合はエラーも出さず即座に何もせず返る設計のため、「書き込みは実質的に試みられてすらいないが、通知だけは届く」という食い違いが起こり得た(v0.59.2のntfyヘッダー起因の失敗、v0.61.1のplayers未登録と同系統、「通知と実処理が分離していて通知だけが独り歩きする」というこのコードベースで繰り返し出てきたバグパターン)。**修正**: `syncPlayerNameToCloud(name)`を、成功/失敗の真偽値を解決するPromiseを返す形に変更(`window.__maskdSaveName`未定義時・例外時は`Promise.resolve(false)`)。`submitPlayerName()`側は、このPromiseの`.then(ok=>{ if(!ok) return; ...ntfy送信... })`の中に通知処理を移動し、実際に書き込みが成功した場合のみ通知するようにした。一方、モーダルを閉じてオンボーディングの次のコールバック(`_afterName`)を呼ぶ処理は従来通り同期的(Firestore書き込みの完了を待たない)のままにしてあり、体験速度への影響はない。`ensurePlayerNameSynced()`(過去の未登録プレイヤーを救済する再送経路)は`syncPlayerNameToCloud`を直接呼ぶだけで通知は送らない、という既存の挙動も変更していない(通知を出すのは新規登録操作の直後だけ、という従来の意図を保った)。**検証**: Playwrightで`window.__maskdSaveName`を3パターン(成功/失敗を返す/未定義のまま)でスタブし、`fetch`をフックしてntfyへのPOST回数を計測、成功時のみ1回・それ以外は0回であることを確認。4パターンのCPU設定によるフルゲーム回帰テストも新規JSエラーなしで通過。

- **v1.0.4（現在）**: ユーザーが実機スクリーンショット(初心者向けカード表示・上級者向けカード表示の対戦画面2枚)で「自分の手札のおすすめがはみ出てます」と報告。**原因**: 手札の「おすすめ札」の印(`.rec-badge`、`{position:absolute; top:-7px; right:-6px}`でカードの右上角に少しはみ出す形で重ねて表示する設計)が、初心者向けカード(`.bcard`、数字・じゃんけん記号・属性名を大きく出す表示モード)の`overflow:hidden`によって角丸の外側で切り取られ、バッジのテキストがほぼ読めない状態になっていた。上級者向けカード(`.hcard.art`、仮面イラスト表示)は`overflow:hidden`を持たないクラス構成のため同じ問題が起きず、ユーザーが送ってくれた2枚の実機画像を比較して発見できた。`.bcard`に`overflow:hidden`が必要な本来の理由は、内部の数字(`.bc-num`)・じゃんけん記号(`.bc-glyph`/`.bc-big`)・属性名バナー(`.bc-name`、カード下端いっぱいの帯)がカードの角丸からはみ出さないようにするためで、`.rec-badge`(カードの外に少しはみ出す設計のバッジ)を巻き添えにしていたのが本質的な原因。**修正**: `.bcard`自体からは`overflow:hidden`を外し、代わりに`beginnerInner()`が返すカード内部要素(`bc-num`/`bc-glyph`/`bc-big`/`bc-role`/`bc-name`)だけを新設の`.bc-inner`(`position:absolute;inset:0;overflow:hidden;border-radius:inherit`)で包んで、そちらにoverflow:hiddenを移した。`.rec-badge`と相手からの見え方表示(`.hc-seen`)は`beginnerInner()`の外側、`.hcard.bcard`要素の直接の子のまま(`el.innerHTML=beginnerInner(c)+recBadge+seenRow`)なので、`.bc-inner`によるクリップの対象外になり、上級者向け表示と同じくカードの外に正しくはみ出して表示されるようになった。`border-radius:inherit`により、`.bc-inner`は`.hcard`(手札・9px)/`.card`(場札)/`.uc`(使用済み小札・4px)など、`.bcard`と組み合わさる文脈ごとの角丸半径をそのまま引き継ぐため、追加の個別調整は不要だった。`beginnerInner()`は`faceCardHTML()`・使用済み札プレビュー・対戦中の場札表示・チュートリアルなど複数箇所で共用されている関数のため、この修正で全ての初心者向けカード表示に一括で適用される。**検証**: Playwrightで`youHand`/`dealerCard`/`possibleAttrsByNum`を直接操作し、SUN✋がMOON✊に確実に勝てる状況を人工的に作って「おすすめ」バッジを発生させ、修正前は`.hcard`(=`.bcard`)の`overflow`が`hidden`でバッジが角丸に切り取られていたのに対し、修正後は`overflow:visible`(バッジは切れない)かつ内部の`.bc-inner`は`overflow:hidden`(数字・属性名バナーは従来通りきれいに角丸内に収まる)であることをスクリーンショットと計算スタイルの両方で確認。4パターンのCPU設定によるフルゲーム回帰テストも新規JSエラーなしで通過。

- **v1.1.0（develop）**: Phase2実プレイヤーからの3件の低評価フィードバック(★1「またやりたい:あまり」、いずれもv1.0.3時点)への対応。**内容**: ①「相手からの見え方や相手の札の予想がヒントとして与えられている基準が分からない」②「『遊び方』の『迷ったらルールを見ろ』は、『詳細はルールに書いてあるので確認せよ』の方が良い。カードの配分について遊び方に何も書かれておらず初戦が意味不明だった」③「『看破』しているはずなのに予想した札が間違っていることがある」。**③の調査(最重要)**: 「看破」が誤って表示される＝推理ロジックのバグではないかと疑い、Playwrightで実際のフルマッチを多数(最終的に16マッチ・約3,000箇所)自動プレイさせ、`showAttrHint()`と同じ入力(`remainingPossibleForNum(num)`)を使って「看破バッジが出た瞬間の予測」と「実際にめくられたカードの属性」を全て突き合わせる検証を行った。1回目の検証では144件もの「矛盾」が見つかり肝を冷やしたが、原因は検証スクリプト側の不備だった：`dealerCard`/`childCard`はラウンド解決後(`allUsedPoolCards`更新後)も次ラウンド開始まで値が残り続けるため、ポーリングのタイミング次第で「解決済みラウンドの、もう使用済みになったカード」を「まだ推理中のカード」として誤って判定してしまっていた(`roundLog`に該当ラウンドの結果が積まれた後の状態を見ていた)。`roundLog.some(r=>r.round===round)`でこの解決済み状態を除外するよう検証ロジックを直したところ、矛盾は0件になった。**結論**: 推理ロジック(`computePossibleAttrs`/`remainingPossibleForNum`/`allUsedPoolCards`)自体に不具合はなく、実際に起きていたのはおそらく「🎭予想」(候補が2つ以上残っている間だけ出る、外れて当然の任意ミニゲーム)と「🔓看破」(候補が1つに絞れた時だけ自動表示される、100%確定のバッジ)の混同。実績名「看破者」が実は別機能(仮面あての的中回数)を指しているという用語の重複も紛らわしさに拍車をかけていたと判断した。**実装**: ①③をまとめて解決するため、ルール説明画面(`renderRulesGuide()`)に他の幕と同じ図解つきの体裁で新章`<div class="rg-ch" id="rg-deduction">`「第八幕：推理と看破」を新設(旧「細則」内の1行だった簡素な説明をここへ移設・拡充)。「候補複数(🎭予想・外れてOK)→候補1つ(🔓看破・自動確定)」の対比を`.rg-mult`/`.rg-mtile`/`.rg-arrow`(第六幕の終盤倍率表示で使っていた既存クラスを流用)で視覚化し、`.rg-list`で3つの状態(予想/看破/読まれている)の違いを明文化。②対戦中に実際に候補アイコンが表示される4箇所(`showAttrHint()`の予想中/看破確定/複数候補時の3分岐、`showYouCardHint()`の読まれている/複数候補時の2分岐)全てに、新設`deductionInfoIcon()`(小さな`ⓘ`、CSSクラス`.ddc-info`)を追加。タップすると新設`openRulesAt('rg-deduction')`がルール画面を開いた上で該当セクションまで`scrollIntoView`で自動スクロールする(Playwrightで、対戦中に🎭予想ヒントに表示された`ⓘ`をタップ→ルールモーダルが開き→`#rg-deduction`が画面上部近くまでスクロールされることを確認)。③「遊び方」(`TUTORIAL_STEPS`)に新ステップ「配札のしくみ」を追加(「カードの出し方」の直後、5属性×0〜5の全30枚が15枚ずつ分配され5枚が初期手札・10枚が山札になることを説明)、最終ステップの文言を「画面下のヒントでも一部は分かるが、詳細はルールに書いてあるので、迷ったらそちらを確認しよう」という趣旨に書き換え。**検証**: 新ステップ・最終ステップの文言、ルール画面の新章の存在と項目数、ⓘタップによるスクロール、4パターンのCPU設定によるフルゲーム回帰テスト、いずれもPlaywrightで確認。**運用**: 仕様変更(新規コンテンツ追加)にあたるため、ユーザーの運用方針に従い`1.x.0`として`develop`ブランチにpush(`main`には反映していない)。

- **v1.2.0（develop）**: v1.1.0に続く、ユーザーからの「根本的に見直したい」という要望への対応・第1弾。**背景（合意した設計方針）**: ユーザーとの複数往復の相談の末、以下を段階的に進めることで合意。
  1. **チュートリアル**を全7段階に拡充し、実際の対戦をプレイしながら段階的にルールを覚える形にする(今回はこれ)。
  2. **実戦**を再構成(チュートリアル完了必須化・CPU強さ+対戦相手タイプのみ選択・★クリア条件廃止で単純な勝敗表示に)。※今回は未着手。
  3. **実績・称号**を新しい構造に合わせて作り直す。※今回は未着手。
  この3段階のうち、以下の設計判断を経て段階1のみを今回実装した：①チュートリアルの内容は「山札の構成(全30枚・15枚ずつ分配)」「ヒント」「相手/自分の手札の読み方」を含めるが、これは全属性(5属性)を覚えた後でないと意味を持たないため、当初案の「ステージ1」ではなく「ステージ4」で教える、とユーザーが訂正(「ルールに記載されている順番とは異なりますが」という補足あり)。②仮面あて(🎭タップ予想)はチュートリアル中は出さない方針とし、実装は「読み合い」ステージだけでなく段階チュートリアル全体で無効化する形にした(コード上、後から個別ステージだけ有効化するのも容易な設計)。③対戦相手タイプ(積極的/慎重/トリッキー)は、現行の`LEVEL_META`のライバル(アルル/ゼファ/ノクス)をそのまま流用する方針をユーザーが指定(名前・画像は別途差し替え予定)が、この対応自体は段階2の作業のため今回は未着手。
  **実装**: 既存の`LEARN_STAGES`(4段階・v0.44.0で実装、v0.56.0でナビゲーションのみ撤去され休眠していた仕組み)を7段階に拡張。
  ```
  1. 色の対決(moon/sun/dark, 4R, 決戦/撤退のみ, ヒントOFF)
  2. 風を読む(+air, 4R, ヒントOFF)
  3. 混沌を制す(+kaos, 4R, ヒントOFF) ※旧仕様は6Rだったが、ユーザー指定の4Rに変更
  4. 読み合い(全属性, 6R, ヒントON=新規, デッキ構成+看破/読まれているを教える)
  5. 戦略・奇襲(6R, force=true)
  6. 戦略・入替(6R, swap=true)
  7. 最終決戦(8R, フルルール=late2x/leftoverも有効)
  ```
  各ステージオブジェクトに新設の`hints`フラグ(true/false)を追加。ステージ中だけ`hintsEnabled`を一時的に上書きし(`startLearnStage()`で退避→上書き、`closeLearn()`/`endLearnStage()`で復元)、**プレイヤー本来のヒント設定(localStorage)には一切触れない**設計にした(ステージを抜けると必ず元の設定に戻る)。`showAttrHint()`/`showYouCardHint()`(候補アイコン・看破・読まれているの表示)は、新設`inTutorialStage()`(`stageConfig`があり`practice`ではない=段階チュートリアル中)と`!hintsEnabled`が両方成立する時だけ内容を空にする条件を追加。**この条件分岐が今回の実装で最も注意した点**：単純に`if(!hintsEnabled)`とすると、通常対戦でヒール設定をOFFにしているプレイヤーの候補アイコン表示まで消えてしまう(候補アイコン表示はこれまで`hintsEnabled`を一切参照せず、ヒント設定に関わらず常時表示されていた=推理要素は「初心者ヒント」ではなく通常ゲームプレイの一部という既存設計だったため)。Playwrightで「通常対戦・ヒントOFF」でも候補アイコンが従来通り表示されることを確認し、この副作用を回避できていることを検証した。`canPredictNow()`にも`inTutorialStage()`のガードを追加し、仮面あて(🎭)の予想UI自体をチュートリアル中は一切出さないようにした(候補アイコン・看破・読まれているの表示自体は出る。あくまで「タップして当てずっぽうする」寄り道機能だけを隠す)。
  ラダー画面(`openLearnLadder()`)のタイトルを「🎓 はじめての対戦」から**「🎓 チュートリアル」**に変更、段階数のハードコード「4段階」を`LEARN_STAGES.length`参照に変更。ステージ紹介画面(`showStageIntro()`)の「相手は弱め」固定表示を`st.cpuDiff`に応じた動的表示に修正し、「入替・奇襲あり/なし」の二択だった行動表示を実際に解禁されている行動(奇襲のみ/入替のみ/両方/なし)を正確に示すよう修正(以前からあった軽微な不正確さの副次的な修正)。ウェルカム画面(`#welcomeModal`)の2択のうち「🎯 先にミッションで学ぶ」(旧・独立クイズ式ミッションへの導線)を**「🎓 先にチュートリアルで学ぶ」**に差し替え、新チュートリアルへ誘導するようにした(`welcomeChoose('learn')`は元々ディスパッチャに存在していたが、v0.56.0以降どのボタンからも呼ばれない到達不能コードだった。今回そこに新しいボタンを繋ぎ直した形)。旧・独立クイズ式ミッション(`TUT_MISSIONS`/`TUT_ADVANCED`、`#tutMissionOverlay`)自体は削除せず、遊び方画面等からの導線は残置(段階2・3の設計時に改めてどうするか判断する)。
  **検証**: Playwrightで、①全7ステージの属性/ラウンド数/行動解禁/ヒントフラグが設計通りであること、②ステージ1(ヒントOFF)ではCPUカード公開時も候補アイコン欄が空になること、③通常対戦でヒント設定をOFFにしていても候補アイコンは従来通り表示されること(重要な非破壊確認)、④ステージ4(ヒントON)では候補アイコン・ⓘは出るが仮面あて予想チップは出ないこと、⑤ステージ1を実際に4ラウンド通しでプレイしてクリアし、ヒント設定が元の値に復元されること、⑥ステージ7(最終決戦)をクリアし「本編を始める」を押すとエラーなくライバル登場演出(既存の実戦導線)に遷移すること、をそれぞれ確認。4パターンのCPU設定によるフルゲーム回帰テスト(通常対戦)も新規JSエラーなしで通過。**運用**: 段階1のみの実装で、段階2(実戦再構成)・段階3(実績/称号再設計)は未着手のため引き続き仕様変更として`1.x.0`・`develop`ブランチにpush(`main`には反映していない)。

- **v1.3.0（develop）**: v1.2.0に続く、段階2「実戦の再構成」。**確定した設計**（ユーザーとの往復で合意）: ①実戦はチュートリアル(全7段階)完了が前提。②CPUの強さは選択できるが、使用するカード・選べるアクション(レベル/ルール)は選択不可(常にフルルール)。③「ボス」という概念はやめ、タイプ別の対戦相手(既存のライバル=アルル/ゼファ/ノクスを流用、名前・画像は暫定で後日差し替え予定)にする。④クリア条件(★)ではなく、単純な勝敗だけを表示する。
  **実装**:
  - `CPU_TYPE_META`(新設)：`LEVEL_META`の`rival`情報(アルル/ゼファ/ノクスの名前・立ち絵・台詞)をレベル(初級/中級/上級)から切り離し、`tricky`/`cautious`/`aggressive`というCPUタイプのキーだけで参照できるデータとして複製。`LEVEL_META`自体は削除せず、チュートリアル前の`practice`モードや実績計算(`levelKeyFromAttrs`等)で引き続き参照されるため残置。
  - **CPU戦略の決定ロジックを修正**（`startGame()`内）：これまで実戦(本戦)は`levelKeyFromAttrs(ATTRS5)`が常に`'adv'`を返す都合で、`LEVEL_META.adv.cpuType`(`'aggressive'`固定)が`selectedCpuType`より優先され、**プレイヤーが対戦相手タイプを選ぶUI自体が存在しなかった上に、選べたとしても実際のCPU行動には反映されない作りだった**(今回の調査で判明)。実戦(`!stageConfig`)の分岐を独立させ、`selectedCpuType`(ランダム込み)をそのままCPU戦略に使うよう変更。段階チュートリアル・練習モードの分岐は変更なし。
  - **`#instructions`(対戦設定画面)を再構成**：レベル選択(`#ruleOpts`、初級/中級/上級/本戦の4択)を削除し、代わりに`#cpuTypeOpts`(JS側には以前から存在したが対応するHTML要素が無く、事実上ずっと機能していなかった要素)を実装：アルル/ゼファ/ノクス/ランダムの4択。「詳細を見る」の中身も「このレベルのルール」(`modeSummary`、ルールチップ表示)と「クリア条件」を削除し、「選んだ対戦相手」(名前・立ち絵・台詞のみ)に簡素化。
  - `beginMatchSetup()`を簡素化：`currentModeConfig()`(レベル判定・練習/本戦分岐)への依存を削除し、常にフルルール(全属性・入替/奇襲/終盤2倍あり)の対戦として開始するようにした。「ランダム」選択時は、ライバル登場演出と実際のCPU挙動が食い違わないよう、`beginMatchSetup()`側で一度だけ乱数解決した結果を`_resolvedMatchCpuType`という新設の一時変数で`startGame()`に橋渡しする設計にした(プレイヤーの「ランダム」という選択自体は`selectedCpuType`に永続的に保持したまま、1試合ごとに解決するだけなので、次の対戦でもまた「ランダム」から選び直される)。
  - `showRivalIntro(typeKey, cb)`：引数を`levelKey`から`typeKey`に変更し、`CPU_TYPE_META`を参照するよう修正。
  - `onHomePlay()`：旧来の「初見なら初級へ直行、それ以外は設定画面へ」(`isFirstTimer()`)というロジックを撤去し、`!learnAllCleared()`ならチュートリアルのステージ選択(`openLearnLadder()`)へ強制的に誘導するよう変更(修了済みなら通常通り対戦設定画面へ)。`isFirstTimer()`自体は呼び出し元が無くなったが、既存踏襲で削除はせず残置。
  - 結果画面(`endGame()`)：★クリア条件の計算・表示(`buildStarCtx`/`applyStars`/`starBlockHTML`)を実戦の結果画面から削除。ライバルの反応は、これまで**本戦は常に`'adv'`(ノクス)固定**だったのを、実際に対戦したCPUタイプ(`cpuStrategy`)に応じて表示するよう修正(新設`cpuTypeReactHTML()`。既存の`rivalReactHTML()`はレベル専用のまま練習モード(`endPracticeMode()`)用に残置し、実戦とは別関数にして影響範囲を分離)。称号(称号8種)の判定(`unlockTitles()`)自体は残し、「3人のライバル全員に勝利する」(`rivalslayer`)の撃破記録キーを、旧来のレベルキー(`beginner`/`inter`/`adv`)から対戦相手タイプ(`tricky`/`cautious`/`aggressive`)に変更(`updateProgress(cpuStrategy, win)`・`buildTitleCtx()`の判定式を対応するキーに更新)。仮面あて累計(`readHitsTotal`)は従来通り毎試合加算を継続。実績(🏅18種、`ACHIEVEMENTS`)は元々レベル非依存の内容だったため変更なし。★システム自体(`applyStars`/`starBlockHTML`/`buildStarCtx`)は削除せず、practice mode専用(`endPracticeMode()`)としてコードは残置(★・称号の全体的な作り直しは段階3で対応予定のため、今回はスコープを実戦の結果画面から★表示を消すことに絞った)。
  **検証**: Playwrightで、①チュートリアル未修了で「対戦する」を押すとチュートリアルのステージ選択へ誘導されること、②チュートリアル修了済みだと対戦設定画面が開き、レベル選択UIが存在せず対戦相手タイプ4択・CPU強さ3択のみであること、③対戦相手（例：ノクス/aggressive）を選ぶと詳細表示が即座に切り替わり、実際に対戦を開始すると`cpuStrategy`が選んだ通りになり、デッキ・ラウンド数は常にフルルール(全5属性・8ラウンド)になっていること、④対戦を最後まで進めた結果画面に★ブロックが出ず、選んだ対戦相手タイプに応じたライバルの反応(勝敗に応じた台詞)だけが出ること、をそれぞれ確認。4パターンのCPU設定によるフルゲーム回帰テスト(`startGame()`を直接呼ぶ既存の検証スクリプト)も新規JSエラーなしで通過。**運用**: 段階3(実績/称号の全体的な再設計)は未着手のため引き続き仕様変更として`1.x.0`・`develop`ブランチにpush(`main`には反映していない)。

- **v1.4.0（develop・現在）**: v1.3.0に続く、段階3「実績・称号の作り直し」。段階2で判明した「実績(🏅18種)」と「称号(称号8種)」の実態を確認：**実績18種はもともとレベル/ボス依存の内容がほぼ無く**(streak/大勝/Kaos特殊/Air使用等、ゲームプレイのパターンが中心)、ユーザーが問題視していたのは主に**称号8種のうち4つ**(`tricolor`/`winddancer`/`chaosbreaker`/`ballmaster`)が「初級/中級/上級で★3つ集める」「全レベルで★9」という、廃止された旧レベル制に直接依存する条件だったため、と判断。**実装**:
  - 実績「修練の達人」(`missions`)：条件を旧・独立ミッション完走(`missionsCleared`)からチュートリアル完走(`learnDone`、既存の`endLearnStage()`が全7段階クリア時に渡していた値をそのまま利用)に変更。文言も「全ミッションをクリアする」→「チュートリアルを全てクリアする」に修正。もう一つの実績「上級突破」(`advanced`、上級ミッション全クリア)は、その名の通り旧・独立ミッション(`TUT_ADVANCED`)固有の実績のため据え置き(旧ミッション自体は遊び方画面から到達可能なまま残っているため無意味ではない)。
  - 称号4種を新条件に差し替え(idは既存ユーザーの解除済みデータを維持するため変更せず、name/desc/checkのみ変更)：
    - `tricolor`「三色の使い手」→**「見習い卒業」**：チュートリアルを修了する(`learnAllCleared()`)。
    - `winddancer`「風読みの騎士」→**「強敵を制す者」**：CPUの強さ「強い」で(本戦を)勝利する。
    - `chaosbreaker`「混沌を制する者」→**「全対戦相手を制す」**：3タイプ(アルル/ゼファ/ノクス)全員に「強い」設定で勝利する(`rivalslayer`の上位互換の位置づけ)。
    - `ballmaster`「仮面騎士の頂点」→**「常勝の証」**：本戦で通算30勝する(`champion`=15勝の上位互換)。
  - 「強い」設定での撃破判定は新しい永続状態を持たず、既存の対戦履歴(`loadGameHistory()`。各試合が`cpuStrategy`/`cpuDifficulty`/`result`/`isTop`を保存済み)から`buildTitleCtx()`内でその場で集計する設計にした(`beatStrongAny`=1タイプ以上、`beatStrongAll`=3タイプ全て)。新しい永続キーを増やさずに済み、実装もシンプルになる。
  - `endLearnStage()`のチュートリアル全クリア時の処理に`unlockTitles()`呼び出しを追加(従来は`checkAchievements()`しか呼んでおらず、称号は次に本戦を1試合終えるまで解除されなかった)。実績と称号の両方の解除通知を1つの通知枠にまとめて表示するよう修正。
  - `endGame()`(本戦の結果画面)の実績判定にも`learnDone:learnAllCleared()`を追加(過去にチュートリアルを修了済みだが「修練の達人」がまだ未解除のプレイヤーが、次に本戦を1試合終えた時点で遡って解除されるようにするための保険)。
  - ★システム自体(`LEVEL_META.stars`/`applyStars`/`starBlockHTML`/`buildStarCtx`)は削除せず、段階2の対応通り練習モード(`endPracticeMode()`、現状ナビゲーションが無く到達不能)専用として残置。
  **検証**: Playwrightで、①称号8種の`name`/`desc`が新条件通りに更新されていること、②`buildTitleCtx()`が進捗なしの状態で全て`false`/`0`を返すこと、③チュートリアル修了後に`unlockTitles()`を呼ぶと「見習い卒業」が解除されること、④対戦履歴に「強い」設定の勝利を1件だけ模擬注入すると`beatStrongAny=true`/`beatStrongAll=false`、3タイプ分注入すると両方`true`になること、⑤その状態で称号解除処理を回すと「見習い卒業」「強敵を制す者」「全対戦相手を制す」が正しくまとめて解除されること、⑥実績の「修練の達人」が`learnDone:true`のctxで正しく解除されること、⑦実績/称号画面(称号4/8・実績0/18)がエラーなく開き、スクリーンショットでも表示崩れがないことを確認。4パターンのCPU設定によるフルゲーム回帰テストも新規JSエラーなしで通過。**運用**: これで「根本的に見直したい」から始まった3段階の再設計(チュートリアル拡充→実戦再構成→実績称号再構築)が一通り完了。仕様変更として`1.x.0`・`develop`ブランチにpush(`main`には反映していない、`main`への反映はユーザーが`/dev/`で確認してから判断)。

- **v1.5.0（develop・現在）**: 段階1〜3の再設計完了後、ユーザーから2件の指示。**①バージョニング方針の確定**: 「developの変更をmainにマージする時にまとめて1.1.0に上げたい。それまでは1.0.0のままにする」という要望を受け、2案（A: develop上のGAME_VERSION表示自体を1.0.0に凍結する／B: develop上の細かいバージョン番号(1.1.0〜1.4.0…)は開発履歴としてそのまま刻み、main反映時だけその時点のdevelopの中身をまとめて「1.1.0」としてmainに書き込む）を提示して相談した結果、**B案**で合意（CHANGELOG/バージョン文字列の使い回しによるキー衝突を避けられる、開発履歴が残る、が主な理由）。つまり**develop側は今後もv1.1.0起点でv1.5.0, v1.6.0…と通常通りインクリメントを続けてよい**。mainへ反映するタイミングになったら、その時点のdevelop上の複数バージョン分の変更点を1本の`v1.1.0`エントリに要約し直し、`GAME_VERSION`をmain側の直近値(現在`v1.0.4`)から`v1.1.0`へ更新してpushする、という手順を踏むこと。②**属性説明の見直し**: ホーム画面(`renderMaskFan()`が描画する`MASK_FAN`配列)の仮面一覧で、AIR「多くは引分」→「絡むと引分」（Airは常に引き分けだが唯一Kaosには負けるため、「多くは」という不正確な曖昧表現を修正）、KAOS「特殊ルール」（内容が一切分からない曖昧な表現だった）→「Airに勝つ」に変更。MOON/SUN/DARKは元から「（3すくみで）何に勝つか」だけを一言で示す一貫した形式だったため、AIR/KAOSもこの形式に揃えることで、5枚を並べて読むと「MOON→DARK→SUN→MOON」の3すくみと「KAOS→AIR」の関係が一目で分かるようにした（MOON/SUN/DARKの文言自体は変更なし）。**検証**: Playwrightでホーム画面をレンダリングし、5枚の`.md`テキストが意図通りであること、7px相当の狭い列でも折り返し無く収まっていることをスクリーンショットで確認。4パターンのCPU設定によるフルゲーム回帰テストも新規JSエラーなしで通過。**運用**: 表示文言のみの変更のため仕様変更として扱い、`1.5.0`として`develop`ブランチにpush(`main`には反映していない)。

- **v1.6.0（develop・現在）**: ユーザーが実際にチュートリアル「ステージ1」をプレイして見つけた6件のフィードバックへの対応。**①ステージ1の名称**：「色の対決」→**「３つの属性」**に変更（ユーザーの元々の設計案の呼び方に合わせた）。**②属性の英語表記統一**：`stageSummary()`が使っていた`ATTR_JP_SHORT`（月/太陽/闇/Kaos/Air）を削除し、既存の`ALABEL`（MOON/SUN/DARK/KAOS/AIR、対戦画面全体で既に使われていた表記）に統一。`LEARN_STAGES`各ステージの`intro`文中の属性名（月✊闇✌太陽✋、Air、Kaos）も全て英語大文字表記に統一。**③終盤2倍がステージ1でも発動する不具合**：`isClimaxRound()`(「クライマックス」＝最終ラウンド2倍演出、`late2x`とは別に「どのレベルでも最終ラウンドは山場を作る」という旧レベル制時代の目的で無条件に`round>=matchRounds`を返す関数)が、チュートリアルの`late2x:false`ステージ(1〜6)でも最終ラウンドに得点2倍・山場バナーを発動させてしまっていた。ステージ紹介画面で「終盤2倍なし」と明言しているにもかかわらず矛盾する挙動になっていたのが原因。`isClimaxRound()`に`if(inTutorialStage() && !stageAllows('late2x')) return false;`を追加し、`showRoundStartModal()`の山場バナー判定(`isFinal`)も独自の`round>=matchRounds`直接比較をやめて`isClimaxRound()`を呼ぶよう統一(得点計算と表示の食い違いを構造的に防止)。通常対戦・練習モード・ステージ7(late2x:true)は従来通り最終ラウンド2倍のまま(回帰テストで確認)。
  **④⑤⑥の共通原因**：v1.2.0でチュートリアル序盤(ステージ1〜3)の推理ヒント(`showAttrHint`/`showYouCardHint`＝候補アイコン・🔓看破・⚠読まれている)を隠すために、`startLearnStage()`内で**プレイヤーの初心者ヒント設定そのもの**(グローバル変数`hintsEnabled`、コーチ表示・「おすすめ」札バッジ・決着時の💡やさしい解説文など複数機能が共有する単一のフラグ)をステージ終了までまるごと上書きしていた(`_preStageHints`による退避・復元の仕組み)。この結果、意図していた推理ヒントだけでなく、**「おすすめ」札バッジ(`canRecommend`)・コーチ吹き出し(`showChildCoach`/`showDecideCoach`)・決着時の💡やさしい解説(`beginnerResultNote`)まで道連れでステージ1〜3の間ずっと消えていた**。特に💡解説は「MOON✊はDARK✌に強いので勝ちました」のような一番肝心な初心者向け説明であり、これが消えていたことがユーザー指摘の「もう一度〜前戦の最後の状態が残ってゲームが進まない(ように見えた)」「場に出したカードの属性の可能性が表示されない」「勝敗メッセージが分かりにくい」の主因だったと判断(「進まない」ように見えたのは実際にゲームが止まっていたのではなく、ヒント・おすすめ・説明が軒並み消えて画面の手がかりが乏しくなり、進行が分かりづらく見えたためと推測。実際、直接関数呼び出し・実クリック両方で二重・8連続リトライを試したが、ゲーム状態が本当にスタックする再現は取れなかった)。
  **対応**：`hintsEnabled`をチュートリアル中に上書きする仕組み(`_preStageHints`退避・復元、`startLearnStage`/`closeLearn`/`endLearnStage`内の関連コード)を全て削除し、プレイヤー本来の初心者ヒント設定に一切触れないようにした。`showAttrHint`/`showYouCardHint`の非表示条件を、グローバルな`!hintsEnabled`ではなく**そのステージ自身の`stageConfig.hints`フラグ**を直接見るように変更(`inTutorialStage() && !stageConfig.hints`)。これにより、「おすすめ」札バッジ・コーチ・💡解説はステージ1から一貫して機能し(初心者への手がかりが常にある状態)、推理ヒント(看破・読まれている・候補アイコン)だけがステージ4(読み合い)まで意図通り伏せられる、という当初の設計意図により正確に一致する形になった。
  **検証**: Playwrightで、①`stageSummary(LEARN_STAGES[0])`が`"MOON・SUN・DARK ／ 4R"`になること、②ステージ1(子番)で「おすすめ」バッジが表示されること(修正前は0件、修正後は複数件)、③ステージ1を通しでプレイし、コーチ吹き出し・おすすめバッジ・決着時の💡解説がいずれも最低1回は表示されること、④ステージ1の最終ラウンドで`isClimaxRound()`が`false`・得点倍率が`1`のままであること、⑤ステージ1を実クリックで8連続リトライしても毎回正常にステージ結果画面まで到達すること、⑥ステージ4では推理ヒント(候補アイコン)が引き続き正しく表示されること、⑦ステージ7(late2x:true)・通常対戦では最終ラウンドの2倍演出が従来通り機能すること、をそれぞれ確認。4パターンのCPU設定によるフルゲーム回帰テストも新規JSエラーなしで通過。**運用**: 仕様変更・不具合修正の混在だが、チュートリアルという未リリース仕様変更の内部の話のため`1.x.0`・`develop`ブランチにpush(`main`には反映していない)。

- **v1.7.0（develop・現在）**: ユーザーからの追加指摘「チュートリアルで奇襲・入替なしなのに、CPUは選択可能になっていないか」への対応。**確認**: `cpuDecide()`(CPUが親として、子の数字を見た後に入替を検討する処理)には`stageAllows('swap')`のチェックが**存在せず**、`cpuSwapUsedThisRound`・`cpuTrust>=SWAP_COST`・`cpuHand.length>0`の3条件だけで入替を実行していた。一方、対になる`cpuWantsForce()`(CPUが子として奇襲を検討する処理)には`if(!stageAllows('force')) return false;`が既にあり、こちらは正しくガードされていた——入替側だけ実装時にこのチェックが漏れていた非対称なバグと判明。**影響**: `swap:false`のステージ1〜4(まだ入替を教えていない段階)でも、CPUが親のときは条件次第で入替を行ってしまい、プレイヤーがまだ見たことのない挙動(場のCPU札が急に変わる)が起きうる状態だった。ユーザーが推測した通り、これが以前の「もう一度で進まないように見えた」等の分かりにくさの一因になっていた可能性が高い。**修正**: `cpuDecide()`内の入替判定の条件に`stageAllows('swap') && `を追加。**検証**: Playwrightで、①ステージ1(swap:false)を6試合通しでプレイし、`cpuSwapUsedThisRound`が一度もtrueにならないこと(修正前は発生し得た)、②ステージ6(swap:true)では引き続きCPUが入替を行うことがあること、を確認。4パターンのCPU設定によるフルゲーム回帰テストも新規JSエラーなしで通過。
  なお、ユーザーから「チュートリアルはシナリオに沿って、決まった手札・決まったCPUの動きにしてはどうか」という設計提案も受けたが、これは既存のゲームエンジン(ランダム配札＋期待値ベースのCPU AI)を土台にした軽量な対応(各ステージの初期手札だけを固定カードに差し替え、ラウンドごとの意思決定は既存AIに任せる)を提案し、フル台本化(CPUの一手一手まで固定)は「子の一手はプレイヤーの選択に依存するため分岐が必要になり大掛かりになる」と説明した上で、ユーザーの意向待ち。今回はまだ着手していない。
  **運用**: 不具合修正だが、未リリースのチュートリアル仕様(develop限定)の内部の話のため、`1.x.0`・`develop`ブランチにpush(`main`には反映していない)。

- **v1.8.0（develop・現在）**: ユーザーの設計提案「チュートリアルはシナリオに沿って、決まった手札を出す流れにしてはどうか」への対応(段階案の軽量版=手札固定のみ、CPUの一手ごとの完全台本化はまだ未着手)。**実装**:
  - `LEARN_STAGES`の各エントリに任意の`openingHand:{you:[...], cpu:[...]}`を持たせられるようにした(未指定なら従来通りランダム配札)。
  - 新設`buildScriptedDeal(stage)`：そのステージの縮小デッキ全カードから`openingHand`で指定した10枚を除いた残りをシャッフルし、両者の山札分として均等に配分。指定の10枚自体は「フルデッキ(推理の母集団)」にもそのまま含まれるので、看破/読まれている等の推理ロジック(`computePossibleAttrs`/`poolOwner`)には影響しない。
  - `startGame()`の初期手札選択(`pick6`)に、固定手札が指定されていればシャッフル抽選をスキップしてそのまま採用する分岐を追加(`fixedHand`引数)。`openingHand`を持たないステージ・通常対戦は元のランダム配札のまま。
  - ステージ1に具体的な初期手札を設定：あなた「DARK3・MOON4・SUN2・DARK0・MOON1」、CPU「DARK1・SUN3・MOON0・DARK5・SUN5」。同属性同士(DARK対DARK)の数字勝負と、3すくみ(MOON→DARK→SUN→MOON)の3パターン全てが手札内に揃うよう選定。
  **検証**: Playwrightで、①10回の対戦開始で初期手札が指定通り(順不同一致)であり、かつ相手側デッキとカードの重複がないこと、②ステージ1を5回フルプレイし、いずれもエラーなく完走し、同属性対決・3すくみ対決の両方が高い頻度(5戦中4戦は両方、残り1戦もどちらか)で自然に発生すること、③`openingHand`を持たないステージ2・3は従来通りランダム配札のままであることを確認。4パターンのCPU設定によるフルゲーム回帰テストも新規JSエラーなしで通過。
  **今回のスコープ外**：CPUの一手一手までの完全台本化(ラウンド1のCPUの手を固定するなど)は、CPUが子として動く時はプレイヤーの選択に応じて分岐が必要になるため、別途相談の上での対応とした(今回は初期手札の固定のみ)。ステージ2以降への同様の適用も、まずはステージ1の結果を見てから判断する想定。
  **運用**: 不具合修正ではなく仕様追加のため、`1.x.0`・`develop`ブランチにpush(`main`には反映していない)。

- **v1.9.0（develop・現在）**: ステージ1の実機スクリーンショットを受けたユーザーからの3件の追加指摘に対応。
  1. **「ヒントがオンになっていない、オンにしても何がオススメか理由がわからない」**：`startLearnStage()`が呼ばれる時点では`hintsEnabled`はプレイヤーの過去の設定(footerの💡ヒントON/OFFトグル、localStorage)をそのまま引き継いでいた。OFFにしたことがあるプレイヤーがチュートリアルに入ると、「おすすめ」札もコーチ助言も出ない状態から始まってしまう。**修正**：チュートリアル突入時(`startLearnStage()`)に強制的に`hintsEnabled=true`にし、元の値は新設`_preLearnHints`に退避、チュートリアルを閉じた時(`closeLearn()`)に復元するようにした(常時ヒントONのプレイヤーには影響なし)。あわせて「おすすめ」の理由説明(`showChildCoach()`のコーチ文)を「手札を開くとおすすめ札に印が付きます」という循環的な文言から、「おすすめ札は、相手の属性が分からなくても比較的勝ちやすい札の印です」という原理の説明に変更。
  2. **「相手の手札には予想属性が表示されているのに、場に出ると見えなくなる」**：画面上部の相手手札ストリップ(`renderOppHand()`→`oppCardHTML()`)は候補アイコンをステージに関わらず常時表示していたが、実際に場に出た札の候補表示(`showAttrHint()`が更新する`oppCardHint`)はステージ1〜3で完全に空にしていた(v1.6.0時点の実装)。同じ「候補」情報が、手札にある間は見えて場に出た瞬間消えるという矛盾が発生していた。**修正**：`showAttrHint()`を、ステージ1〜3では🔓看破・🎭予想・ⓘなどの発展的な仕組みは伏せたまま、手札ストリップと同じ「可能性: [候補アイコン]」という基本表示だけは出すよう変更(推理そのものの用語・遊び方はステージ4で正式に教える設計は維持しつつ、既に見えている情報が急に消える矛盾だけを解消)。
  3. **「相手の山札の（推理）が何を意味しているのかわからない」**：`renderOppDeckChip()`(相手の山札の残り候補を統計的に見せる「推理 ›」チップ、タップで内訳モーダル)は、まだ推理の概念自体を教えていないステージ1〜3でも常時表示されていた。**修正**：`inTutorialStage() && !stageConfig.hints`の間はこのチップ自体を非表示にし(`display:none`)、ステージ4以降・通常対戦では従来通り表示。
  **検証**：Playwrightで、①ヒント設定をOFFにした状態でもステージ中は`hintsEnabled=true`になり、チュートリアルを閉じると元のOFFに復元されること、②ステージ1で「おすすめ」バッジ・新しいコーチ文言・簡略版の候補アイコン(oppCardHint)が表示され、「推理」チップは非表示であること、③ステージ4では「推理」チップが表示され、oppCardHintも従来通りⓘ付きのフル表示になること、を確認。ステージ1のリトライを8回連続実施してエラー・スタックなし、4パターンのCPU設定によるフルゲーム回帰テストも新規JSエラーなしで通過。
  **運用**: 不具合修正＋UI改善のため、`1.x.0`・`develop`ブランチにpush(`main`には反映していない)。

- **v1.10.0（develop・現在）**: ルール説明タブ「第八幕：推理と看破」から予想（🎭）の説明を削除(「予想の記述は不要では」)。予想は候補が2つ以上残る間だけ現れる、勝敗に無関係な任意ミニゲームで、看破(🔓・確定)と混同されやすい元凶だったため、説明文からは削除し看破・読まれているの2項目のみに整理(左側の図解ボックスの「🎭タップで予想」キャプションも「まだ属性は絞れていない」に変更)。**機能自体(実際にタップして予想できる仕組み)は削除しておらず、対戦中の表示のみ**。
  あわせて、ホーム画面の「ミッション」ボタンを「チュートリアル」に差し替え、タップで新チュートリアル(`openLearnLadder()`)に遷移するよう変更。旧・独立ミッション形式チュートリアル(`TUT_MISSIONS`/`TUT_CORE`、基本セット)への到達経路はこれで無くなったが、実装自体(`openMissions()`/`tutMissionOverlay`)は削除していない — 「遊び方」画面の最終ステップにある「⚔️ 上級ミッションに挑戦」のリンク(`openMissions('advanced')`、`TUT_ADVANCED`)が引き続きこの仕組みに依存しており、実績「上級突破」(`advanced`)の唯一の解除経路でもあるため。基本セット(`TUT_CORE`)自体の削除、および上級ミッションの扱いについては、ユーザーへ別途確認中。
  **検証**：Playwrightで、ホームの新ボタンがチュートリアル画面を開くこと、ルール本文から「予想（🎭）」の記述が消え「看破（🔓）」「読まれている（⚠）」は残ること、「遊び方」の上級ミッションへの導線は引き続き機能することを確認。4パターンのCPU設定によるフルゲーム回帰テストも新規JSエラーなしで通過。
  **運用**: 内容整理・UI変更のため、`1.x.0`・`develop`ブランチにpush(`main`には反映していない)。

- **v1.11.0（develop・現在）**: 前回相談した2件の残タスクを実装。
  1. **旧・基本ミッションセットの削除**：`TUT_MISSIONS`(核10問)・`TUT_CORE`・`MISSIONS_DONE_KEY`/`missionsCleared()`を削除。`openMissions(which)`は引数を廃し常に上級セット(`TUT_ADVANCED`)を開くだけの関数に簡略化、`tutFinish()`も基本セット分岐を除去。旧・練習モード(`endPracticeMode()`、現状到達不能)内で基本ミッションの1問だけを開く`openMissionLesson()`と、それが参照していた`UNLOCKS.missionIdx`も削除(依存先ごと除去)。上級ミッション自体・実績「上級突破」は影響を受けず、「遊び方」画面から引き続き遊べる。
  2. **対戦相手ロースターの刷新**（ユーザー提供の6キャラクター画像に準拠）：
     - 既存3タイプ(tricky/cautious/aggressive)の`CPU_TYPE_META`を、画像の「○○仮面□□」表記・プレイスタイル説明に合わせて全面更新（`CPU_MASK_NAME`は元々ほぼ一致していたため変更なし）。portrait(属性カード絵の流用)も画像の配色に近い属性へ再割当（アルル→moon、ゼファ→dark、ノクス→sun）。
     - 新タイプ`balanced`(調和仮面リヴェル・バランス型)・`chaotic`(無秩序仮面カオス・先の読めない型)を`CPU_STRATEGIES`/`CPU_TYPE_META`/`CPU_MASK_NAME`/対戦設定画面のチップに追加。AI行動は、`balanced`は既存の分岐に一切引っかからない「補正なしのニュートラルAI」として扱い(何も追加実装せず自然に成立)、`chaotic`は`cpuDecide()`の決戦閾値・`cpuChooseCard()`のノイズ値・`cpuWantsForce()`の奇襲判定バーの3箇所に、`tricky`の同種の乱数補正を約2倍に増幅した分岐を追加(「予測不能」を数値的に表現)。
     - `指導仮面アストラ`(初心者騎士を一人前に育成する教育者)は、対戦相手としてではなく**チュートリアル専用キャラ**として採用。`startGame()`のチュートリアル分岐で`cpuStrategy='cautious'`(ゼファ流用)としていたのを`cpuStrategy='mentor'`に変更(`CPU_STRATEGIES`には含めないため対戦設定には出ない=選択不可、`CPU_MASK_NAME.mentor`のみ追加)。AI行動分岐はどれも`mentor`にマッチしないため、自動的に補正なしの公平な基準AIになる(これは意図した挙動)。対戦中の相手名バッジがステージ1〜7を通じて「鉄壁仮面」から「指導仮面」に変わる。
     - 称号「全対戦相手を制す」(`chaosbreaker`)・「主を退けし者」(`rivalslayer`)の必要人数を、3人→5人(新2タイプ込み)に更新(説明文・判定式とも)。`beatStrongAll`の判定は`CPU_STRATEGIES.every(...)`化し、今後ロースターが増減しても自動追従するようにした。
  **検証**：Playwrightで、①対戦設定画面のチップが5タイプ+ランダムの6択になっていること、②`balanced`/`chaotic`を選んで実際に対戦を開始すると`cpuStrategy`が選んだ通りになること(親決めで引き分けを引いた場合の再めくりにも対応した検証スクリプトに修正して確認)、③チュートリアル中は`cpuStrategy==='mentor'`・相手名バッジが「指導仮面」になること、④称号8種の説明文・`beatStrongAll`(4/5勝利ではfalse、5/5勝利でtrueになること)を確認、⑤実績/称号画面がエラーなく開くこと、⑥「遊び方」からの上級ミッション導線が引き続き機能すること、を確認。4パターンのCPU設定によるフルゲーム回帰テストも新規JSエラーなしで通過。
  **今回のスコープ外・申し送り**：新2タイプ(リヴェル/カオス)・アストラの立ち絵/画像は、既存同様に属性カード絵を暫定流用(専用イラストは未用意)。AIの個性づけも簡易な数値補正に留めており、既存3タイプほど作り込んではいない(様子を見て追加調整の余地あり)。
  **運用**: 仕様変更として`1.x.0`・`develop`ブランチにpush(`main`には反映していない)。

- **v1.12.0（develop・現在）**: ユーザーから送付された6キャラクターの立ち絵合成画像(2行×3列、各パネルにイラスト+紋章+名前+説明文が焼き込まれたもの)を実際に切り出して使用したいという要望に対応。**画像処理**：Python(PIL)で合成画像(1024×1536)を6分割(各341×768)し、各パネルの説明文部分を除いたイラスト部分のみ(上部約450px、グリッド境界線を数px単位でトリム)を抜き出し、幅300pxにリサイズしてJPEG(quality85)で圧縮。6枚合計約280KB(base64化で約370KB)。既存の`CARD_IMG`(属性カード絵)と同じ「index.html内にdata URIとして直接埋め込む」方式を踏襲し、新設`RIVAL_IMG`オブジェクト(`CARD_IMG`定義の直後に配置)としてキャラクター名(tricky/cautious/aggressive/balanced/chaotic/mentor)をキーに格納。index.htmlは約889KB→約1.18MBに増加。
  **実装**：新設`rivalImgUrl(typeKey)`(`RIVAL_IMG`にあればそれを、なければ従来の属性カード絵にフォールバック)を3箇所に配線：
  1. **ライバル登場演出**(`showRivalIntro()`の`#riPortrait`、150×210)
  2. **対戦設定画面の「対戦相手を選ぶ」チップ**：静的だった`#cpuTypeOpts`のHTMLを撤去し、新設`renderCpuTypeOpts()`が`CPU_STRATEGIES`から自動生成する方式に変更(ロースターが増減しても手直し不要に)。各キャラの正式名(例:「技巧仮面アルル」)を`仮面`の位置で正規表現分割し、称号部分をチップ下段の小文字、名前部分を上段に表示。40×40の円形立ち絵(`.co-face`)を新設。
  3. **対戦中の相手情報欄**(`#cpuCell`)：新設`.pi-face`(28×28円形)を追加し、`startGame()`でcpuStrategy確定直後に1回だけ立ち絵を設定(チュートリアルのアストラも含め、全モードで自動的に反映)。
  **検証**：Playwrightで、①対戦設定画面の5チップ全てに立ち絵が表示されること、②ライバル登場演出の立ち絵が選んだタイプと一致すること、③対戦開始後、相手情報欄の立ち絵が一致すること、④チュートリアル(アストラ)でも同様に反映されること、をスクリーンショット付きで確認。4パターンのCPU設定によるフルゲーム回帰テスト(レイアウト崩れ=横スクロール発生の有無を含む)も新規JSエラー・overflowなしで通過。
  **今回のスコープ外**：クロップ・圧縮は簡易的な自動処理であり、構図の微調整(顔の中心出し等)は行っていない。ユーザーが正式な立ち絵を用意した場合は`RIVAL_IMG`オブジェクトの該当キーを差し替えるだけで反映される設計。
  **運用**: 仕様変更として`1.x.0`・`develop`ブランチにpush(`main`には反映していない)。

- **v1.13.0（develop・現在）**: 対戦設定画面のスクリーンショットを受けた4件の指摘に対応。
  1. **「ランダムにはアストラのイメージを表示したい」**：`renderCpuTypeOpts()`の「ランダム」チップと`renderLevelInfo()`のランダム分岐、両方の立ち絵に`rivalImgUrl('mentor')`(指導仮面アストラ)を設定。チップは他の5タイプと同じ`with-face`スタイルに揃え、名前を「🎲 ランダム」、サブラベルを「おまかせ」に変更。
  2. **「選んだ相手の文章が全文表示されていない」**：`.lv-quote`に`white-space:nowrap; overflow:hidden; text-overflow:ellipsis;`が付いており、キャラクターの台詞(intro)が長いと省略記号で切れていた(旧・短い台詞専用のCSSがそのまま残っていたのが原因)。`white-space:nowrap`等を外し複数行に折り返すよう修正、あわせて`.lv-head`を`align-items:flex-start`にして顔アイコンが折り返した文章の途中で浮かないようにした。
  3. **「CPUの強さの説明はよくわからない」**：「弱い/CPUが手加減」「普通/(説明なし)」「強い/CPUが本気」という非対称かつ抽象的な説明だったのを、実際の内部仕様(`cpuEps()`のミス確率: 弱い33%／普通10%／強い0%)に即して「弱い/CPUがよくミスする」「普通/標準的な強さ」「強い/CPUはミスしない」に統一。
  4. **「詳細を見るを閉じる意味はあるのか？表示位置は妥当なのか？」**：`<details><summary>詳細を見る（対戦相手の情報）</summary>...</details>`という開閉トグルを撤去(閉じても得るものがなく、むしろ選んだ相手の情報を隠すだけだったため)。位置も、CPUの強さ(手順2)の後ろに置かれていたのを、対戦相手選択(手順1)の直下に移動(選んだ相手の情報は手順1に従属する内容のため)。関連する`.setup-details`のCSSも削除(他に参照箇所なし)。
  **検証**：Playwrightで、①ランダムのチップ・詳細ボックス双方にアストラの立ち絵が表示されること、②`<details>`要素が存在しないこと、③カオス選択時に台詞が省略されずscrollWidth<=clientWidthで収まる(複数行で全文表示される)こと、④CPUの強さ3択のラベルが意図通りであることを確認。4パターンのCPU設定によるフルゲーム回帰テストも新規JSエラー・レイアウト崩れなしで通過。
  **運用**: UI改善のため、`1.x.0`・`develop`ブランチにpush(`main`には反映していない)。

- **v1.14.0（develop・現在）**: ユーザーからの7件の指摘に対応。
  1. **初回プレイ時に「遊び方」を表示**：`maybeShowTutorialFirstTime()`が2択の`welcomeModal`を出していたのを、`openTutorial()`(概要スライド)を直接開くよう変更。`welcomeModal`自体のHTML/JSは削除せず休眠のまま残置(他から参照されなくなっただけ)。
  2. **管理画面にプレイヤーごとのチュートリアル進行度を表示**：`endLearnStage()`に`logEvent('learn_stage_clear', {stage, stageName, totalStages})`を追加(段階チュートリアルには今までこの種の計測が一切無かった)。admin.htmlの`playersSectionHTML()`に`learnProgressFor(x)`を追加し、`playerId`一致するイベントの最大`stage`を「・チュートリアルN/7」としてプレイヤー行に表示(`playCountFor`と同じ突き合わせパターンを踏襲)。playerIdの無い古い登録データは対象外(概算すらできないため空欄)。
  3. **「遊び方」画面の「上級ミッションに挑戦」を削除**：`#tutorialOverlay`内のリンクを削除。これにより上級ミッション(`TUT_ADVANCED`)・実績「上級突破」への到達経路が無くなった点はユーザーへ報告。
  4. **「配札のしくみ」の15枚表示の混同を解消**：`tutBack(15,'あなた')`がカードの数字バッジ(`.tc-num`)を「15」という(実在しない)カード数字のように見せてしまっていた。新設`tutBackCount(count,label)`で、カードの外側に「×15枚」という別枠のラベルを添える形に変更(カードの数字バッジは一切使わない)。
  5. **「相性で勝敗が決まる」のアイコンを属性アイコンに変更**：5つの`.hint-icon`(抽象的な色付き図形)を、`GLYPH`(☾≈☀✷✦、ホーム画面のMASK_FANと同じ記号)+属性名ラベルの組に変更(新設`.tut-attr-row`/`.tut-attr-ic`)。
  6. **「はじめる」ボタンでチュートリアル選択/対戦設定へ遷移**：`tutNext()`の最終ステップ分岐を、`closeTutorial()`のみだったのを`learnAllCleared()`で分岐し`openLearnLadder()`または`openMatchSetup()`へ進むよう変更。
  7. **各チュートリアルステージ開始時にアストラの説明イメージを表示**：`showStageIntro()`に新設`.mentor-say`(アストラの立ち絵+吹き出し風の`st.intro`)を追加。
  **検証**：Playwrightで、①TUT_KEY未設定の初回訪問で`tutorialOverlay`が自動表示されること(welcomeModalは出ない)、②上級ミッションリンクが消えていること、③配札のしくみステップに`.tc-num`が存在せず「×15枚」表示になっていること、④相性ステップに`.hint-icon`が存在せず5属性のグリフ+ラベルが出ること、⑤未クリア時は「はじめる」でチュートリアル選択へ、クリア済みなら対戦設定へ遷移すること、⑥ステージ開始画面にアストラの立ち絵つき吹き出しが出ること、⑦admin.htmlのモックデータで`learn_stage_clear`イベントを含むプレイヤーが「・チュート7/7」と正しく表示され、イベントの無いプレイヤー・playerId無しプレイヤーは表示されないことを確認。4パターンのCPU設定によるフルゲーム回帰テストも新規JSエラー・レイアウト崩れなしで通過。
  **運用**: 仕様変更・UI改善のため、`1.x.0`・`develop`ブランチにpush(`main`には反映していない)。admin.html/HANDOFF.mdも同時にpush。

- **v1.15.0（develop・現在）**: ユーザーからの3件の指摘のうち、実装が明確な1件に対応(残り2件は仕様確認/回答のみ、コード変更なし)。
  - **ステージ4の開始時に配札演出を追加**：`LEARN_STAGES[3]`(読み合い)に`dealAnim:true`を追加し、`startLearnStage()`で`st.dealAnim`が立っているステージだけ、本戦と同じ`playDealAnimation()`(「全30枚を2人に分配します」の視覚演出)を挟んでから`startGame()`を呼ぶよう変更。この演出は元々「演出用の見せ札」で実際の配札とは完全に独立している(実データを表示すると推理が崩れるため)ため、チュートリアルのランダム/縮小デッキ配札ロジックとは無関係に安全に流用できた。ステージ4は5属性30枚デッキが初めて登場する回のため、この演出が最も意味を持つ。他のステージには追加していない。
  **検証**：Playwrightで、①ステージ4開始直後に配札演出overlayが表示されタップでスキップ後ラウンドが正常に始まること、②ステージ1など`dealAnim`未指定のステージでは演出が出ないこと、③ステージ4をフルプレイしてエラーなく完走することを確認。4パターンのCPU設定によるフルゲーム回帰テストも新規JSエラー・レイアウト崩れなしで通過。
  「相手の仮面当て要素(🎭予想)はまだ残ってる？」への回答：実戦・ステージ4以降では引き続き有効(仕様通り)。ステージ1〜3のみ非表示。特に変更は依頼されていない(質問への回答のみ)。
  **運用**: 仕様変更のため、`1.x.0`・`develop`ブランチにpush(`main`には反映していない)。

- **v1.16.0（develop・現在）**: 前バージョンで保留にした「相手の山札の『推理』表示を、自分の山札同様に次の数字を表示したい」を実装。CPUの山札(`cpuDrawPile`)が配札時点で順序の確定した実データである以上、これを文字通り実装するとCPU側には無い情報優位をプレイヤーが得ることになる、と一旦保留にしていたが、ユーザーから「お互いが引く次の数字のみが分かってもゲーム性に問題ないのでは」と指摘を受け再検討。**判断**：本ゲームはCPU(AI)を相手にしたソロプレイであり、対人戦の公平性(相手にも同じ情報を与えるべきか)は本来問題にならない。またルール上そもそも「数字は伏せない、隠すのは属性だけ」が一貫した設計思想であり(場に出た札の数字は常に公開、看破/読まれているも属性のみが対象)、山札の次の数字だけ例外的に隠していたのが、むしろこの一貫性を崩していた側面がある。以上からユーザーの提案通り実装するのが妥当と判断。
  **実装**：`renderOppDeckChip()`を、`oppDrawPileCandidates()`由来の「推理 ›」ラベルから、自分の山札(`renderDeckChip()`)と全く同じ「次<b>${num}</b>」形式に変更(`cpuDrawPile[cpuDrawPile.length-1].num`を直接参照。属性は引き続き非公開)。`openOppDeckModal()`(タップ時の内訳モーダル)も、従来「最小」だった先頭バッジを、実際の次の数字と一致する行に「次」バッジが付くよう修正(自分の山札の`openDeckModal()`と同じ`nextNum`照合パターンに統一)。
  **検証**：Playwrightで、対戦中の相手デッキチップの表示数字が実際の`cpuDrawPile`の末尾(次に引かれる札)の数字と一致すること、内訳モーダルの「次」バッジが同じ数字の行に付くことを確認。4パターンのCPU設定によるフルゲーム回帰テストも新規JSエラー・レイアウト崩れなしで通過。
  **運用**: 仕様変更のため、`1.x.0`・`develop`ブランチにpush(`main`には反映していない)。

- **v1.17.0（develop・現在）**: ユーザーから「相手の仮面当ての要素はまだ残っていますか？」→「不要ではないですか？」→「削除してください」と段階的に確認を受け、仮面あて(🎭タップ予想のミニゲーム)を全面削除。
  **経緯**: 仮面あては元々「決着時に的中していれば+2pt」という得点の一部だったが(v0.59.0)、CPU側に対応する仕組みが無く非対称という指摘を受けて「勝敗に影響しない独立した成績」に格下げ済み(HANDOFF内 v0.59.0 参照)。さらに直近(v1.1.0)では「看破しているはずなのに外れることがある」というユーザー体験上の混乱の一因として、「🎭予想(候補が複数残っている間の任意ミニゲーム、外れて当然)」と「🔓看破(候補が1つに絞れた時の自動確定バッジ、100%的中)」の用語的な紛らわしさが既に問題視されていた。今回ユーザーへの確認の結果、勝敗に影響しないおまけ機能である仮面あて自体が不要と判断され、削除が決定。看破/読まれている等の推理の根幹は現行のまま維持。
  **実装**: `canPredictNow()`/`setPrediction()`/`checkPrediction()`/`cpuFieldCard()`/`READ_BONUS`定数を削除。状態変数`youPrediction`/`youReadBonus`/`youReadHits`とその全リセット箇所(`startGame()`/`startRound()`)を削除。`showAttrHint()`から候補2つ以上の時にタップ予想チップを出す`predict`分岐を削除し、候補表示(可能性)→看破(確定)の既存2分岐はそのまま残置(動作に変更なし)。決着/撤退の公開処理(`finishPassReveal()`/`resolveFightJudge()`)から`checkPrediction()`呼び出しを削除。結果画面(実戦`endGame()`のmilestoneSection、練習モード`endPracticeMode()`)から「🎯仮面あて的中N回」の表示行を削除。テレメトリ(`buildGameLog()`)から`readHits`/`readBonus`フィールドを削除。CSSの`.pred-chip`/`.pred-lb`/`.pred-hint`/`.pred-done`/`.pred-row`を削除(`.hint-icon-row`など看破/候補表示と共有のクラスは残置)。
  **依存関係の整理**: ①初級レベルの★2つ目の目標「相手の仮面を3回見破る（仮面あて）」(`check:c=>c.readHits>=3`)を、既存の`buildStarCtx()`が既に算出していた`rpsWins`(3すくみの相性勝ちラウンド数、その試合限定)を使い「色の相性勝ちを2回決める」(`check:c=>c.rpsWins>=2`)に差し替え。新規の計測は追加せず、既存データの再利用のみ。②実績(称号)「看破者」(`desc:'仮面あてを通算20回的中させる'`, `check:c=>c.readHitsTotal>=20`)は、`id:'seer'`と名前「看破者」はそのまま維持しつつ(推理・看破の概念自体は現行ゲームに残っているため)、条件を「本戦の個別ラウンドで通算60勝する」(`check:c=>c.totalRoundWins>=60`)に変更。`totalRoundWins`は`buildTitleCtx()`内で`loadGameHistory()`の各試合が既に保持している`g.youWins`(その試合でのラウンド勝利数、`saveGameToHistory()`が`computeMatchSummary()`から保存済み)を合算するだけで新規計測不要。`loadProgress()`/`saveProgress()`/`updateProgress()`から`readHitsTotal`フィールドを完全に削除(バックワード互換の妥協は行わず、既存localStorageの当該キーは単に無視される)。
  **README.md**: 「特徴」の仮面あて紹介文を「🔎 推理と看破」(残存する候補絞り込み/看破バッジの説明)に差し替え。
  **検証**: Playwrightで、①`index.html`内のJSを`new Function()`で構文チェック、②通常対戦(tricky/normal)を実際に6ラウンドプレイし、`#oppCardHint`のHTMLに`pred-chip`が一度も出現しないこと・「可能性」表示は引き続き出ること、③実績画面(`openAchievements()`)に「看破者」が新しい説明文で表示され旧文言「仮面あて」が残っていないこと、④`buildTitleCtx()`が`totalRoundWins`(number型)を返し`readHitsTotal`キーが存在しないこと、⑤初級レベルの★構成が新条件で正しく表示され、`rpsWins:2`を含むctxで実際に★2つ目が解除されること、を確認。4パターンのCPU設定によるフルゲーム回帰テストも新規JSエラー・横スクロール崩れなしで通過(既知のFirebase CDNネットワークノイズ8件のみ)。
  **運用**: 機能削除・仕様変更のため、`1.x.0`として`develop`ブランチにpush(`main`には反映していない)。

- **v1.18.0（develop・現在）**: ユーザーが「Xに投稿しているがまったくプレイしてもらえない」と相談してきたのを機に発覚した、来訪数計測の不備を修正。
  **経緯**: Xの投稿スクリーンショット(インプレッション31/73/157件)とadmin.htmlのダッシュボード(登録プレイヤー5人・実プレイヤーのべ3人・対戦記録42件)を突き合わせたところ、「そもそも投稿の閲覧数自体が数十〜百数十件と少ない」ことが主因と判明(コンテンツの質の問題ではなくリーチの問題)。あわせてダッシュボードの「計測イベント」欄が`セッション開始:0 / 対戦開始:0`なのに`離脱:14`だけ計上されているという矛盾に気づき調査。**原因**: `logEvent()`(session_start/game_start/abandon/tutorial_*用)は`LOG_CONSENT_KEY==='yes'`の時だけ送信するが、`session_start`はページ読み込み直後、`game_start`は初回対戦開始時という「同意が得られているはずがないタイミング」で発火時点の同意状態をチェックしていた。加えてFirebase初期化(`<script type="module">`側の`await import(...)`)は非同期・低優先度(module=defer相当)で、クラシックscript側の同期実行(session_start呼び出しを含む)より確実に遅れて完了するため、初回セッションでは`window.__maskdSaveLog`自体がまだ存在しない可能性が高い。一方`abandon`や試合結果送信(`maybeSendGameLog`、同意は「明示的に拒否しない限り送信」という別モデル)はゲームがある程度進行した後に発火するため、その頃には両方の問題が解消されており送信できていた。つまり「セッション開始:0」は不具合の反映であり、実際の来訪数はもっと多いと推測された。
  **設計判断**: 既存の同意モーダル(`logConsentModal`)は「プレイ結果を送らないを選べば送信しません」とユーザーに明示的に約束しているため、この約束を破らない形で来訪数だけを正確に取りたいとユーザーに確認。「①匿名の来訪カウントを新設(推奨)」と「②既存の軽量イベントの同意チェックを撤廃(約束と矛盾するため文言修正が必要)」を提示し、①を選択。
  **実装**: 新設の`visits`/`visits_dev`コレクションに、`serverTs`のみ(playerId等の識別子は一切含まない)のドキュメントを、Firebase初期化成功直後に無条件で1件書き込む(`window.__maskdLoggingReady = true`の直後、`<script type="module">`内)。既存の`LOG_CONSENT_KEY`・`showLogConsent()`・`maybeSendGameLog()`には一切変更なし(「プレイ結果を送らない」の約束は従来通り維持)。`firestore.rules`に`isValidVisit()`(フィールド数上限3・`playerId`禁止)を追加。`admin.html`に`totalVisits`(`getCountFromServer`によるサーバー側集計)を追加し、KPIグリッド先頭に「来訪数(同意不要・全訪問)」タイルとして表示。
  **要作業**: Firebaseコンソール →「Firestore Database」→「ルール」タブに、更新後の`firestore.rules`の内容を貼り付けて公開する必要がある(過去の`hidden_records`等追加時と同じ手順)。これをしないと`visits`への書き込み・読み取りは権限エラーで失敗する。
  **検証**: `new Function()`/`AsyncFunction`でindex.html全script(クラシック・module)・admin.htmlのモジュールscriptの構文チェックを実施。admin.htmlは`__adminMock`フックで来訪数タイルの描画を確認(JSエラー0)。index.htmlは同意`'no'`状態でも来訪カウントの書き込み試行がページの動作を妨げないことを確認(このサンドボックスはFirebase CDNへの外向き通信自体が塞がれているため実際の書き込み成否は未検証・本番では上記のルール公開が前提)。4パターンのCPU設定によるフルゲーム回帰テストも新規JSエラー・横スクロール崩れなしで通過(既知のFirebase CDNネットワークノイズ8件のみ)。
  **運用**: 計測基盤の追加のため、`1.x.0`として`develop`ブランチにpush(`main`には反映していない)。

- **v1.19.0（develop・現在）**: ユーザーがFirestoreルールを公開した後、「訪問日時などもわかりますか？」と質問。v1.18.0の`visits`コレクションは総数(`getCountFromServer`)のみ取得しており、個々の来訪日時は見えていなかったため追加対応。
  **実装**: `admin.html`の`loadData()`で、`visits`コレクションの総数に加えて直近2000件の実データも取得(`allVisits`)。新設`visitsSectionHTML()`が、既存の「日別プレイ数」と同じ`bars()`パターンで日別の来訪数グラフを描画し、その下に直近50件の来訪日時(`serverTs.toDate()`)を一覧表示(`players`セクションの行スタイル`.rrow`/`.rt`を流用)。対戦ログが0件の早期リターン分岐(`render()`冒頭の`if(!N)`)にも`visitsSectionHTML()`を追加し、対戦記録が無くても来訪データだけは見えるようにした。テスト用フック`window.__adminMock`に`visits`/`visitCount`引数を追加。
  **検証**: Playwrightで、`__adminMock`に`serverTs.toDate()`互換のダミーTimestampを渡し、①来訪データが空の状態では見出しなしの案内文のみ表示、②来訪データがある状態では見出し・日別グラフ・個々の日時が正しく描画されることをJSエラー0で確認。
  **運用**: 管理画面の機能追加のため、`1.x.0`として`develop`ブランチにpush(`main`には反映していない)。

- **v1.1.1（main）**: ユーザーから「1.1.1としてマージしてください」と明示的に依頼を受け、develop の v1.17.0(仮面あて削除)〜v1.19.0(来訪履歴)をmainへ統合。手順は前回のv1.1.0統合(`git checkout -B main-release origin/main` → `git merge origin/develop --no-commit --no-ff` → 手動でGAME_VERSION/CHANGELOGを単一エントリに統合 → 回帰テスト → `push origin main-release:main`)と同じ。develop側の細かいバージョン履歴(v1.2.0〜v1.16.0)はmain側では既にv1.1.0の単一エントリに集約済みだったため、今回はv1.17.0〜v1.19.0の3件だけを新規のv1.1.1エントリとして統合(仮面あて削除・来訪カウント追加の2行に要約。admin.html限定のv1.19.0の内容はプレイヤー向けCHANGELOGには含めず)。マージ後、4パターンのCPU設定によるフルゲーム回帰テストが新規JSエラー・横スクロール崩れなしで通過することを確認してからpush。
  **運用メモ**: mainには`.github/workflows`による自動コミット(`chore: developプレビューを更新`、`/dev/`配下のプレビューファイルのみ更新)が定期的に入るため、`main-release`ブランチをpushする直前には必ず`git fetch origin main`で最新化し、必要なら`git rebase origin/main`してからpushすること(実際、本バージョンの作業中に1度push が rejected され、rebaseで解決した)。

- **v1.20.0（develop・現在）**: ユーザーから「他の項目もリセットしたいです（別の項目として追加して、見やすくしたい）」という依頼。直前のやり取りで、admin.htmlの日時表示が全てUTCだった(ユーザーが「UTC表記になっていませんか？」と指摘)ことが判明し、来訪履歴・登録プレイヤー・アンケート回答・対戦ログの日時表示を日本時間(JST)に修正済み(この修正自体は`fix:`コミットとして`develop`・`main`双方に即座に反映済み。バージョン番号は据え置きだったため、今回のCHANGELOGにまとめて記載)。続けて、「計測イベント」欄(セッション開始/対戦開始が同意タイミングの不具合でほぼ0件のまま溜まっている件)についても、ユーザーの元々の相談「どこまでが計測されているか分かりにくい」を踏まえ、既存の全期間集計とは別に「リセット基準日時」以降だけの集計を並べて表示できるようにしてほしいと依頼された。
  **確認**: 対象範囲(計測イベントのみ／バランス分析等も含め全体)と基準日時の決め方(ボタンを押した時点／固定の過去日時)をユーザーに確認し、「計測イベントのみ」「ボタンを押した時点」を選択。
  **設計判断**: 実データ(Firestore)は一切変更・削除しない。地に足のついた「非表示(論理削除)」の設計思想([hidden_records](#)と同様、追記のみで過去を消さない)を踏襲し、リセット基準時刻はadmin.htmlのブラウザのlocalStorageにのみ保存する表示上のフィルタとして実装(Firestoreには何も書き込まない・他の閲覧者やブラウザには影響しない)。
  **実装**: `eventStatsHTML()`を`eventCountsCardHTML(logs, title)`(集計＋bars描画を再利用可能な形に分離)と`eventResetCtrlHTML()`(基準時刻の表示・「ここからリセット」「今からに更新」「解除」ボタン)に分割。基準時刻が未設定なら従来通り全期間の1枚のカードのみ、設定済みなら「イベント件数（全期間）」カードに加えて、`evMillis(e)`(`serverTs.toDate()`優先、無ければクライアント`ts`をフォールバック)で基準時刻以降だけに絞った「イベント件数（リセット後）」カードを追加表示。ボタンは`.evreset-set`/`.evreset-clear`クラス+`bindEventResetButtons()`(`bindHideButtons()`と同じ「data属性を持つ要素にrenderの度onclickを再バインドする」既存パターンを踏襲)で動作。`EVRESET_KEY`はdev/prodプレビューで別々のlocalStorageキー(`_dev`サフィックス)を使うよう分離。
  **検証**: Playwrightで、①基準時刻未設定時は「全期間」カードのみ表示、②「ここからリセット」クリックで基準時刻ラベル・「リセット後」カード(該当イベント0件なら案内文)が表示、③「解除」クリックで元の表示に戻ることを確認。④`localStorage`に過去の基準時刻を直接セットし、基準時刻の前後にまたがるイベント群を用意した上で、「全期間」「リセット後」それぞれのカードの実際の件数(離脱2件→全期間、うち1件がリセット後、等)がバー要素のDOM値として正しく一致することをピクセル単位ではなく実データで検証。
  **ハマった点**: 検証スクリプトで`typeof showDash==='function'`のガードが常に`false`になり、`#dash`(CSSで`display:none`)がクリック可能な状態にならず`locator.click()`がタイムアウトする問題が発生。原因は`<script type="module">`内の関数宣言はグローバル(`window`)に付与されないため、`page.evaluate()`側のトップレベルスコープから`showDash`等の関数名を直接参照できないこと(`window.__adminMock`のように明示的に`window`へ代入した関数だけが外部から呼べる)。テストでは`document.getElementById('dash').style.display='block'`のようにDOM操作で直接迂回して解決。アプリ自体の実際の解錠フロー(`tryUnlock`ボタンクリック)には影響のない、テストスクリプト側だけの問題。
  **運用**: 管理画面の機能追加のため、`1.x.0`として`develop`ブランチにpush(`main`には反映していない)。

- **v1.21.0（develop・現在）**: v1.20.0のスクリーンショットを見たユーザーから「見方がいまいちわかりません。登録プレイヤー数も表示したいです。」との指摘。実際に、離脱数(4)が対戦開始数(1)を上回り「離脱率400.0%」という一見ありえない数値が出ており、かつ「全期間」と「リセット後」の境目が地の文(muted note)だけで視覚的に分かりにくかった。
  **実装**: ①`eventCountsCardHTML()`から`title`引数を除去し、代わりに新設`eventGroupHTML(logs, label, emptyNote)`が枠線・背景色つきの`.evgroup`ブロックとしてラベル(`📊 全期間` / `🔄 リセット後（日時〜）`)ごと視覚的にグルーピングするよう変更。②離脱率が100%を超える、または対戦開始数が3未満の場合に「⚠「対戦開始」の計測が実態より少なく出る既知の制限があるため、離脱率は参考値です。」という注記を表示(数値自体は隠さず、バーの表示だけ100%で頭打ちにして視覚的な破綻を防止)。③セクション冒頭に「プレイヤーがどこで離脱しているか…」という平易な説明文を追加。④トップのKPIグリッド(来訪数・総レコード数など横並びの数値タイル)に「登録プレイヤー数」(`totalPlayers`)を追加(既存の👤登録プレイヤーセクションと数値は重複するが、スクロールせず一目で見られるようにする目的)。
  **検証**: Playwrightで、①離脱4件/対戦開始1件(400%相当)を模したモックデータで警告注記が表示されること、②「全期間」「リセット後」それぞれのグループラベルが表示されること、③トップKPIの「登録プレイヤー数」が正しい値で表示されることを確認。スクリーンショットで実際のレイアウトも目視確認。4パターンのCPU設定によるフルゲーム回帰テストも新規JSエラー・崩れなしで通過。
  **運用**: 管理画面の改善のため、`1.x.0`として`develop`ブランチにpush(`main`には反映していない)。

- **v1.22.0（develop・現在）**: ユーザーから「管理画面へのログインをGoogle認証にしたい」と依頼。admin.htmlはこれまで、ハードコードしたパスフレーズのSHA-256ハッシュ(`PASS_SHA256`)と照合する簡易ゲートで保護していた(URLに`?pass=合言葉`を付けて通知リンクから自動解錠する仕組みつき)。
  **確認**: セキュリティ範囲について、①admin.htmlのログイン画面だけをGoogle認証に置き換える(Firestoreの読み取り権限自体は変更しない)か、②Firestore側の読み取りもGoogle認証必須にする(ただし`logs`コレクションはindex.html側の「みんなの統計」機能が公開読み取りに依存しているため、その機能自体の廃止・変更が必要になる)かをユーザーに確認し、①を選択。あわせて許可するGoogleアカウントのメールアドレス(`58flyingcar@gmail.com`)を確認。
  **実装**: `PASS_SHA256`/`UNLOCK_KEY`/`sha256hex()`/`tryUnlock()`/`tryAutoUnlockFromURL()`/`lock()`を全て削除し、代わりに`ADMIN_ALLOWED_EMAILS=["58flyingcar@gmail.com"]`と`initAuth()`を追加。Firebase Authentication(`firebase-auth.js`)を動的importし、`onAuthStateChanged`でログイン状態を監視。許可リストに載っているメールアドレスなら`showDash(user)`でダッシュボードを表示、載っていなければ即座に`signOut()`させて「このGoogleアカウント（…）には管理画面へのアクセス権がありません。」と表示する。ヘッダーに現在ログイン中のメールアドレスを小さく表示(`#authEmailTag`)し、「ロック」ボタンは「ログアウト」に変更(`signOut()`→`location.reload()`)。**ハマった点**: `initializeApp(firebaseConfig)`は同じ設定でも2回呼ぶとエラーになるため、従来`init()`(Firestore用、ダッシュボード表示後に遅延実行)が独自に呼んでいた`initializeApp`を`initAuth()`(ページ読み込み時に即実行)側に一本化し、`init()`はその結果(`firebaseApp`という共有変数)を再利用するだけに変更した。`index.html`側の`ADMIN_URL`(通知タップで管理画面を開くリンク)から`?pass=maskd-admin`のクエリを削除(Firebase Authは既定でログイン状態を端末に永続化するため、一度ログインすれば通知リンクを開くだけで再ログイン不要になる)。
  **検証**: Playwrightで、①ネットワーク遮断環境でも`initAuth()`が例外を投げずにエラーメッセージ(「Google認証の読み込みに失敗しました」)を表示することを確認。②`page.route()`で`firebase-app.js`/`firebase-auth.js`の動的importをスタブに差し替え、許可外アカウント(`stranger@example.com`)でログインすると即サインアウトされゲートに留まりエラー文言が出ること、許可アカウント(`58flyingcar@gmail.com`)でログインするとゲートが隠れダッシュボードが表示されヘッダーにメールアドレスが出ることを、実際のログイン〜許可判定〜表示切替のフルフローで確認(スタブは`firebase-firestore.js`まではカバーしていないため、データ読み込み自体は従来通りネットワーク制限で失敗するが、ゲート/認証ロジックの検証には影響しない)。4パターンのCPU設定によるフルゲーム回帰テストも新規JSエラー・崩れなしで通過。
  **運用**: 管理画面のセキュリティ改善のため、`1.x.0`として`develop`ブランチにpush(`main`には反映していない)。実際にFirebaseコンソールの「Authentication」→「Sign-in method」でGoogleプロバイダを有効化する作業がユーザー側で必要(未実施の間はログインボタンを押してもエラーになる)。
  **追記(適用作業でのつまずき)**: Googleプロバイダ有効化後も`auth/unauthorized-domain`エラーで実際にはログインできず、原因は`perusonao.github.io`がFirebaseの「承認済みドメイン」に未登録だったこと。この項目は直感に反して**「Authentication」→「設定」タブではなく「ログイン方法」タブの、プロバイダ一覧(メール/パスワード・Google)よりさらに下にスクロールした先**にある(Firebaseコンソールの商品検索でも「承認済みドメイン」「authorized domain」いずれもヒットしない=検索対象外の模様)。直接URL`https://console.firebase.google.com/project/maskd-8eafb/authentication/settings`でも「設定」タブに飛ぶだけで見つからず、結局「ログイン方法」タブを下までスクロールして発見した。今後同種の設定を案内する際は、この場所を最初から案内すること。

- **v1.23.0（develop・現在）**: ユーザーから「管理画面に表示されるデータを過去分を表示しないような構成にしたい」と依頼。v1.20.0で作った「計測イベント」欄限定のリセット機能(全期間とリセット後を並べて表示)とは異なり、今回は対象範囲・見せ方の両方が違うとユーザーに確認：①対象は計測イベント欄だけでなく対戦ログ・バランス分析・日別プレイ数・登録プレイヤー・アンケート・来訪履歴を含む**ダッシュボード全体**、②見せ方は両方並べるのではなく**過去分を完全非表示**にする、のどちらもユーザーが選択。
  **設計**: 実データ(Firestore・`*Raw`変数群)には一切触れず、admin.htmlのブラウザのlocalStorageに保存する「表示基準日時」1つで、ダッシュボード全体の表示だけをフィルタする。取得した生データを保持する`allLogsRaw`/`allSurveysRaw`/`allPlayersRaw`/`allVisitsRaw`(および対応する`totalXxxRaw`)と、実際に画面へ描画する`allLogs`/`allSurveys`/`allPlayers`/`allVisits`(および`totalXxx`)を分離。新設`applyDisplayFilter()`が、基準日時が設定されていれば`*Raw`から`recMillis(rec)>=基準`のレコードだけを抽出して表示用変数を再計算する(未設定なら`*Raw`をそのまま使う)。
  **実装**: `loadData()`・`window.__adminMock`は今後`*Raw`側にデータを詰め、最後に`applyDisplayFilter()`を呼んでから`render()`する形に統一。`render()`側は`allLogs`等の変数名をそのまま使い続けられるため、バランス分析・日別プレイ数・対戦ログテーブル(`renderRows`/`filtered`/`showDetail`)・CSVエクスポート等、既存コードの大部分は無変更で自動的にフィルタの恩恵を受ける(「今画面に表示されているものがそのままエクスポートされる」という一貫した挙動になる)。表示基準日時の操作UIは新設`displayResetCtrlHTML()`(「ここから表示をリセット」「今からに更新」「解除(全期間を表示)」ボタン、`.dispreset-set`/`.dispreset-clear`クラス+`bindDisplayResetButtons()`)としてダッシュボード最上部(アンケート回答セクションより上)に配置。`getCountFromServer`によるサーバー側総数(`totalCountRaw`等)は基準日時を考慮できないため、基準が有効な間は絞り込み後の配列長をそのまま「総数」として使う設計に変更(`$('meta')`の見出しも「総レコード数」→「表示中のレコード数」に切り替え)。v1.20.0で作った「計測イベント」欄専用のリセット機能(`EVRESET_KEY`/`eventGroupHTML`/`eventResetCtrlHTML`等)は、今回のダッシュボード全体機能に完全に置き換わるため削除し、`eventStatsHTML()`は他のセクションと同じ単一表示に戻した(離脱率100%超過時の注記・平易な説明文などv1.21.0の改善はそのまま維持)。
  **検証**: Playwrightで、①新旧混在データ(2026-07-10と2026-07-19)を`__adminMock`に渡し、基準日時未設定では両方表示、`2026-07-15T00:00:00Z`を基準に設定すると対戦ログ・アンケート回答(区別しやすい文字列で確認)・登録プレイヤー・来訪データそれぞれで古い方だけが正しく除外されること、②計測イベントの件数(離脱2件→基準設定後1件)が実データの集計値として正しく変わること、③「解除」ボタンで元の全期間表示に戻ることを確認。既存の来訪履歴・JST表示・Google認証関連のテストも再実行し、リグレッションが無いことを確認(2件は文言変更に伴う想定通りの差分)。
  **運用**: 管理画面の機能追加のため、`1.x.0`として`develop`ブランチにpush(`main`には反映していない)。

- **v1.1.2（main）**: ユーザーから「mainに1.1.2としてマージしてください」と明示的に依頼を受け、developのv1.18.0(来訪計測)〜v1.23.0(表示基準日時)をmainへ統合。手順は前回のv1.1.1統合と同じ(`main-release`ブランチで`git merge origin/develop --no-commit --no-ff`→手動でGAME_VERSION/CHANGELOGを単一エントリに統合→回帰テスト→push)。**マージ作業中のミス**: CHANGELOG配列のコンフリクト解決時、共通コンテキスト行(develop側の項目テキストと、その後に続く`]},`や次のバージョンエントリ)を古い側(HEAD)のold_stringに含めてしまい、新しいv1.1.2エントリを書いた後にv1.1.1エントリの閉じ括弧`]},`を書き忘れる構文エラーを作ってしまった。コミット前の`new Function()`構文チェックで検出し、`]},`を1行追加して修正(実際に壊れた状態でpushしてはいない)。以降、CHANGELOG配列のコンフリクト解決では、置き換えるブロックの開始・終了を`old_string`に完全に含めるか、置き換え後に必ず構文チェックを行うことを徹底する必要がある。
  **運用メモ**: 今回もmainへの自動プレビュー同期コミット(`chore: developプレビューを更新`)により`push`が一度rejectedになったため、`git fetch origin main`→`git merge-base --is-ancestor`で確認してからpushする手順を踏襲した。

- **v1.24.0（develop・現在）**: ユーザーから「ゲーム中、CPUが『奇襲』、『入替』を選択した際、早すぎてよくわからないので、演出を見直したい」と依頼。
  **調査**: `cpuDecide()`内のCPU入替(親としての入替)は、`showActionBanner()`等の演出が一切無く、`dealerCard`を書き換えて`cardHTML()`で即座に描画するだけだった(バナー0件・遅延0ms)。CPU奇襲(子として親の撤退に対抗)は`showActionBanner('kishuu',...)`(900ms)自体はあったが、`cpuTrust`減算・`hint()`表示・`renderScore()`をバナー表示**前**に全て終わらせてから見せていたため、スコアが無音で先に変わり、直後にバナーが一瞬光って終わる、という「結果が先、演出が後付け」の順序になっていた。
  **実装**: ①`showActionBanner()`に新カード`irekae`(🔄「入替！」、`--gold-bright`色)を追加し、`dur`は`kishuu`と揃えて900→1100msに延長。②`cpuDecide()`を再構成: 入替の可否判定(`swapIndex`)だけ先に行い、実際の差し替え(`dealerCard`書き換え・`cpuTrust`減算・`renderScore()`)は`showActionBanner('irekae', callback)`のコールバック内に移動。差し替え後さらに500ms待ってから決戦/撤退の判断(`decideFightOrPass`、名称は据え置きだが関数として抽出)に進むようにした。決戦/撤退の判断はこの新しい(差し替わった後の)`dealerCard`を見て行うため、CPUの戦略ロジック自体は変更していない(判断→演出→適用の順序だけを入れ替えた)。③CPU奇襲(子側)も同様に、`showActionBanner('kishuu', callback)`を先に呼び、`cpuTrust`減算・`hint()`・`renderScore()`はコールバック内に移動。さらにコールバック内で500ms待ってから`resolveFight()`に進むようにした(告知→結果反映→間を置いて決着、の3拍子に)。プレイヤー自身が任意で押す奇襲ボタン(相手が親で撤退した時に自分が選べる方)はプレイヤー操作なので変更していない。
  **検証**: Playwrightで、①`showActionBanner('irekae', cb)`を直接呼び、`action-banner irekae show`のクラス・「🔄入替！」のテキストが表示され、コールバックが約1100ms後に発火することを確認。②4種類のCPU性格(cautious/aggressive/tricky/chaotic)×3試合ずつ、計12試合を自動プレイ(ラウンドごとにランダムで撤退/決戦を選ぶよう調整し、CPUの入替・奇襲を誘発しやすくした)し、`.action-banner`のクラスに`irekae`が11/12試合、`kishuu`が7/12試合で実際に出現したこと、いずれの試合も新規JSエラーなくVICTORY/DEFEAT/DRAWまで正常に完走したことを確認。4パターンのCPU設定による通常の回帰テストも新規JSエラー・崩れなしで通過。
  **運用**: ゲームプレイの演出改善のため、`1.x.0`として`develop`ブランチにpush(`main`には反映していない)。

- **v1.1.3（main）**: ユーザーから「1.1.3としてmainにマージしてください」と依頼を受け、developのv1.24.0(CPU入替/奇襲の演出改善)をmainへ統合。手順は前回までと同じ(`main-release`ブランチで`git merge origin/develop --no-commit --no-ff`)。**前回(v1.1.2統合時)のCHANGELOG閉じ括弧忘れの反省を踏まえ**、今回はコンフリクト解決のold_stringに`<<<<<<< HEAD`〜`>>>>>>> origin/develop`の行だけを過不足なく含め(前後の共通コンテキスト行は絶対に混ぜない)、解決直後に`new Function()`で構文チェック＋`CHANGELOG`配列を実際にパースしてエントリ数・先頭バージョンを検証、という手順を徹底したところ、今回は一発でミスなく完了した。
- **Xプロモーション動画の生成**: ユーザーから「本日の改修をまとめて、Xに投稿したい」と依頼。当日の変更はv1.20.0〜v1.24.0の5件だが、うちv1.20.0〜v1.23.0はadmin.html限定の裏側改修でプレイヤー向けではないため、投稿内容はv1.24.0(CPU入替/奇襲の演出改善)一本に絞ることをユーザーに提案・合意。投稿文案を作成した上で「画像か動画をつけたい」と言われ、動画を選んで制作。**手法**: ローカルに`python3 -m http.server 8934`を起動し(`file://`ではなくHTTPサーバー経由にすることでrecordVideoの安定性を確保)、Playwrightの`recordVideo`で実際のゲームプレイ(`aggressive`/`strong`のCPU、過去の12試合テストで入替・奇襲とも100%発生したため選定)を約58秒録画。マーク用に`Date.now()`ベースの経過時間ログを仕込んだが、録画の実フレーム時刻とはズレが生じた(スクリーンキャストの遅延によるものとみられ、今回は約2〜3.5秒のズレ)。そのため`ffmpeg`の`showinfo`フィルタで各フレームの正確な`pts_time`を取得し、コンタクトシート(タイル画像)を目視して実際にバナーが映っている正確な区間を特定する方式に切り替えた。奇襲バナーは46.6〜47.7秒(dur:1100ms設定とほぼ一致)、入替バナーは52.7〜54.2秒に出現していることを確認し、それぞれ前後に余裕を持たせて4秒強ずつ切り出し(`ffmpeg -ss -t`)、`concat`で1本(8.48秒、402×708)に結合した。**教訓**: Playwright側の経過時間ログだけでは動画内の正確なタイミングを保証できない(スクリーンキャストの遅延が一定しないため)。動画のどの瞬間に何が映っているかは、必ず`showinfo`等でフレーム単位のpts_timeを取得するか、実際にフレームを抽出して目視確認してから編集すること。

- **v1.25.0（develop・現在）**: ユーザーとの会話「ユーザー登録されていないってことは、誰もチュートリアルはおろか、プレイしてくれていないってことですよね？」から発覚。調査の結果、①名前登録(`beginMatchSetup()`)はチュートリアルには不要で、対戦(練習/本戦問わず)を実際に始める時にだけ要求される仕組みだったため、「登録0件」は「対戦未着手」の根拠にはなっても「チュートリアル未着手」の根拠にはならないと判明。②さらに、admin.htmlの計測イベント欄に表示されていた「チュートリアル開始/完走」(`logEvent('tutorial_start'/'tutorial_complete')`)は、実は`openMissions()`という**旧UIの「上級ミッション」機能**からしか発火しない設計だったが、その旧UIへの唯一の入口だった`welcomeModal`はv1.14.0の「初回に遊び方スライドを直接表示」への刷新で`.classList.add('show')`を呼ぶ箇所が無くなっており、**到達不能なデッドコードと化していた**ことが判明(同意タイミングの問題以前に、そもそも現行のチュートリアル導線とは無関係だった)。
  **実装**: 現行の7段階チュートリアル(`LEARN_STAGES`)の実際の開始/完走ポイントに、`visits`と全く同じ設計思想(playerId等の識別子を持たない、同意不要・匿名の単純カウント)で新規計測を追加。新設Firestoreコレクション`tutorial_events`(フィールドは`type:'start'|'complete'`と`serverTs`のみ)。`startLearnStage(id)`の冒頭で`id===1`の時だけ`window.__maskdSaveTutorialEvent('start')`を呼び(ステージ1のみ=「チュートリアルを始めた」の意)、`endLearnStage()`で`next`(次のステージ定義)が存在しない時=最終ステージ(7)クリア時に`window.__maskdSaveTutorialEvent('complete')`を呼ぶ。両方とも同意チェックを一切行わない(visitsと同じ)。旧`logEvent('tutorial_start'/'tutorial_complete')`(`openMissions()`/`tutFinish()`)はデッドコードのまま放置(今回のスコープ外、実害なし)。
  **admin.html側**: `eventStatsHTML()`(計測イベント/離脱率のカード)から、常に0で意味を持たなかった「チュートリアル開始」「チュートリアル完走」「チュート完走率」の行を削除し、見出しも「計測イベント（離脱・チュートリアル完走）」→「計測イベント（離脱）」に変更。代わりに新設`tutorialEventsSectionHTML()`(`visitsSectionHTML()`と同じ構成)を、来訪履歴セクションのすぐ下に追加し、開始数・完走数・完走率を表示。`allTutEventsRaw`/`allTutEvents`を他の`*Raw`/表示用変数と同じパターンで追加し、`applyDisplayFilter()`のフィルタ対象にも含めたため、v1.23.0で作った「表示基準日時」機能はチュートリアル計測にもそのまま効く。
  **検証**: Playwrightで、①`startLearnStage(1)`は`start`を1回だけ発火し`startLearnStage(2)`は発火しないこと、②`endLearnStage()`は最終ステージ(id=7)クリア時のみ`complete`を発火し、非最終ステージ(id=3)クリアでは発火しないことを、`window.__maskdSaveTutorialEvent`をモックして厳密に確認。③admin.html側で、モックデータ(開始3件・完走1件)から開始/完走/完走率(33.3%)が正しく表示され、表示基準日時を設定すると古い開始記録だけが除外されて件数・完走率(50.0%)が正しく再計算されることをDOM値で確認。④実際のゲーム画面でステージ1を開始画面から起動し、ネットワーク遮断下でも例外を投げず正常に対戦が始まることを確認。4パターンのCPU設定による通常の回帰テストも新規JSエラー・崩れなしで通過。
  **要作業**: 他の同意不要コレクション同様、Firebaseコンソールで`firestore.rules`を再公開する必要がある(`tutorial_events`/`tutorial_events_dev`を追加済み)。
  **運用**: 計測基盤の追加のため、`1.x.0`として`develop`ブランチにpush(`main`には反映していない)。

- **v1.26.0（develop・現在）**: ユーザーから「初回ログイン時にユーザー名登録させるようにしたい」と依頼。
  **調査**: これまで名前登録(`showNameModal()`)は`beginMatchSetup()`(実際に対戦を始める操作)からしか呼ばれず、チュートリアルだけプレイして離脱したユーザーは一切登録されない設計だった(v1.25.0で判明した「登録0件≠チュートリアル未着手」の裏返し)。初回訪問時のフローは`maybeShowTutorialFirstTime()`(ページ読み込み末尾で無条件に呼ばれる)が`TUT_KEY`(遊び方スライド既読フラグ)だけを見て、未読なら450ms後に`openTutorial()`を開くだけだった。
  **実装**: `maybeShowTutorialFirstTime()`の先頭に`if(!playerName){ setTimeout(()=>{ showNameModal(maybeShowTutorialFirstTime); }, 450); return; }`を追加。`playerName`が未設定(=初回ログイン)なら、既存の名前入力モーダル(`nameModal`、閉じるボタンなし・空欄では`submitPlayerName()`が受け付けず`inp.focus()`するだけの「実質必須」仕様はそのまま流用)を450ms後に表示し、送信後のコールバックとして自分自身(`maybeShowTutorialFirstTime`)を渡すことで、名前登録→(未読なら)チュートリアルスライドの順で自動的に続くようにした。`beginMatchSetup()`側の`if(!playerName){ showNameModal(beginMatchSetup); return; }`は保険としてそのまま維持(通常は初回訪問時に名前が確定しているため素通りする)。
  **検証**: Playwrightで、①完全初回(localStorage空)でページを開くと名前モーダルが自動表示され、チュートリアルは(名前確定まで)表示されないこと、②空欄で送信してもモーダルが閉じないこと、③名前を入力して送信すると`localStorage`に保存されモーダルが閉じ、続けてチュートリアルスライドが表示されること、④名前もチュートリアル既読フラグも設定済みの既存プレイヤーでは何も自動表示されないこと、⑤名前だけ未設定でチュートリアルは既読の場合、名前モーダルのみ表示されチュートリアルは(既読のため)続けて出ないこと、の5パターンをJSエラー0で確認。4パターンのCPU設定による通常の回帰テストも新規JSエラー・崩れなしで通過。
  **運用**: 初回導線(オンボーディング)の仕様変更のため、`1.x.0`として`develop`ブランチにpush(`main`には反映していない)。

- **v1.1.4（main）**: ユーザーから「1.1.4としてmainにマージしてください」と依頼を受け、developのv1.25.0(チュートリアル計測)〜v1.26.0(初回名前登録必須化)をmainへ統合。手順は前回までと同じ(`main-release`ブランチで`git merge origin/develop --no-commit --no-ff`)。前回までに確立した手順(コンフリクト解決のold_stringに`<<<<<<< HEAD`〜`>>>>>>> origin/develop`の行だけを過不足なく含める、解決直後に`new Function()`構文チェック＋`CHANGELOG`配列を実パースして検証)を踏襲し、一発でミスなく完了。CHANGELOG本文は、v1.25.0(チュートリアル計測、個人情報を含まない旨をプレイヤー向けにも明記)とv1.26.0(初回名前登録必須化)の2行を新規v1.1.4エントリとして統合。4パターンのCPU設定によるフルゲーム回帰テストも新規JSエラー・崩れなしで通過。

- **v1.27.0（develop・現在）**: ユーザーから「管理ページでチュートリアルがどこまで終わっているかを一つずつ確認することはできますか？」と質問。v1.25.0で作った計測は「開始（ステージ1）」「完走（ステージ7）」の2点だけで、途中のどのステージで離脱しているかは分からなかった。
  **確認**: 「一つずつ」が①段階別(ステージ1〜7)の到達人数を匿名集計で見たいのか、②個々のプレイヤー単位で追跡したいのか(要プレイヤー識別、既存の匿名方針からの転換が必要)をユーザーに確認し、①(段階別の到達人数)を選択。
  **実装**: `tutorial_events`のスキーマに`stage_clear`タイプ(`stage`フィールド:1〜7の整数)を追加し、`isValidTutorialEvent()`のバリデーションを拡張(`d.size()<=4`、`stage_clear`の場合のみ`stage`が1〜7の整数であることを要求)。`endLearnStage()`側では、従来「最終ステージのみ`complete`を送信」していたのを、**毎ステージクリア時に`stage_clear`(該当する`st.id`つき)を送信**するよう変更(完走は`stage===7`のstage_clearとして自然に表現されるため、`complete`タイプは新規発行を停止。ただし後方互換のためFirestoreルール・admin.html集計側では引き続き受理・合算する)。また、`endLearnStage()`内にあった`logEvent('learn_stage_clear', {...})`(同意ゲート付き・playerId付きの`logs`コレクション行き)は、実は`isValidLog()`の許可タイプ一覧に`learn_stage_clear`が含まれておらずFirestore側で書き込みが常に拒否される**壊れたデッドコード**だったため、今回の対応と重複・混同を避ける目的で削除した。`window.__maskdSaveTutorialEvent(type, extra)`は第2引数でstage等の追加フィールドを渡せるよう拡張。
  **admin.html側**: `tutorialEventsSectionHTML()`を全面書き換えし、「開始」に続けてSTAGE1〜7それぞれのクリア人数を`bars()`で縦に並べる、ふるい落とし(ファネル)形式の表示に変更(`TUT_TOTAL_STAGES=7`はindex.html側の`LEARN_STAGES`件数と合わせた定数)。既存の「表示基準日時」フィルタ(`applyDisplayFilter()`)はそのまま効く(`allTutEvents`を経由するため変更不要)。
  **検証**: Playwrightで、①`startLearnStage(1)`→`endLearnStage()`→`startLearnStage(2)`→`endLearnStage()`→(ステージ7を模した)`endLearnStage()`の一連の呼び出しで、`start`が1回、`stage_clear`が各ステージ番号(1・2・7)つきで正しく発火することを`__maskdSaveTutorialEvent`のモックで確認。②admin.html側で、開始5件・STAGE1〜7ごとに5/4/4/3/3/2/2件という段階的に減っていくダミーデータを`__adminMock`に渡し、8行(開始+STAGE1〜7)全てが正しい件数で描画されることをDOM値で確認。いずれもJSエラー0。4パターンのCPU設定による通常の回帰テストも新規JSエラー・崩れなしで通過。
  **要作業**: `firestore.rules`の`isValidTutorialEvent()`を更新した(`stage_clear`タイプ・`stage`フィールドを許可)ため、Firebaseコンソールでの再公開が必要(`tutorial_events`/`tutorial_events_dev`向け)。
  **運用**: 管理画面の機能拡張のため、`1.x.0`として`develop`ブランチにpush(`main`には反映していない)。

- **v1.1.5（main）**: ユーザーから「1.1.5としてmainにマージしてください」と依頼を受け、developのv1.27.0(チュートリアル段階別計測)をmainへ統合。手順は前回までと同じ(`main-release`ブランチで`git merge origin/develop --no-commit --no-ff`、コンフリクト解決のold_stringに`<<<<<<< HEAD`〜`>>>>>>> origin/develop`の行だけを過不足なく含める、解決直後に`new Function()`構文チェック＋`CHANGELOG`配列を実パースして検証)。CHANGELOG本文はv1.27.0の1行を新規v1.1.5エントリとして統合。4パターンのCPU設定によるフルゲーム回帰テストも新規JSエラー・崩れなしで通過。

- **v1.28.0（develop・現在）**: v1.1.5公開直後、ユーザーから「登録プレイヤーが少しだけ増えましたが、チュートリアル完了しているプレイヤーがゼロなので、悩ましいです」と実データ(スクリーンショット)の共有あり。「開始（ステージ1）」9件に対し「STAGE 1 クリア」以降が全て0件という状況だった。
  **調査**: Playwrightで初回訪問者と全く同じ導線(名前登録→遊び方スライド8枚→ステージ選択→STAGE1説明→ラウンド1説明→対戦4ラウンド)を実際に辿って再現テストしたところ、技術的なバグは無く、STAGE1(3属性・4R)は正常にクリアまで到達することを確認(スクリーンショット付きで報告)。一方で、プレイ開始前に読む/タップする画面が「名前登録→スライド8枚→ステージ選択(7段階中6つが🔒でロック)→STAGE1説明→ラウンド1説明」と5段階もあり、特にステージ選択画面で最初から6つの🔒ステージが並ぶ「壁」が、初回訪問者の離脱要因になっているのではという仮説をユーザーと共有。改善案を3つ提示し(①遊び方スライド短縮、②ステージ選択画面の🔒表示を控えめに、③STAGE1説明とラウンド1説明の統合)、「コストが低く効果が見込める」として②を推奨、ユーザーが承認。
  **実装**: `openLearnLadder()`に`isFirstVisit`(`learnClearedMax()===0`)判定を追加。初回訪問時(1段階もクリアしていない状態)のみ、未解放ステージ(STAGE2〜7)を個別の🔒行として描画せず、`.ls-remain`という1行の軽い案内文(「この先 STAGE 2〜7 も、進めるごとに解放されます」)にまとめる。見出し文も「全7段階クリアで一人前！」(達成の重さを強調)から「まずはSTAGE 1を1戦だけ。クリアすると次のルールが解放されます。」(直近の1手にフォーカス)に変更。1段階でもクリアした後は、従来通り全7ステージを個別表示する分岐に戻る(進捗が見える方がモチベーションになる、という判断)。
  **検証**: Playwrightで、①`learnClearedMax()===0`の状態で`openLearnLadder()`を呼ぶと、`.ls-item`が1件(STAGE1のみ)・`.ls-remain`が表示されることをDOM値で確認、②`maskd_learn_stage=1`(STAGE1クリア済み)の状態では`.ls-item`が7件・`.ls-remain`は非表示で従来通りであることを確認、③初回導線を再度フルで自動プレイし、STAGE1クリアまで新規JSエラーなく到達することを確認。4パターンのCPU設定による通常の回帰テストも新規JSエラー・崩れなしで通過。
  **運用**: UI改善のため、`1.x.0`として`develop`ブランチにpush(`main`には反映していない)。実際に効果があったかは、しばらく実データ(「開始」に対する「STAGE 1 クリア」の比率)を見て判断する必要がある。

- **v1.1.6（main）**: ユーザーから「mainにマージして」と依頼を受け、developのv1.28.0(初回のステージ選択画面の改善)をmainへ統合。手順は前回までと同じ(`main-release`ブランチで`git merge origin/develop --no-commit --no-ff`、コンフリクト解決のold_stringに`<<<<<<< HEAD`〜`>>>>>>> origin/develop`の行だけを過不足なく含める、解決直後に`new Function()`構文チェック＋`CHANGELOG`配列を実パースして検証)。CHANGELOG本文はv1.28.0の1行を新規v1.1.6エントリとして統合。4パターンのCPU設定によるフルゲーム回帰テストも新規JSエラー・崩れなしで通過。あわせてXの投稿用に、変更前(6ステージが🔒で並ぶ画面)/変更後(STAGE1のみに絞った画面)を並べた比較画像を生成(Playwrightで両状態のスクリーンショットを撮り、HTML+CSSでカード風に合成)。**ハマった点**: 最初に作った「変更前」スクリーンショットは、実は既に修正後のUIを撮ってしまっていた(検証用に何度も`onboarding_walkthrough.js`を実行し直した際、同じファイル名`05_learn_ladder.png`を最新の(修正後の)コードに対して上書きしていたため)。`git show <修正前のコミット>:index.html`で修正前のindex.htmlの中身だけを取り出し、別ディレクトリで一時的にHTTPサーバーを立てて撮り直すことで正しい「変更前」を再取得した。以後、before/after画像を作る際は、比較対象のスクリーンショットのファイル名や取得タイミングが本当に別コードのものかを再確認すること。

- **v1.29.0（develop・現在）**: v1.28.0の効果を確認中のユーザーから、新しい実データ(開始10・STAGE1クリア1、離脱率450%等)の共有を受け、「まだ悩ましい」と相談。ラウンド数の選択制／チュートリアル最後だけ必須化+手厚いシナリオ化、の2案が提示され、「STAGE1の各ラウンドに簡単な正誤フィードバックを追加する」という軽量版をユーザーが承認。
  **設計判断**: 結果の勝敗ではなく「判断の過程」を評価する方針とした(不利な決戦に運良く勝っても、次への教訓としては「不利だった」という事実を伝える)。既存のコーチヒント(`showDecideCoach()`の⚖️有利/不利判定、`showChildCoach()`の💡おすすめ札)は「行動する前のアドバイス」だったが、今回追加したのは「行動した後、そのアドバイス通りに動けたか」を対戦結果画面でさりげなく振り返れる機能。STAGE1(`stageConfig.id===1`)限定とし、他ステージには影響しない。
  **実装**: 新規モジュール変数`tutMoveFeedback`(`{type:'decide'|'pick', ok:boolean}|null`、ラウンド開始時`startRound()`でリセット)を追加。①`resolveFight(doFight, isCpuDealer)`の冒頭で、プレイヤーが親として決戦/撤退を選んだ直後(`!isCpuDealer`)に、`playerEstimate`+`hintVerdict`から算出した⚖️の見立て(「有利」「不利」を含む場合のみ`advice`とし、「互角」等の曖昧な場面は評価対象外としてフィードバックを出さない)と実際の選択を比較して記録。②`playYouCard(idx)`で、プレイヤーが子として札を選んだ直後(`!youIsDealer`)に、手札に💡おすすめ札(`win>=0.6`)が存在したかどうか、存在した場合はそれを選べたかを記録。③新設`tutMoveFeedbackNote()`が、この記録を「✅ コーチの⚖️の見立て通り、良い判断でした。」等の1行に整形し、`resolveFightJudge()`(決戦時)と`finishPassReveal()`(撤退時)の結果詳細に追記。
  **検証**: Playwrightで、STAGE1を6戦分自動プレイしつつ、決戦/撤退や手札の選択を意図的にランダム化して多様なケースを作り、各ラウンドの決定直前に独立して算出した「期待される正誤」と、実際に`tutMoveFeedback`に記録された値・画面に表示された文言とを突き合わせ、22件のフィードバック発生イベント全てで一致することを確認(不一致0件)。**ハマった点**: 検証スクリプトの初回実装では、①戦略カウンター不足で「撤退」ボタンが無効化されている時に誤ってそれをクリックしてno-opになり前ラウンドの結果が残ったままになる、②固定のミリ秒待機(900ms)が、決戦の演出(バナー+開示×2の合計で最大1850ms程度)より短く、新しい結果が描画される前にDOMを読んでしまう、という2つのテスト側の不具合で見かけ上の不一致が出た。前者はdisabledボタンを避けて必ず決戦を選ぶよう修正、後者は固定待機を`#btnNextRound`に`show`クラスが付くのを待つ方式に変更して解消(いずれもテストコードの問題で、実装側のロジックは最初から一貫して正しかった)。4パターンのCPU設定による通常の回帰テストも新規JSエラー・崩れなしで通過。
  **運用**: ゲームプレイの改善のため、`1.x.0`として`develop`ブランチにpush(`main`には反映していない)。

- **v1.30.0（develop・現在）**: ユーザーから詳細な改善指示書(調査→計画提示→実装の順で進めること、STAGE1優先・新ゲーム機能は追加しない・通常対戦への影響最小限、という制約付き)を受け、STAGE1を「短い成功体験」へ再構成。
  **調査**: チュートリアルの画面/状態管理(`LEARN_STAGES`/`LEARN_KEY`/`stageConfig`/`inTutorialStage()`)、初回説明スライド(`TUTORIAL_STEPS`/`openTutorial()`/`maybeShowTutorialFirstTime()`)、カード選択/ラウンド結果/ステージ結果の実装箇所(`playYouCard`/`resolveFight`/`resolveFightJudge`/`endLearnStage`)、CPUのカード選択ロジック(`cpuChooseCard`/`cpuActAsChild`)、計測処理(`window.__maskdSaveTutorialEvent`、v1.25.0〜v1.27.0で作った匿名`tutorial_events`コレクション)を洗い出した上で実装。「learn_stage_clear」という名称のイベントは既に存在せず(v1.27.0で、Firestoreルールが許可していない型のため常に失敗していた壊れた`logEvent`呼び出しを削除し、`tutorial_events`のstage_clearに一本化済み)、依頼の「既存のlearn_stage_clearを可能な限り利用」は現行の`stage_clear`イベントの拡張として実施。
  **実装**: ①`LEARN_STAGES[0]`(STAGE1)を`rounds:1`・`forceDealer:'you'`・`guidedWin:{yourAttr:'sun',cpuAttr:'moon'}`の1問構成に再設計(openingHandも作り直し、CPUのMOON札が数字0にならないよう調整=数字ボーナスが必ず正になり必勝を保証)。②`startLearnStage()`で`forceDealer`を見て親を固定。③`cpuActAsChild()`に`stageConfig.guidedWin`分岐を追加し、CPUに指定属性(MOON)を強制的に出させる(見つからない場合は通常ロジックへフォールバックする安全装置つき)。④`startRound()`は`guidedWin`ステージでは`showRoundStartModal()`を省略し`beginRoundPlay()`を直接呼んで最初の操作までの時間を短縮。⑤`renderHand()`に`guidedAttr`判定を追加し、対象外の札を`opacity:.32`+`pointer-events:none`で一時的に選択不可にし、対象の札には新設`.guided-target`(金色パルス発光、`prefers-reduced-motion`対応)を付与、`coach()`で「👉このSUNカードを選びましょう」を表示。⑥`renderActions()`は`guidedWin`時に撤退/入替ボタンを出さず、決戦ボタンのみを`.guided-target`で光らせ、コーチ文言も「👉『決戦』ボタンを押して確定しましょう」に差し替え(撤退はSTAGE5で教える設計のため、STAGE1からは隠しただけで実装自体は変更していない)。⑦新設`winReasonCode()`(`judge()`の判定ロジックは複製せず、既に確定した`result`とカードの属性だけから`attribute`/`number`/`draw`/`other`に分類)と`winReasonBannerHTML()`(チュートリアル中のみ、結果カード上部に「SUNがMOONに勝利！」+「属性勝ち！」を大きく表示)を追加し、`resolveFightJudge()`の結果HTML生成に合成。⑧`endLearnStage()`に`st.guidedWin`分岐を追加し、既存の「ステージクリア/惜敗」表示とは別の専用演出(「STAGE 1 CLEAR」+「属性相性を覚えました！」、`sfx('win')`+`navigator.vibrate`+CSSポップアニメーション、ボタンは「次へ」1つのみ)を表示(youFinal>cpuFinalが保証されるため、惜敗分岐には元々入らない設計)。⑨`maybeShowTutorialFirstTime()`を変更し、初回訪問時に8枚の遊び方スライドを自動表示するのをやめ、直接ステージ選択(v1.28.0で既に最小化済みの1画面)へ誘導。8枚スライドはホームの「遊び方」ボタンから引き続き閲覧可能(既存機能を維持、削除はしていない)。
  **計測**: 既存の匿名`tutorial_events`(`visits`と同方式、playerId等の識別子なし)を拡張。新規`stage_start`イベント(全ステージ共通、`startLearnStage()`冒頭で発火)、既存の`start`/`stage_clear`に`tutorial_version:'2'`を追加、`stage_clear`にはセッション内のみで保持する`retry_count`(同一ステージの再挑戦回数)と`elapsed_ms`(ステージ開始からクリアまでの経過時間)を追加。`firestore.rules`の`isValidTutorialEvent()`を拡張し、`stage_start`型の許可、`tutorial_version`(string, ≤8文字)/`retry_count`(int, 0〜999)/`elapsed_ms`(int, 0〜3600000)のバリデーションを追加(`d.size()`上限を4→7に緩和)。依頼にあった`learn_first_card_selected`/`learn_round_result`(win_reason付き)/`learn_stage_exit`/独立した`learn_tutorial_complete`は、ライフサイクルイベントの重複送信防止など安全な実装に追加調査が必要なため、大規模化を避ける目的で今回は見送り(`stage_clear`のstage=7到達で完走はホーム画面の管理ダッシュボードから既に追える)。
  **状態保存の互換性**: `LEARN_KEY`(`maskd_learn_stage`、クリア済み最大ステージ番号の整数)の保存形式は一切変更していない。既存ユーザーの進行状況(例: STAGE3までクリア済み)はそのまま維持され、再訪時に何も自動表示されない。新規ユーザーのみ、新しいSTAGE1(誘導必勝フロー)を体験する。STAGE1を再挑戦(リトライ)しても同じ新フローが正常に動作する。
  **検証**: Playwrightで、①完全新規ユーザーが名前登録→(8枚スライドを経ずに)ステージ選択→STAGE1紹介(詳細行なし・短い文面)→SUN札のみ選択可能・強調→決戦ボタンのみ表示・強調→勝因バナー「SUNがMOONに勝利！／属性勝ち！」→「STAGE 1 CLEAR」演出、まで一気通貫でJSエラー0で完了することを確認。②`start`/`stage_start`/`stage_clear`(`tutorial_version`/`retry_count`/`elapsed_ms`付き)の発火を確認。③既存ユーザー(STAGE3までクリア済み)を模したlocalStorageで再訪しても、名前モーダル・ステージ選択とも自動表示されず進行状況(3)が維持されることを確認。④STAGE2は`guidedWin`が無いため、詳細行・ラウンド開始モーダル・4ラウンド構成とも従来通り変化がないことを確認。⑤STAGE1を2回連続プレイし`retry_count`が0→1と正しく増えることを確認。⑥SUN札・決戦ボタンを連打しても、既存の`busy`フラグにより二重進行せず「STAGE 1 CLEAR」が1回だけ表示されること、`localStorage`が正しく更新されること、リロード後も名前モーダルが再表示されず進行状況が壊れないことを確認。⑦4パターンのCPU設定による通常対戦の回帰テストが新規JSエラー・崩れなしで通過し、通常対戦のCPU/勝敗判定に影響がないことを確認。スクリーンショットで実際の見た目(カードの強調発光、勝因バナー、クリア演出)も目視確認。
  **要作業**: `firestore.rules`の`isValidTutorialEvent()`を更新した(`stage_start`型・`tutorial_version`/`retry_count`/`elapsed_ms`フィールドを追加)ため、Firebaseコンソールでの再公開が必要(`tutorial_events`/`tutorial_events_dev`向け)。
  **運用**: チュートリアルの導線・演出の大幅改善のため、`1.x.0`として`develop`ブランチにpush(`main`には反映していない)。

- **v1.31.0（develop・現在）**: v1.30.0のSTAGE1改善版をベースに、ユーザーから「さらに分かりやすさとテンポを向上させてほしい」と、6項目の詳細な改善指示(開始前説明の図式化、開始ボタンの明確化、決定ボタンの整理、勝利結果画面の情報量削減、最終結果を見る画面の省略、クリア画面の簡潔化)を受けて実装。
  **調査**: 「最終結果を見る」画面は、実際には既にv1.30.0の時点で`endGame()`が`stageConfig`ありの場合に`endLearnStage()`へ即分岐する設計になっており、中間のVICTORY/DEFEAT画面は表示されていなかった(ボタンの**文言だけ**が「最終結果を見る」のままで紛らわしい状態)。この調査結果を踏まえ、実際に画面を消す作業ではなく文言修正で済むと判断し、他の5項目に実装の重点を置いた。
  **実装**: ①`showStageIntro()`をSTAGE1限定で全面書き換えし、新設`attrCycleDiagramHTML()`(既存の`RPS_GLYPH`/`ALABEL`/属性カラー変数`var(--sun)`等を再利用、新規コンポーネントは最小限)でSUN→MOON→DARK→SUNの相性を図式表示。文章は「矢印の先の属性に強い」「今回はSUNを選ぼう」の2行のみ。②`openLearnLadder()`のSTAGE1カードに、視覚的な「STAGE 1を始める」ボタン(`.ls-startbtn`)を追加(クリックはカード全体の既存ハンドラに一本化し、ボタン自体には個別のonclickを付けないことで二重発火を回避)。③連打防止のため汎用`guardedNav(fn)`ヘルパーを新設し、ステージ選択・ステージ開始・クリア画面の遷移ボタンに適用(実装中、最初は500msのクールダウンにしたところ、テストで正規の連続操作(ラダー→紹介画面→開始)まで誤ってブロックする不具合を発見。DOM置き換えによる自然な二重発火防止で大部分はカバーされることを踏まえ、350msに短縮して解決)。④`renderActions()`のSTAGE1限定分岐で、ボタン文言を「決戦」→「決定」に短縮し、重複していた案内文(`.fa-ctx`の「決戦を選んで確定しよう」)を削除、`coach()`側の1メッセージ(「選んだカードで勝負しましょう」)に一本化。⑤`resolveFightJudge()`のSTAGE1限定分岐で、結果カードを「勝因バナー(あなたの勝ち/SUNがMOONに勝利！/属性勝ち！/スコア)」のみ先頭表示にし、既存の`detail`(得点内訳・戦略カウンター増減等)・`beginnerResultNote`・`tutMoveFeedbackNote`は新設`.rd-more`(初期非表示)へ移動、新設`toggleResultDetail()`で開閉できる「詳しく見る ▾／閉じる ▴」トグルを追加(既存の判定ロジック・データ処理は一切変更せず、表示順序と初期表示/非表示のみ変更)。⑥`showNextBtn()`をSTAGE1限定で「次へ」表示に統一(実際の遷移先は変更なし)。⑦`endLearnStage()`のSTAGE1クリア画面に「STAGE 2が解放されました」の案内(次ステージがある場合のみ)と、「次のステージへ」(主ボタン)/「ステージ一覧へ」(副ボタン、新設`advanceFromStageClear()`経由)を追加。
  **計測**: 新規`card_confirm`(最初のカード選択、`attr`/`elapsed_ms`付き)、`decide_confirm`(決定ボタン押下、`resolveFight()`の`busy`ガード後段に配置し連打時も1回のみ送信)、`result_detail_open`(「詳しく見る」を開いた瞬間、ラウンドごとに1回だけ)、`stage_advance`(クリア画面の「次のステージへ」を押した瞬間、`next_stage`付き)を、既存の`tutorial_events`(匿名・`visits`と同方式)に追加。`tutorial_version`を`'2'`→`'3'`に更新。`firestore.rules`の`isValidTutorialEvent()`を拡張し、新規4型と`attr`(5属性のいずれか)/`next_stage`(1〜7)フィールドのバリデーションを追加(`d.size()`上限を7→8に緩和)。依頼にあった`learn_first_card_selected`/`learn_card_confirm`/`learn_result_detail_open`等の命名例(`learn_`接頭辞つき)は、既存の`tutorial_events`コレクションが既にスコープを持っているため接頭辞なしの命名規則(`start`/`stage_start`/`stage_clear`と同様の裸の`type`値)に合わせて実装した。
  **検証**: Playwrightで、①小さい端末(360×640)で新規ユーザーの一気通貫フロー(名前登録→ラダーの明確な開始ボタン確認→図式表示確認(長文が出ていないこと)→SUN札選択→決定ボタン→結果画面の情報量確認(詳細が初期非表示)→詳しく見るタップで詳細表示→次へ→STAGE1 CLEAR(STAGE2解放案内含む)→次のステージへでSTAGE2紹介に遷移)を通し、いずれの画面でも横スクロールが発生しないことを含めてJSエラー0で確認。②`start`/`stage_start`/`card_confirm`/`decide_confirm`/`result_detail_open`/`stage_clear`/`stage_advance`の7イベントがそれぞれ正確に1回ずつ発火することを確認。③STAGE1のステージ選択カードを連打しても2重に画面遷移しないことを確認。④既存ユーザー(STAGE3までクリア済み)の非干渉、STAGE2(通常フロー)が図式化・詳細情報の折りたたみとも無関係で完全に無変更であることを確認。⑤4パターンのCPU設定による通常対戦の回帰テストが新規JSエラー・崩れなしで通過。スクリーンショットで実際の見た目(図式・開始ボタン・決定ボタン・結果画面の折りたたみ・クリア画面)を目視確認。
  **要作業**: `firestore.rules`の`isValidTutorialEvent()`を更新した(`card_confirm`/`decide_confirm`/`result_detail_open`/`stage_advance`型・`attr`/`next_stage`フィールドを追加)ため、Firebaseコンソールでの再公開が必要(`tutorial_events`/`tutorial_events_dev`向け)。
  **運用**: チュートリアルUIの改善のため、`1.x.0`として`develop`ブランチにpush(`main`には反映していない)。

- **v1.32.0（develop・現在）**: v1.31.0のSTAGE1改善版をベースに、ユーザーから「初見プレイヤーが気持ちよくSTAGE2へ進みたくなる体験」を作るための8項目の詳細なUI/UX改善指示(リファクタリング禁止、ルール/勝敗判定/Analytics/チュートリアル構成は変更しない制約付き)を受けて実装。
  **調査**: 結果画面(`resolveFightJudge`/`showNextBtn`)・STAGE1 CLEAR(`endLearnStage`)・名前入力(`nameModal`/`submitPlayerName`/`showNameModal`)・ステージ一覧(`openLearnLadder`/`LEARN_STAGES`)の実装箇所を特定。調査の結果、「①結果画面の情報量を減らす」はv1.31.0時点で既に「あなたの勝ち→勝因バナー→(詳しく見る)→スコア→次へ」の表示順・折りたたみが実装済みと判明し、今回は「②段階表示」のアニメーション追加のみで対応(表示順自体の変更は不要と判断)。
  **実装**: ①新設CSSクラス`.rc-staged`(`stageConfig.guidedWin`時のみ結果カードに付与)で、タイトル→勝因バナー→スコア/次へボタンの順に`animation-delay`(0/0.14/0.30/0.40秒、`rStepIn`キーフレーム)でフェード表示。付与タイミングは`resolveFightJudge()`内の既存`overlayDelay`(760ms、カードの砕ける/輝く演出を見せた後)のsetTimeoutコールバック内に合わせ、演出が見える前に段階表示が終わってしまわないようにした。`prefers-reduced-motion`では全要素即時表示。②`renderActions()`のSTAGE1限定分岐で、決定ボタンに`.guided-compact`(min-height 40px、上下パディング縮小)、ボタンを囲む`#fieldActions`に`.compact`(余白縮小)を追加(タップ領域40px以上は維持、他ステージ・通常対戦のボタンは無変更)。③`nameModal`に「あとから設定画面で変更できます」の小さな注記を追加。この文言を実際に正しくするため、設定画面(`appSettings`)に「✏️ プレイヤー名を変更」行を新設し、既存の`showNameModal(cb)`をコールバック付きで呼び出して名前を再編集できるようにした(入力必須仕様・保存先・Firebase同期ロジックは`submitPlayerName()`をそのまま再利用、変更なし)。④`LEARN_STAGES`の各ステージに実際の内容に忠実な`tagline`フィールド(例: STAGE1「属性を覚えよう」、STAGE4「相手の属性を読もう」)を追加し、`openLearnLadder()`の`.ls-d`表示を`stageSummary(st)`(属性名羅列)からこちらに差し替え(フォールバックとして`stageSummary`は保持)。⑤`endLearnStage()`のSTAGE1クリア画面に、新設`.s1c-flash`(金色の放射状グロー、0.55秒、reduced-motion時は非表示)を追加(拡大演出・振動・SEは既存のまま活用)。⑥同クリア画面の解放案内を「STAGE 2 解放！」+「次は{tagline}」の2行に変更し、STAGE2への導線を強化(テキストのみ、遷移先・ボタン構成は無変更)。
  **計測**: Analyticsのイベント名・パラメータは一切変更していない(`start`/`stage_start`/`card_confirm`/`decide_confirm`/`result_detail_open`/`stage_clear`/`stage_advance`の7種のまま)。今回のUI変更(段階表示・コンパクトボタン・タグライン等)は表示のタイミング/見た目のみで、どの発火ロジックにも触れていないため、Playwrightで実際にSTAGE1を一気通貫プレイしてイベント発火ログを取得し、送信漏れ・重複が無いことを確認した。`firestore.rules`の変更なし(Firebaseコンソールでの再公開作業は不要)。
  **検証**: Playwrightで、①小さい端末(360×640)・iPhone縦(390×844)の両方で新規ユーザーの一気通貫フロー(名前登録の注記確認→ラダーのタグライン確認→STAGE1紹介→SUN札選択→決定ボタンがコンパクト(40px以上)であることを確認→結果画面で`rc-staged`クラスと`animation-delay`が正しく設定されていることを確認→詳しく見るで詳細展開→STAGE1 CLEAR(金色フラッシュ+2行の解放案内)確認)を通し、横スクロール無し・JSエラー0で確認。②`getComputedStyle`のフレーム単位トレースで、タイトル→バナー→スコアの順に実際にopacityが段階的に上がっていくことを直接検証(初期実装では`animation-fill-mode`に`both`ではなく`forwards`のみを指定しており、遅延中は静的な`opacity:0`宣言が効いていたため見た目上は正しく動いていたが、念のためフレームトレースで実際の時系列を確認した)。③設定画面から名前を再編集すると、`nameModal`に現在の名前がプリフィルされ、変更後は設定画面のラベルにも即座に反映されることを確認。④STAGE2(`guidedWin`無し)・本戦(`stageConfig===null`)を`resolveFightJudge()`に直接最小状態を注入する形で個別に検証し、`rc-staged`クラスも`.guided-compact`も一切付与されず、結果画面の見た目が完全に旧来通りであることを確認(通常対戦・他ステージへの影響ゼロを実証)。⑤ラダー連打・戻る・リロードでもJSエラーや二重遷移が起きないことを確認。⑥4パターンのCPU設定による通常対戦の回帰テストが新規JSエラー・崩れなしで通過。スクリーンショットで実際の見た目(名前入力の注記、ラダーのタグライン、コンパクトな決定ボタン、結果画面、STAGE1 CLEARの金色フラッシュ、設定画面の名前変更行)を目視確認。
  **残っている懸念点**: 設定画面から名前を「変更」した場合も、既存の`submitPlayerName()`内のFirestore通知ロジック(ntfy.sh、開発者のスマホへ「新しいプレイヤーが登録しました」とプッシュ通知)がそのまま流用されるため、実際には新規登録ではなく改名であっても同じ文言で通知が飛ぶ(開発者向け内部通知のみで、プレイヤー側の見た目・データには影響なし)。今後、改名と新規登録を区別する通知文言に分けると、より正確な運用ログになる。
  **運用**: STAGE1周辺のUI/UX改善のため、`1.x.0`として`develop`ブランチにpush(`main`には反映していない)。

- **v1.33.0（develop・現在）**: v1.32.0の動画を見たユーザーから高評価(90→93〜95点相当)のフィードバックを受け、次の優先順位として「①チュートリアル完了後の導線改善」「②初回対戦の体験改善」「③Firebaseファネル分析」「④SNS流入ユーザーの登録率・継続率改善」の4項目を提示された。ユーザーが①を選択したため実装。
  **確認**: ユーザーの例示にあった「オンラインへ」は、本ゲームにオンライン対戦機能が存在しない(ホーム画面の「ランク」ボタンは`homeRankSoon()`で「近日公開予定」トーストを出すだけの未実装プレースホルダー)ため、実質「チュートリアル完了→本戦(CPU戦)への自然な導線」として実装する方針をユーザーに確認して合意を得た。また「チュートリアル完了直後の初めての本戦」のCPU強さ・相手選択について、現状維持(ランダム・普通の強さ)かデフォルトを弱めにするかを確認し、現状維持(ロジック変更なし)を選択された。
  **調査**: `endLearnStage()`の最終ステージ(`!next`、STAGE7クリア)分岐は、他ステージと同じ汎用の「ステージ クリア！」見出し+「全ステージ制覇、おめでとうございます！」の一文のみで専用演出が無く、CTAボタンも「本編を始める」という素っ気ない文言だった。一方、`beginMatchSetup()`は実は対戦設定画面を経由せず直接ライバル登場演出→本戦へ進む設計であることが判明し、「設定画面という壁」は元々存在しなかった(この点はユーザーへの報告で明確化)。
  **実装**: `endLearnStage()`の`!next`(最終ステージ)分岐を、通常のステージクリア分岐から完全に切り離した専用ブロックとして再構成(早期`return`)。既存のSTAGE1 CLEARで使っている`.stage1-clear-anim`/`.s1c-flash`(金色の放射状グロー・ポップ演出)をそのまま再利用し、見出しを「🎉 チュートリアル完了！」、サブテキストを「いよいよ実戦です！」に変更。実績/称号アンロック通知(`achNote`)はそのまま維持。CTAボタンを「本編を始める」→「初めての実戦へ ▶」に変更し、新設`startFirstRealMatch()`(`guardedNav()`でラップし連打保護)経由で`closeLearn(); beginMatchSetup();`を呼ぶ(本戦への遷移ロジック自体は無変更)。この演出は、最後のラウンドの勝敗(win/draw/lose)に関わらず表示する(非最終ステージの「惜敗…でも解放！」と同じく、チュートリアルは負けても進行が止まらない既存仕様をそのまま踏襲し、「到達」自体を祝う設計とした)。CPUの強さ・対戦相手抽選ロジック(`selectedCpuDifficulty`/`selectedCpuType`)は一切変更していない。
  **計測**: 新規`complete_advance`イベント(チュートリアル完了画面の「初めての実戦へ」を押した瞬間、`stage:7`固定+`tutorial_version`)を、既存の匿名`tutorial_events`に追加。今後、`stage_clear`(stage=7、チュートリアル完了数)に対する`complete_advance`の比率で、チュートリアル完了→本戦着手への転換率が測れるようになる。`firestore.rules`の`isValidTutorialEvent()`を拡張(`complete_advance`型を追加、`d.size()`上限は8のまま)。
  **検証**: Playwrightで、①STAGE7を勝利で終えると、専用演出(金色フラッシュ、「🎉 チュートリアル完了！」「いよいよ実戦です！」)と実績/称号通知が正しく表示され、「初めての実戦へ」をタップするとライバル登場演出(`rivalIntro`)へ正しく遷移し`learnOverlay`が閉じることを確認。②`stage_clear`(stage:7)と`complete_advance`(stage:7)の両イベントが正確に1回ずつ発火することを確認。③STAGE7を負けで終えても同じ完了演出が表示され、進行度(`learnClearedMax()`)が7まで正しく解放されることを確認(既存の「惜敗でも解放」仕様を維持できていることの実証)。④STAGE3(非最終ステージ)のクリア画面が、旧来のヘッダー形式・「次のステージへ」ボタン・フラッシュ無しのまま完全に無変更であることを確認。⑤小さい端末(360×640)・iPhone縦(390×844)の両方で、新しい見出しテキストが折り返しても横スクロールが発生しないことを確認。⑥v1.32.0までの全回帰テスト(STAGE1誘導フロー・名前変更・タグライン・段階表示・STAGE2/本戦への非干渉)を再実行し、崩れが無いことを確認。
  **要作業**: `firestore.rules`の`isValidTutorialEvent()`を更新した(`complete_advance`型を追加)ため、Firebaseコンソールでの再公開が必要(`tutorial_events`/`tutorial_events_dev`向け)。
  **運用**: オンボーディング導線の改善のため、`1.x.0`として`develop`ブランチにpush(`main`には反映していない)。ユーザーからは、次の優先順位として「②初回対戦の体験改善」「③Firebaseファネル分析(STAGE1開始→…→通常対戦終了までの各段階の離脱率を可視化)」「④SNS流入ユーザーの登録率・継続率改善」も提示されており、次回セッションではこれらを検討するとよい。

- **v1.34.0（develop・現在）**: v1.33.0に続き、ユーザーが優先順位②「初回対戦の体験改善」を選択したため実装。
  **調査**: 実戦(通常対戦)の結果画面(`endGame()`)は、得点内訳テーブル・MVP/通算成績/連勝のマイルストーン・アンケート導線・実績/称号通知・折りたたみ式の詳細統計、という構成で、これは2戦目以降のリピーターにとっても2戦目以降の全ての対戦で共通のUIだった。チュートリアル完了直後の「初めての本戦」だけに特化した専用の歓迎・継続促進の一言は存在せず、「もう一度」ボタンも常に同じ素っ気ない文言だった。
  **実装**: `endGame()`の冒頭(`saveGameToHistory()`で履歴に保存する直前)で`const isFirstEverRealMatch = !loadGameHistory().some(g=>g.isTop);`により「本戦(`isTop:true`)としては初めての試合か」を判定(練習モード`isTop:false`の記録が先にあっても正しく「本戦初回」と判定されるよう、`.length===0`ではなく`isTop`の有無で判定する設計にした)。初回本戦の結果画面にだけ、新設`.first-match-note`(勝ちなら「🎉 初めての本戦、勝利おめでとうございます！」、負け・引き分けなら「初めての本戦、お疲れ様でした！次はきっと勝てます」)を得点内訳テーブルの直後に追加。「もう一度」ボタン(`id="ovAgainBtn"`を新設)のテキストも、初回本戦の時だけ「もう1戦挑戦する ▶」に変更(2戦目以降は`endGame()`が呼ばれるたびに`isFirstEverRealMatch`を再判定して通常の「もう一度」に自動的に戻るため、特別なリセット処理は不要)。CPUの強さ・対戦相手の抽選ロジック(`selectedCpuDifficulty`/`selectedCpuType`/`cpuStrategy`)、得点計算(`youFinal`/`cpuFinal`の算出式)、実績判定(`checkAchievements`)には一切触れていない。
  **計測**: 新規`first_match_result`イベント(初回本戦の結果を`result:'win'|'lose'|'draw'`で記録)を、既存の匿名`tutorial_events`に追加。`firestore.rules`の`isValidTutorialEvent()`を拡張し、`first_match_result`型と`result`フィールド(enum検証)を追加(この型は`stage`フィールドを必須にしない唯一の型)。これで今後、「STAGE7クリア→本戦着手(complete_advance)→初回本戦の勝敗(first_match_result)」という導線全体をFirebaseで追えるようになる。
  **検証**: Playwrightで、①`endGame()`を初回本戦を模した状態(履歴を空にした状態)で直接呼び出し、勝利時の一言・「もう1戦挑戦する ▶」ボタン・`first_match_result`(result:'win')イベントの発火を確認。②同じ端末で2戦目を模した状態(履歴に1件残った状態)で再度`endGame()`を呼ぶと、一言が表示されず「もう一度」ボタンに戻ることを確認(2戦目以降への影響が無いことの実証)。③負け・引き分けでも初回本戦なら正しい(敗北時用の)一言が表示されることを確認。④練習モード(`isTop:false`)の記録だけが先にある状態でも、本戦としては初回と正しく判定され一言が表示されることを確認(`.length===0`ではなく`isTop`ベースの判定にした設計判断が正しく機能していることの実証)。⑤v1.30.0〜v1.33.0までの全回帰テスト(STAGE1誘導フロー・名前変更・タグライン・段階表示・チュートリアル完了演出・STAGE2/本戦への非干渉)を再実行し、崩れが無いことを確認。⑥スクリーンショットで実際の見た目(得点内訳→一言→マイルストーン→アンケート→実績通知→「もう1戦挑戦する ▶」ボタン)を目視確認。
  **要作業**: `firestore.rules`の`isValidTutorialEvent()`を更新した(`first_match_result`型・`result`フィールドを追加)ため、Firebaseコンソールでの再公開が必要(`tutorial_events`/`tutorial_events_dev`向け)。
  **運用**: 初回対戦の体験改善のため、`1.x.0`として`develop`ブランチにpush(`main`には反映していない)。次の優先順位として、ユーザーからは「③Firebaseファネル分析」「④SNS流入ユーザーの登録率・継続率改善」も提示されている。

- **v1.35.0（develop・現在）**: v1.34.0に続き、ユーザーが優先順位③「Firebaseファネル分析」を選択したため実装。この回はゲーム本体(`index.html`)ではなく管理画面(`admin.html`)限定の改修。
  **調査**: `admin.html`の`tutorialEventsSectionHTML()`(v1.27.0で新設)は、既に「開始(ステージ1)→STAGE1〜7クリア(完走)」までのふるい落とし(ファネル)を`bars()`ヘルパーで表示していたが、v1.33.0/v1.34.0で追加した`complete_advance`(本戦への着手)・`first_match_result`(初回本戦の勝敗)のイベントは、まだこのファネルに反映されていなかった(せっかく計測しても見る手段が無い状態)。
  **実装**: `tutorialEventsSectionHTML()`の`rows`配列に、STAGE7クリアの行に続けて「本戦に挑戦（初めての実戦へ）」(`complete_advance`の件数)と「初回本戦を終えた」(`first_match_result`の件数)の2行を追加(既存の`bars()`をそのまま再利用、新規コンポーネントなし)。下部の注釈にも、本戦挑戦率(本戦に挑戦/完走)・初回本戦完了率(初回本戦を終えた/本戦に挑戦)・初回本戦の勝率(`first_match_result`の`result:'win'`集計)を追加。既存のSTAGE別ふるい落としの計算式・表示・空データ時のメッセージ(「チュートリアルの開始/進捗記録はまだありません」)は一切変更していない。
  **検証**: Playwrightで、既存のテストフック`window.__adminMock(...)`(第9引数`tutEvents`)にダミーの`start`/`stage_clear`(1〜7)/`complete_advance`/`first_match_result`(win 3件・lose 1件)を渡し、①各バー行の値(開始10、STAGE1〜7クリアが段階的に減少、本戦に挑戦5、初回本戦を終えた4)が正しく集計されること、②注釈の各種比率(完走率60.0%・本戦挑戦率83.3%・初回本戦完了率80.0%・初回本戦の勝率75.0%)が正しく計算されることを確認。③`tutEvents`が空配列の場合、従来通り「チュートリアルの開始/進捗記録はまだありません」の空データメッセージが表示され、新規行が描画されないことを確認(空データ時の後方互換性を実証)。④`index.html`/`admin.html`双方の構文チェックを実施(`admin.html`は`<script type="module">`のためモジュール構文を考慮した抽出方法で検証)。⑤スクリーンショットで実際の見た目(ファネルバーグラフに新2行が追加され、既存セクションのレイアウトが崩れていないこと)を目視確認。
  **要作業**: Firestore/Analyticsの変更は無いため、Firebaseコンソールでの再公開作業は不要。
  **運用**: 管理画面限定の裏側改修(プレイヤー側の見た目に変更なし)のため、`1.x.0`として`develop`ブランチにpush(`main`には反映していない)。次の優先順位として、ユーザーからは「④SNS流入ユーザーの登録率・継続率改善」が提示されている(現状、流入元を区別する計測が存在しないため、着手する場合はまず新しい計測の追加から始める必要がある)。

- **v1.1.7（main）**: ユーザーから「1.1.7としてmainにマージして」と依頼を受け、developのv1.29.0〜v1.35.0(STAGE1の正誤フィードバック〜チュートリアル完了導線〜初回本戦体験〜管理画面ファネル拡張まで、計7バージョン分)をmainへ統合。手順は前回までと同じ(`main-release`ブランチで`git merge origin/develop --no-commit --no-ff`、コンフリクト解決のold_stringに`<<<<<<< HEAD`〜`>>>>>>> origin/develop`の行だけを過不足なく含める、解決直後に`new Function()`構文チェック＋`CHANGELOG`配列を実パースして検証)。CHANGELOG本文は、develop側のv1.29.0〜v1.35.0の7エントリを、新規v1.1.7エントリの`items`配列に新しい順で1行ずつ統合(v1.1.4での複数バージョン統合と同じ形式)。`admin.html`(v1.35.0のファネル拡張)・`firestore.rules`(v1.30.0〜v1.34.0で追加した各イベント型の許可)も合わせて統合。4パターンのCPU設定によるフルゲーム回帰テストも新規JSエラー・崩れなしで通過。

- **v1.36.0（develop・現在）**: v1.1.7公開後、ユーザーから「実戦(CPU戦)がチュートリアル全7ステージクリア必須になっていないか」と確認あり。`onHomePlay()`(`index.html`)が`learnAllCleared()`を要求している実装を提示して確認したところ、ユーザーからadmin.htmlの実データで「STAGE2クリアが0件」という共有があり、全7ステージ完走前提の設計がほぼ全プレイヤーをSTAGE1で足止めしている強い証拠と判断。「STAGE1クリアだけ必須化」という軽い修正ではなく「7ステージ構成自体の根本的な再設計」を希望されたため、Plan Mode(EnterPlanMode)で調査に入った。
  **要件確定の経緯**: ユーザーから、既存のWeb実装とは無関係に外部で作成されたと思われる非常に詳細な改修計画書(v0.9、チュートリアル→初級/中級/上級リーグ→フリー対戦という新構成、Phase1〜6の実装優先順位、教官仮面、セーブデータ設計、将来のFlutter版移植を見据えた設計など)が提示された。この計画書は現在の実装(ホーム画面の実際のボタン構成、`beginMatchSetup()`等)を踏まえずに書かれていたため、実コードとの対応関係をこちらから提案し直す形で調整した。Plan Mode中にAskUserQuestionでスコープ確認を試みたところ一度拒否され(「STOP what you are doing and wait」)、直後にコンテナ再起動でPlan Modeも終了したが、作業内容(調査結果・plan.md)は保持されたため、通常のチャットで方針を文章提示→ユーザーが番号付きで訂正、という形に切り替えて合意形成した。
  **最終合意**: ①フリー対戦=既存`beginMatchSetup()`/CPU選択/難易度選択をそのまま流用しメニュー上の名称のみ変更(判定ロジック不変)。②既存ユーザーの移行はシンプルな3区分(未着手→そのまま／STAGE1〜6→初級リーグ解放／STAGE7クリア→上級まで全解放)。③Phase1の範囲はホーム画面・初回起動判定・スキップのみとし、チュートリアル本体(STAGE1〜7の内容・判定)には触れない。④ユーザー追加要望として、Phase2以降の実装を楽にするため「GameMode」定義(TUTORIAL/BEGINNER/INTERMEDIATE/ADVANCED/FREE)をPhase1のうちに用意しておく(未使用でもよい)。
  **調査で判明した重要事項**: (a) ホーム画面の各ボタン(「対戦する」等)は全て画像アセット(`btn-duel.png`等)であり、新規アセットなしにテキストを書き換えられない。ただし画像化以前に使われていた**テキスト版CSS**(`.home-cta`の`hc-jp`/`hc-en`、`.hm-btn`の`hm-ic`/`hm-jp`/`hm-en`)が未使用のまま残存しており、これを再利用すれば新規アセット無しで新しいラベルを実現できると判明。(b) 「ようこそ！チュートリアルを始めますか？」に相当するUIも、`welcomeModal`/`welcomeChoose()`として過去に実装されたまま、どこからも`.classList.add('show')`されず**到達不能なデッドコード**と化していたものが存在し、これを復活・改修して使えると判明(過去のセッションでも同種のデッドコード発見が何度かあったパターンと同じ)。(c) ステージ数・7という数字への依存は`LEARN_STAGES.length`経由の動的参照がほとんどで、`st.id===1`等の個別ハードコードはSTAGE1限定の演出分岐のみに限られており、ステージ構成自体を変える際の壊れやすさは低いことを確認。
  **実装(Phase1のみ)**: ①`GameMode`定数(`Object.freeze`)と、新規localStorageキー群(`LEAGUE_KEYS`: `tutorialCompleted`/`beginnerUnlocked`/`intermediateUnlocked`/`advancedUnlocked`/`currentLeague`/`gameMode`/`firstLaunchDone`)、その読み書きヘルパー(`getLeagueFlag`/`setLeagueFlag`/`getCurrentLeague`/`setCurrentLeague`)を新設。②`migrateToLeagueSystem()`を新設し、`maskd_league_migration_v1`という一度きりフラグで移行が二重に走らないようにした上で、`learnClearedMax()`(旧7ステージ制の到達済み最大ステージ)から新フラグへ一括変換(未着手=何もしない／1〜6=初級解放／7=全解放)。スクリプト読み込み時に1回呼び出す。③ホーム画面の「対戦する」(画像ボタン`home-cta-img`)を、テキスト版`.home-cta`(「フリー対戦」/「FREE MATCH」)に差し替え。`onHomePlay()`から`learnAllCleared()`によるゲートを撤去し、常に`openMatchSetup()`(既存の対戦設定画面)を開くようにした。8枚スライドの「遊び方」を読み終えた後の遷移(`tutNext()`)も同様にゲートを撤去。④ホームに新設`#leagueMenuRow`(`renderLeagueMenuRow()`が描画)を追加し、初級/中級/上級リーグを`.hm-btn`(テキスト版)で表示。解放状態に応じて🔒/🏆アイコンと`.locked`(半透明)スタイルを切り替える。タップすると`leagueComingSoon()`が「近日公開予定」的なトースト(`homeToast()`、既存の`homeRankSoon()`と同じパターン)を表示するのみで、実際のリーグ対戦自体はPhase3以降で実装予定。⑤`welcomeModal`の文言を「MASKDへようこそ！」「チュートリアルを始めますか？」「始める」「あとで」に変更し、`maybeShowTutorialFirstTime()`を、以前は自動的に`openLearnLadder()`へ飛ばしていたのをやめてこのモーダルを表示するよう変更。「始める」は`openLearnLadder()`(チュートリアル本体はまだ旧7ステージ制のまま、Phase2で置き換え予定)、「あとで」は閉じるだけ(フリー対戦はすでに常時解放済みのため追加の遷移は不要)。⑥`openLearnLadder()`と`showStageIntro()`の両画面に「チュートリアルをスキップ」リンク(既存の`.home-learn-link`スタイルを再利用)を追加し、新設`skipTutorial()`が`beginnerUnlocked`を解放してホームへ戻る(旧7ステージの進行データ・判定ロジックには一切触れない)。
  **検証**: Playwrightで、①完全新規ユーザーが名前登録→「ようこそ」モーダル表示→「あとで」でホームに留まり、その時点でチュートリアル未修了のままフリー対戦(対戦設定画面)が開けることを確認。②リーグ行が3つとも🔒ロック表示であることを確認。③「始める」を選ぶとチュートリアルラダーが開き、「チュートリアルをスキップ」リンクの表示、スキップ実行で`beginnerUnlocked`が解放されることを確認。④既存ユーザーの移行を3パターンで検証: STAGE3までクリア済み→`beginnerUnlocked`のみtrue・`currentLeague`が`beginner`、全7ステージクリア済み→3フラグ全てtrue・`currentLeague`が`advanced`(リーグ行の表示も全て解放状態になることをDOM値で確認)、完全未着手(`TUT_KEY`既読のみ)→何も解放されず「ようこそ」モーダルも出ない(初回以外は出さない既存仕様を維持)。⑤移行処理が一度きりであること(手動でフラグをfalseに戻してリロードしても再上書きされない)を確認。⑥v1.29.0〜v1.35.0までの全既存回帰テスト(STAGE1誘導フロー、名前変更、タグライン、段階表示、チュートリアル完了演出、初回本戦体験、STAGE2/本戦への非干渉)を再実行し、崩れが無いことを確認(既存の名前登録直後のテストは、新設の「ようこそ」モーダルを閉じるステップの追加が必要だったため、テストスクリプト側を修正して対応)。⑦360×640・iPhone縦(390×844)双方で新しいホーム画面に横スクロールが発生しないこと、リーグボタンのラベルが不自然に折り返さないこと(`white-space:nowrap`を追加して対応)を確認。スクリーンショットで実際の見た目を目視確認。
  **要作業**: Analytics/Firestoreの変更は無いため、Firebaseコンソールでの再公開作業は不要。
  **運用**: `1.x.0`として`develop`ブランチにpush(`main`には反映していない)。**Phase2(STEP0〜7の新チュートリアル本体)・Phase3(初級リーグ、18枚デッキ・段階的配札)・Phase4(中級リーグ、AIR→KAOS→戦略カウンター→奇襲の順に解禁)・Phase5(上級リーグ、入替含む完全ルール)・Phase6(フリー対戦への設定変更機能)・教官仮面・演出は未着手**。次回セッションでは、まずPhase2(新チュートリアル本体)から着手するとよい。ユーザーの元の改修計画書(v0.9)は今後の実装の参照元として重要なので、次回セッション開始時に再度参照できるよう本チャットログを確認すること。

- **v1.37.0（develop）**: v1.36.0確認後、ユーザーから「ボタンは後ほど画像と差し替えましょう」と了承を得た直後、実際のUIモックアップ画像2枚(改修後ホーム画面イメージ、6人の仮面キャラクター一覧)が添付され、詳細なホーム画面リニューアル指示を受けて実装。
  **調査で判明した重要事項**: (a) 添付キャラクター一覧(剛力仮面ノクス/太陽・鉄壁仮面ゼファ/闇・技巧仮面アルル/月・調和仮面リヴェル/風・無秩序仮面カオス/混沌・指導仮面アストラ/星)は、**`CPU_TYPE_META`に寸分違わず既に定義済み**で、`RIVAL_IMG`オブジェクトに専用の立ち絵(base64)も**既に埋め込まれ実際に使われていた**(CPU_TYPE_METAのコメントに「専用の立ち絵は別途用意予定」とあったが、それは既に満たされていた)。`rivalImgUrl(typeKey)`を呼ぶだけで、新規アセット作成・配置作業なしにキャラクター画像を利用できると判明。(b) 「仮面見習い★★★★☆」のような星評価は、v1.24.0で★クリア条件が廃止されて以来対応するデータが存在せず、ユーザー自身の指示(「データを捏造しないでください」)に従い**表示を省略**。(c) 「今日のミッション：報酬100コイン」も対応するデイリーミッション/コイン機能が存在しないため、既存の`openMissions()`への導線として固定文言「ミッションを確認しよう」に留めた。(d) 初級リーグ「現在:第2戦/全5戦」も実体が無いため進行度表示は省略し、クリック時は既存のチュートリアルラダー(`openLearnLadder()`)へ接続(ユーザー指定の優先順位「1.既存リーグ処理→2.既存チュートリアル→3.準備中」に従った)。中級/上級リーグはクリックしても「準備中」トーストのみとし、既存の本戦(`beginMatchSetup()`)へ勝手に接続することはしなかった(ユーザーの明示的な禁止事項)。(e) `.home-screen`の背景は既に`rgba(6,5,10,..)`系のほぼ黒に近い多重グラデーションで、添付イメージの「ほぼ黒」トーンを実質満たしていたため、グローバルな`--bg-void`等は変更せず、新規コンポーネント(リーグカード等)は同じ`rgba(8,6,14,..)`系の暗色パネルで統一(既存の`.home-badge`/`.home-bell`と同じ配色手法を踏襲)。
  **実装**: ①CSSに新規コンポーネント一式(`.mentor-say-home`/`.league-list`/`.league-card`(+`.current`/`.locked`修飾)/`.mission-bar`)を追加(既存の`--gold`/`--ink`/`--win`/`--lose`/`--sun`/`--moon`/`--dark`/`--air`をそのまま再利用し、新規グローバル変数は増やしていない)。`.home-screen`に`env(safe-area-inset-top/bottom)`を追加し、縦スクロールを許可(`overflow-y:auto`)。`prefers-reduced-motion`でトランジションを無効化。②ホーム画面HTMLを全面差し替え: `#homeScreen`直下に`.home-top`(プレイヤー情報バッジ+お知らせベル、両方`<button>`要素化してアクセシビリティ向上)→MASKDロゴ→`#homeMentorSay`(指導仮面アストラの案内吹き出し)→`#leagueList`(リーグカード4枚)→`.mission-bar`→既存の下部メニューグリッド(遊び方/ルール/実績/ミッション/ランク/戦績)→設定、の順に再構成。5属性カードの横並び(`#maskFan`)は削除(`renderMaskFan()`自体は残置、要素が無ければ何もしない安全な設計のため実害なし)。③JS新設: `renderHomeMentorSay()`(`rivalImgUrl('mentor')`を再利用)、`renderLeagueCards()`+`leagueCardHTML()`(初級=`rivalImgUrl('aggressive')`+`openLearnLadder()`、中級=`rivalImgUrl('cautious')`+ロック判定、上級=`rivalImgUrl('tricky')`+ロック判定、フリー対戦=`rivalImgUrl('balanced')`+`onHomePlay()`)、`leagueComingSoon(name)`(中級/上級タップ時の「近日公開予定」トースト、既存`homeToast()`を再利用)。v1.36.0の`GameMode`/`LEAGUE_KEYS`/`getLeagueFlag`/`getCurrentLeague`等はそのまま流用し、重複定義はしていない。旧`renderLeagueMenuRow()`/`leagueComingSoon(name,unlocked)`は新関数に置き換え。
  **検証**: Playwrightで、①完全新規ユーザーで4枚のリーグカードが正しい状態(初級=解放+current、中級/上級=ロック、フリー対戦=解放)で描画され、各カードにキャラクター立ち絵の`background-image`が設定されていることを確認。②指導仮面アストラの吹き出し(顔画像+テキスト)が描画されることを確認。③`#maskFan`要素がDOMから削除されていることを確認。④初級リーグタップ→チュートリアルラダーが開く、中級リーグタップ→「準備中」トースト(ラダーや本戦には遷移しない)、フリー対戦タップ→対戦設定画面が開く、をそれぞれ確認。⑤既存ユーザー(全7ステージクリア済み)では上級リーグがcurrent+解放表示になることを確認(v1.36.0の移行ロジックとの整合性を確認)。⑥画面幅320/375/390/430/768pxの5パターンで横スクロールが発生しないことを確認(320px幅ではリーグカード名が2行に折り返って崩れる問題を発見し、`@media(max-width:350px)`でポートレート幅・フォントサイズ・パディングを縮小して解消。「お知らせ」ボタンの折り返しも同様に対応)。⑦既存メニュー(実績・戦績・設定・ルール)がすべて開けることを確認し、既存機能への非干渉を実証。⑧v1.30.0〜v1.36.0までの全既存回帰テスト(STAGE1誘導フロー、名前変更、初回本戦体験、STAGE2/本戦への非干渉)を再実行し、崩れが無いことを確認。スクリーンショットで実際の見た目を目視確認。
  **要作業**: Analytics/Firestoreの変更は無いため、Firebaseコンソールでの再公開作業は不要。
  **残っている懸念点**: 「今日のミッション」バッジ(`#missionBadge`)は未受取報酬の実データが無いため常に非表示のまま(将来ミッション/報酬システムを実装する際に接続する想定)。星評価・リーグ内の戦数進捗も同様に、実体ができ次第接続する必要がある。
  **運用**: `1.x.0`として`develop`ブランチにpush(`main`には反映していない)。Phase2(新チュートリアル本体)以降は引き続き未着手。

- **v1.38.0（develop）**: v1.37.0確認後、ユーザーから新規の注釈付きモックアップ画像(現行実装への「改善ポイント」8項目付きレビュー)が添付され「UIの修正をお願いします」との依頼を受けて実装。画像の指摘は、①教官キャラクターをもっと大きく表示、②ミッション導線をホーム中段から削除し別画面(下部メニュー)のみに戻す、③1画面に収まるようスクロールを無くす、④初級リーグの現在地・進行度を可視化、の4点が中心。一方で画像には「★★★★☆」の星評価や「あと2勝で解放」のような具体的カウントも描かれていたが、これらはv1.24.0で廃止済みの★評価制度や存在しない勝利数条件に相当し、本セッションで繰り返し確認してきた「実データが無いものは捏造しない」の原則に反するため、**採用しなかった**(星評価は非表示のまま、中級/上級の解放条件は既存のテキストのみで「あと◯勝」等のカウントは追加していない)。
  **実装**: ①`.mentor-say-home`を全面リデザイン: 顔アイコンを52px→76pxに拡大し、右寄せの小さな添え物だったレイアウトを、顔(左)+吹き出し(右、ツールチップ風の三角形付き)の全幅バナーに変更(`renderHomeMentorSay()`のDOM順序も顔→吹き出しに変更)。②ホーム中段の`.mission-bar`(HTML・CSS・`#missionBadge`)を削除し、ミッションへの導線は既存の下部メニュー「ミッション」タイル(`openMissions()`)のみに統一(画像の指摘⑤と一致)。③`renderLeagueCards()`に、初級リーグ限定で実データ(`learnClearedMax()`/`LEARN_STAGES.length`)による進行度バー(`.lc-progress`)を追加: 未着手時は非表示、進行中は「現在 STAGE n/7」+実際の割合%のバー、全クリア時は「STAGE 7/7 クリア済み」と表示。中級/上級は実体が無いため進行度表示を追加していない(既存の「◯◯クリアで解放」ラベルのみ)。④スクロール量を減らすため、`.home-screen`の`gap`を10px→8px、上下パディングを16/22px→12/16pxへ軽く圧縮。
  **検証**: Playwrightで、①ホームから`.mission-bar`が完全に消え、下部メニューの「ミッション」タイルは維持されていることを確認。②教官の顔画像が76px幅で描画されることを確認。③初級リーグ未着手時は進行度バーが出ず「プレイ」表示、STAGE1のみクリア時は「現在 STAGE 2/7」+14%のバー、全7クリア時は「STAGE 7/7 クリア済み」+「復習する」表示になることを確認(いずれも実データ連動)。④初級リーグ・フリー対戦それぞれのタップで既存のチュートリアルラダー・対戦設定画面が変わらず開くことを確認(回帰なし)。⑤画面幅320/375/390/430/768pxで横スクロールが発生しないことを確認。⑥画面高さ568/640/667/736/812/844pxで縦の超過量を計測し、圧縮前の状態と比べて改善したものの、iPhone SE相当(568px)で約15px・iPhone 6/7/8相当(667px)で約12pxの縦スクロールがわずかに残ることを確認(タップ領域や文字サイズを画像の要求どおり保った上でのトレードオフとして許容。完全な「スクロール不要」までは詰め切っていない)。
  **要作業**: Analytics/Firestoreの変更は無いため、Firebaseコンソールでの再公開作業は不要。
  **残っている懸念点**: 短い画面高さ(568〜736px)でわずかな縦スクロールが残る。星評価・中級/上級の解放カウント(あと◯勝等)は実データが無いため未実装のまま(将来、進行度や勝利数を管理するデータ構造ができ次第、同じ`.lc-progress`パターンで接続できる)。
  **運用**: `1.x.0`として`develop`ブランチにpush(`main`には反映していない)。Phase2(新チュートリアル本体)以降は引き続き未着手。

- **v1.39.0（develop）**: v1.38.0確認後、ユーザーから外部で作成されたと思われる非常に詳細な改修プロンプト(18項目、CSS実装例・推奨高さ配分・4サイズでのスクリーンショット確認・10項目の完了報告フォーマット付き)が「Claude Codeへそのまま渡してください」という形式で提示され、実装。要旨は「1画面(スクロール無し)に収める」「初級リーグを主役に、中級/上級は横2列でロック状態を明確に(プレイボタンを出さない)」「ミッション専用バー・大型設定ボタンを廃止し下部メニューへ統合」「教官キャラクターはコンパクトな横並び表示(44〜54px)に留める」の4点が中心。
  **実装**: ①`.home-screen`をflex中央寄せからCSS Grid(`justify-items:center; align-content:start;`)へ変更し、`height:100dvh`+`gap`/`padding`を`clamp()`で圧縮。ただし文字拡大等でコンテンツが収まらない場合に備え、指示例の`overflow:hidden`ではなく`overflow-y:auto`を維持(押しても消えないアクセシビリティ上のフォールバックとして意図的に採用。詳細は下記「懸念点」)。②プレイヤーヘッダー(`.home-top`)を1行のコンパクト表示に再設計: 称号バッジ・勝率バーを廃止し、`playerName`(実際に登録された名前)+称号を1行、簡易戦績「n戦・n勝」をもう1行で表示。お知らせは🔔アイコンのみ(文字ラベル廃止)。③v1.38.0で拡大した教官バナー(76px+吹き出しボックス)を、指示どおり44〜54pxのコンパクトな横並び表示に戻し、320px以下では2行目のセリフを非表示にして1行へ短縮。④リーグカードを3種類に再構成: 初級=`.league-card--featured`(横長・主役、進行度は実データ`learnClearedMax()`のみ使用)、中級/上級=`.league-card--compact`(2列グリッド、画像上/タイトル/説明省略/解放条件、**解放状態に関わらずプレイボタンを一切表示しない**設計に変更。実体が無い中級/上級は、たとえ移行処理で「解放」フラグが立っていても「準備中」ラベルのみでCTAボタンは出さない)、フリー対戦=`.league-card--free`(横長・小型、既存の`onHomePlay()`をそのまま再利用、差別化のため緑系の配色)。ロック中カード(`locked`かつ`!hasContent`)は`onclick`属性自体を付与せず`tabindex="-1"`にして実質的に無効化し、`aria-disabled="true"`を設定。⑤下部メニューを画像アセット(`btn-howto.png`等6種)+独立の大型`btn-setting.png`から、既存だが未使用だった`.hm-btn`(テキスト+絵文字アイコン)を3列×2行グリッドへ転用する形に変更し、「設定」もここへ統合(独立の大型設定ボタンを廃止)。ランクは実装が無いため、指示の推奨構成どおりホームから一時的に除去(`homeRankSoon()`は呼び出し元が無くなったため削除)。ミッションタイルには、実データ(`advancedCleared()`、上級ミッションTUT_ADVANCEDの完了フラグ)に基づく赤い点バッジのみ追加(報酬数などの捏造データは追加していない)。
  **見つけて直した不具合**: 実装中の確認で、プレイヤーヘッダーに実際の`playerName`を表示するよう変更したことで、名前登録直後にホームへ戻ってもヘッダーが登録前のプレースホルダ("―")のまま更新されない不具合を発見(`submitPlayerName()`が`playerName`を更新するのみで`renderHomeStats()`を呼んでいなかったため。既存の`hbTotal`/`hbTitle`表示でも本来は同じ不具合があったが、対戦数がその場で変わることは無いため症状が表面化していなかった)。`submitPlayerName()`末尾に`renderHomeStats()`呼び出しを追加して修正(名前の初回登録・設定画面からの再編集の両方に効く)。
  **検証**: Playwrightで、①`#leagueList`の構造が「featured 1枚 + `.league-grid`(compact 2枚) + free 1枚」の3ブロックであることを確認。②プレイヤーヘッダーの高さが要求範囲(50〜58px)内、お知らせボタンに文字ラベルが無いことを確認。③中級/上級カードは常に`.lc-cta`が存在しない(ロック中・解放済みで実体が無い場合の両方)ことを確認(既存ユーザーがSTAGE7クリア済みの状態を`page.addInitScript`でスクリプト実行前にlocalStorageへ注入し、移行処理後に中級/上級が「準備中」表示(ロックアイコン無し、しかしCTAボタンも無し)になることも別途確認)。④初級リーグ未着手/進行中(STAGE3/7・29%)/全クリア(進行バー無し「見習い卒業」)の3状態を確認。⑤初級リーグ・フリー対戦・下部メニュー(遊び方/ルール/設定)タップで既存の画面遷移が変わらず動作することを確認(回帰なし)。⑥画面幅320/375/390/430/768pxで横スクロールが発生しないことを確認。⑦要求された4サイズ(320×700, 375×812, 390×844, 430×932)でスクリーンショットを取得し、縦方向の超過量をそれぞれ11px/8px/7px/4pxまで削減できたことを確認(v1.38.0時点の15px/12px/10px/8pxからさらに圧縮。詳細は下記「懸念点」)。⑧STAGE1誘導フロー・名前変更・段階表示・詳細展開・STAGE1 CLEAR演出・ラダー連打耐性・戻る・リロード耐性の既存回帰スイートを再実行し、崩れが無いことを確認(1回目の実行で「決定」ボタン検出に失敗する既知のフレーク(本セッションで過去にも発生した、原因不明の一過性の資源競合と推測される事象)が発生したが、直後の再実行では全項目が問題なく通過し、コード変更に起因する再現性のある不具合ではないことを確認)。
  **要作業**: Analytics/Firestoreの変更は無いため、Firebaseコンソールでの再公開作業は不要。
  **残っている懸念点**: (a) 指示のCSS例は`.home-screen{overflow:hidden}`を提示していたが、これは文字サイズ拡大や極端に低い高さなど「収まりきらない場合はアクセシビリティを優先してスクロールを許可する」という同じ指示内の例外規定と矛盾する(`overflow:hidden`はコンテンツを切り捨てるだけでスクロールを許可しない)。そのため意図的に`overflow-y:auto`のまま実装し、通常のスマートフォンでは実質的にスクロール不要(4〜11pxの誤差のみ)としつつ、収まらない場合は安全にスクロールできる状態を優先した。(b) 320〜430px×700〜932pxの範囲では縦の超過は最大11px(320×700)まで圧縮できたが、完全な0pxではない。タップ領域(44px以上)や文字サイズを既存の可読性要件より詰めればさらに削れる余地はあるが、可読性とのトレードオフのため現状で止めている。(c) 星評価・中級/上級の「あと◯勝で解放」等は引き続き実データが無いため非表示のまま。(d) ランクメニューはホームから一時的に除去(実装され次第、下部メニューへ戻すか判断が必要)。
  **運用**: `1.x.0`として`develop`ブランチにpush(`main`には反映していない)。Phase2(新チュートリアル本体)以降は引き続き未着手。

- **v1.40.0（develop・現在）**: v1.39.0確認後、ユーザーから「かなり良くなった(85点)」という評価とともに、AI生成と思われる高完成度なモックアップ画像1枚+10項目の磨き込み指摘が提示され実装。指摘は①初級リーグをもっと主役に(最優先)②キャラクター画像を拡大③教官アストラを吹き出し風に④フリー対戦の配色(黒×金の世界観に対し青緑が浮いている)⑤中級/上級のロック表示に解放条件テキストを⑥プレイヤーヘッダーの名前切れ・現在地優先表示⑦DEV PREVIEWを簡潔に⑧下部メニューアイコンの統一感⑨カード高さを10〜15%圧縮⑩背景に星・粒子の10点。一方でモックアップには「第2戦/全5戦」「★★★◇◇」「Lv.4」「見習い卒業まであと3戦!」等、実データに対応しない表示(v1.24.0で廃止済みの★評価、7ステージ制なのに5戦制で表現、存在しないレベル制)も含まれていたため、これらは**そのまま採用せず**、本セッションを通じての「実データが無いものは捏造しない」方針を継続した。
  **実装**: ①`.league-card--featured`に常時ゴールドのグラデーション背景+太めの縁取り(1.5px、current時2px)+内側シャドウを追加し、他カードより視覚的に格上げ(currentでなくても目立つよう常時適用)。②キャラクター立ち絵を拡大: featured 68〜80px→82〜100px、compact 48〜58px→54〜66px、free 46〜56px→50〜60px(⑨のカード高さ圧縮で確保した余白を充てる形で実施)。③`.mentor-say-home .msh-bubble`に枠線+ツールチップ状の三角(`::before`)を追加し、吹き出し風の見た目に変更。④フリー対戦カードの配色を青緑(#8fe3c2系)から、背景=深緑のグラデーション、縁取り=深緑、カード名=金、CTAボタン=既存の金グラデーション(他カードと共通)へ変更し、単独の配色浮きを解消。⑤中級/上級の解放条件テキスト(🔒+「◯◯クリアで解放」)は既存実装で対応済みだったため、文字サイズ・アイコンサイズをわずかに拡大する程度の微調整に留めた。⑥プレイヤーヘッダーを「称号(なければ名前、どちらも無ければ「仮面の騎士」)」を1行目、2行目は初級リーグ進行中なら実データ(`LEARN_STAGES.length - learnClearedMax()`)による「初級卒業まであとn戦」、それ以外は簡易戦績、という優先順位に変更(「Lv.4」のような実在しないレベル表記は採用せず、既存の実データだけで「現在地」を表現)。`.hb-name`/`.hb-record`の`max-width`固定値を撤廃して`.home-badge`を`flex:1`化し、名前が途中で切れる問題を解消。⑦`#devEnvBadge`を`.home-top`内(バッジとベルの間)へ移設し、文言を「DEV PREVIEW」→「DEV」の小さなピルに変更。⑧下部メニュー6タイルの絵文字アイコン(📖📜🏆🎯📊⚙️)を、統一されたstroke線画のinline SVG(本・巻物・トロフィー・的・棒グラフ・歯車、いずれも`stroke="currentColor"`統一)へ差し替え。⑨カード高さを全体的に10〜15%程度圧縮(featured: 104〜124px→90〜108px、compact: 98〜118px→84〜102px、free: 66〜82px→58〜72px)、カード間gap・パディングも合わせて微調整し、拡大したキャラクター画像とのバランスをとった。⑩`.home-screen`の背景に、8点の極小radial-gradient(白・金の点在)+1枚の薄い青系radial-gradientレイヤーを追加(静止画像のみ、アニメーション無しなので`prefers-reduced-motion`は自然に満たす)。
  **検証**: Playwrightで、①既存の回帰スイート一式(v1.39.0と同じ6ケース、STAGE1誘導フロー・名前変更・段階表示・詳細展開・CLEAR演出・ラダー連打・戻る・リロード耐性含む)を再実行し崩れが無いことを確認(既知のフレークが1回発生したが再実行で解消、コード起因でないことを確認済み)。②画面幅320/375/390/430/768pxで横スクロールが発生しないことを確認。③要求4サイズ(320×700〜430×932)で縦の超過量が11px/8px/7px/4pxとv1.39.0からほぼ変わらず(キャラクター拡大とカード圧縮が打ち消し合い、1画面表示の水準を維持できていることを確認)。④背景のグラデーションレイヤー数が12層(星8+青1+既存3)になっていることをComputedStyleで確認。⑤中級カードのportraitが390px幅で66pxまで拡大されていることを確認。⑥プレイヤーヘッダーが称号未設定時にplayerNameへ正しくフォールバックすることを確認。⑦フリー対戦のCTAボタンが他カードと共通の金グラデーションになっていること(緑系のCTA配色を削除済み)をコードで確認。
  **要作業**: Analytics/Firestoreの変更は無いため、Firebaseコンソールでの再公開作業は不要。
  **残っている懸念点**: 星評価・「Lv.4」・「あと◯勝で解放」のような実データに対応しない表示は、モックアップにあったが引き続き非採用(実データ基盤ができ次第、対応する項目から個別に追加を検討)。縦のわずかな超過(最大11px、320×700)は引き続き残っている。
  **運用**: `1.x.0`として`develop`ブランチにpush(`main`には反映していない)。Phase2(新チュートリアル本体)以降は引き続き未着手。

## 9. 次にやり得ること（未確定・候補）

> 外部レビュー(9.2/10)の主な指摘は v0.33〜v0.37 で対応済み（CPU個性/難易度・勝敗演出・リザルト充実・初心者ガイド・ミッション式チュートリアル）。以下は残りの候補。

- **効果音**: 意図的に未実装（自動再生の煩わしさ回避のため）。入れるならフッターにトグル（既定OFF）＋WebAudioの控えめなSFX推奨。
- さらなるバランス検証（新ログ回収 → シミュレータ再実行）。※シミュレータは scratchpad(揮発) にあった `sim.js`/`cpumod.js`/`verify5.js` 方式。必要なら再作成：本物のjudge/配札/得点＋index.htmlからCPU関数を抽出し、強い参照(greedy)と対戦させ勝率を測る。移植の忠実性は `cpumod.js`(実関数抽出)＋統一エンジンで検証できる。
- 長期リプレイ機能（実績・デイリー・ランク等）、上級者向けリッチ表示の作り込み。
- コード分割（保守性向上）※ただし単一HTML＝GitHub Pages運用の前提あり。要相談。
- ※これらは未確定。実装前にユーザーへ方向性を確認すること。

## 10. 引き継ぎ時の注意

- コミットは日本語の要約＋箇条書き（既存の履歴に倣う）。バージョンは `GAME_VERSION` と `CHANGELOG` を**必ず両方**更新。
- モデル識別子（`claude-opus-4-8` 等）はコミット/コード/PR等の成果物に**書かない**（チャット内のみ）。
- PR はユーザーが明示的に頼んだ時だけ作成。
